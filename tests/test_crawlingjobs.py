"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: ae8ac6392d00

from pipeshub_sdk import Pipeshub, models
from pipeshub_sdk.utils import parse_datetime
from tests.test_client import create_test_http_client


def test_crawlingjobs_schedule_crawling_job_daily_sync():
    test_http_client = create_test_http_client("scheduleCrawlingJob-dailySync")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "daily",
                "is_enabled": True,
                "timezone": "UTC",
                "hour": 2,
                "minute": 0,
            },
            priority=5,
            max_retries=3,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_schedule_crawling_job_hourly_sync():
    test_http_client = create_test_http_client("scheduleCrawlingJob-hourlySync")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "hourly",
                "is_enabled": True,
                "timezone": "America/New_York",
                "minute": 30,
                "interval": 4,
            },
            priority=3,
            max_retries=5,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_schedule_crawling_job_weekly_sync():
    test_http_client = create_test_http_client("scheduleCrawlingJob-weeklySync")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "weekly",
                "is_enabled": True,
                "timezone": "Europe/London",
                "days_of_week": [
                    1,
                    3,
                    5,
                ],
                "hour": 3,
                "minute": 0,
            },
            priority=5,
            max_retries=3,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_schedule_crawling_job_custom_cron():
    test_http_client = create_test_http_client("scheduleCrawlingJob-customCron")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "custom",
                "is_enabled": True,
                "timezone": "UTC",
                "cron_expression": "0 */6 * * *",
                "description": "Every 6 hours",
            },
            priority=5,
            max_retries=3,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_schedule_crawling_job_one_time_sync():
    test_http_client = create_test_http_client("scheduleCrawlingJob-oneTimeSync")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "once",
                "is_enabled": True,
                "timezone": "UTC",
                "scheduled_time": parse_datetime("2027-12-25T10:00:00Z"),
            },
            priority=1,
            max_retries=3,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_schedule_crawling_job():
    test_http_client = create_test_http_client("scheduleCrawlingJob")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.schedule(
            connector="drive",
            connector_id="507f1f77bcf86cd799439011",
            schedule_config={
                "schedule_type": "once",
                "is_enabled": True,
                "timezone": "UTC",
                "scheduled_time": parse_datetime("2024-12-25T10:00:00Z"),
            },
            priority=5,
            max_retries=3,
            timeout=300000,
        )
        assert res is not None
        assert res == models.ScheduleCrawlingJobResponse(
            success=True,
            message="Crawling job scheduled successfully",
            data=models.ScheduleCrawlingJobData(
                job_id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                connector="drive",
                connector_id="507f1f77bcf86cd799439011",
                schedule_config=models.DailyScheduleConfig(
                    is_enabled=True,
                    timezone="UTC",
                    hour=2,
                    minute=0,
                ),
                scheduled_at=parse_datetime("2024-01-15T10:30:00Z"),
            ),
        )


def test_crawlingjobs_get_crawling_job_status():
    test_http_client = create_test_http_client("getCrawlingJobStatus")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.get_status(
            connector="drive", connector_id="507f1f77bcf86cd799439011"
        )
        assert res is not None
        assert res == models.GetCrawlingJobStatusResponse(
            success=True,
            message="Job status retrieved successfully",
            data=models.JobStatus(
                id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                name="crawl-drive-507f1f77bcf86cd799439011",
                data=models.CrawlingJobData(
                    connector="drive",
                    connector_id="507f1f77bcf86cd799439011",
                    schedule_config=models.HourlyScheduleConfig(
                        is_enabled=True,
                        timezone="UTC",
                        minute=30,
                        interval=1,
                    ),
                    org_id="507f1f77bcf86cd799439012",
                    user_id="507f1f77bcf86cd799439013",
                    timestamp=parse_datetime("2024-11-06T06:42:05.632Z"),
                ),
                delay=3600000,
                timestamp=1703520000000,
                attempts_made=0,
                failed_reason=None,
                state="waiting",
            ),
        )


def test_crawlingjobs_remove_crawling_job():
    test_http_client = create_test_http_client("removeCrawlingJob")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.remove(
            connector="drive", connector_id="507f1f77bcf86cd799439011"
        )
        assert res is not None
        assert res == models.RemoveCrawlingJobResponse(
            success=True,
            message="Crawling job removed successfully",
        )


def test_crawlingjobs_get_all_crawling_job_status():
    test_http_client = create_test_http_client("getAllCrawlingJobStatus")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.list_all_statuses()
        assert res is not None
        assert res == models.GetAllCrawlingJobStatusResponse(
            data=[
                models.JobStatus(
                    id="crawl-drive-507f1f77bcf86cd799439011-507f1f77bcf86cd799439012",
                    name="crawl-drive-507f1f77bcf86cd799439011",
                    data=models.CrawlingJobData(
                        connector="drive",
                        connector_id="507f1f77bcf86cd799439011",
                        schedule_config=models.MonthlyScheduleConfig(
                            is_enabled=True,
                            timezone="UTC",
                            day_of_month=1,
                            hour=4,
                            minute=0,
                        ),
                        org_id="507f1f77bcf86cd799439012",
                        user_id="507f1f77bcf86cd799439013",
                        timestamp=parse_datetime("2024-11-27T06:37:05.054Z"),
                    ),
                    delay=3600000,
                    timestamp=1703520000000,
                    attempts_made=0,
                    failed_reason=None,
                    state="waiting",
                ),
            ],
        )


def test_crawlingjobs_remove_all_crawling_job():
    test_http_client = create_test_http_client("removeAllCrawlingJob")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.remove_all()
        assert res is not None
        assert res == models.RemoveAllCrawlingJobResponse()


def test_crawlingjobs_pause_crawling_job():
    test_http_client = create_test_http_client("pauseCrawlingJob")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.pause(
            connector="drive", connector_id="507f1f77bcf86cd799439011"
        )
        assert res is not None
        assert res == models.PauseCrawlingJobResponse()


def test_crawlingjobs_resume_crawling_job():
    test_http_client = create_test_http_client("resumeCrawlingJob")

    with Pipeshub(
        server_url="http://localhost:3000/api/v1",
        security=models.Security(),
        client=test_http_client,
    ) as pipeshub:
        assert pipeshub is not None

        res = pipeshub.crawling_jobs.resume(
            connector="drive", connector_id="507f1f77bcf86cd799439011"
        )
        assert res is not None
        assert res == models.ResumeCrawlingJobResponse()
