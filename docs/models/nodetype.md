# NodeType

Type of the node

## Example Usage

```python
from pipeshub_sdk.models import NodeType

# Open enum: unrecognized values are captured as UnrecognizedStr
value: NodeType = "app"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"app"`
- `"recordGroup"`
- `"folder"`
- `"record"`
