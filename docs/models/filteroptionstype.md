# FilterOptionsType

Filter input type

## Example Usage

```python
from pipeshub_sdk.models import FilterOptionsType

# Open enum: unrecognized values are captured as UnrecognizedStr
value: FilterOptionsType = "select"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"select"`
- `"multiselect"`
- `"text"`
- `"boolean"`
