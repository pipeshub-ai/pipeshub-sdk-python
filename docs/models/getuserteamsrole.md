# GetUserTeamsRole

## Example Usage

```python
from pipeshub_sdk.models import GetUserTeamsRole

# Open enum: unrecognized values are captured as UnrecognizedStr
value: GetUserTeamsRole = "owner"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"owner"`
- `"admin"`
- `"member"`
- `"viewer"`
