"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .persistedsemanticsearchcitation import (
    PersistedSemanticSearchCitation,
    PersistedSemanticSearchCitationTypedDict,
)
from .persistedsemanticsearchsharedwithentry import (
    PersistedSemanticSearchSharedWithEntry,
    PersistedSemanticSearchSharedWithEntryTypedDict,
)
from datetime import datetime
from pipeshub_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class PersistedSemanticSearchTypedDict(TypedDict):
    r"""Persisted search document as returned to clients.
    `records` is a string-valued map: each value is `JSON.stringify(<record>)`,
    keyed by the source record's `_id` or `_key`. Clients must `JSON.parse`
    each value to recover the underlying record object (whose shape resembles
    `SemanticSearchGraphRecord`). This intentionally differs from the
    `searchResponse.records` array on the POST `/search` response, which is
    passed through from the retrieval service untouched.

    """

    id: str
    v: int
    query: str
    limit: int
    org_id: str
    user_id: str
    citation_ids: List[PersistedSemanticSearchCitationTypedDict]
    records: Dict[str, str]
    r"""Map of source-record id (or `_key`) to a JSON-stringified record object.
    Clients must `JSON.parse` each value before reading fields.

    """
    is_shared: bool
    shared_with: List[PersistedSemanticSearchSharedWithEntryTypedDict]
    is_archived: bool
    created_at: datetime
    updated_at: datetime
    share_link: NotRequired[str]
    r"""Set once the search has been shared via `/search/{searchId}/share`."""
    archived_by: NotRequired[Nullable[str]]
    r"""User ID of the last user who archived this row, or `null` after
    an unarchive cleared the archive state. Absent on rows that have
    never been archived. Currently-archived rows cannot reach this
    endpoint because `buildFilter` enforces `isArchived: false`.

    """


class PersistedSemanticSearch(BaseModel):
    r"""Persisted search document as returned to clients.
    `records` is a string-valued map: each value is `JSON.stringify(<record>)`,
    keyed by the source record's `_id` or `_key`. Clients must `JSON.parse`
    each value to recover the underlying record object (whose shape resembles
    `SemanticSearchGraphRecord`). This intentionally differs from the
    `searchResponse.records` array on the POST `/search` response, which is
    passed through from the retrieval service untouched.

    """

    id: Annotated[str, pydantic.Field(alias="_id")]

    v: Annotated[int, pydantic.Field(alias="__v")]

    query: str

    limit: int

    org_id: Annotated[str, pydantic.Field(alias="orgId")]

    user_id: Annotated[str, pydantic.Field(alias="userId")]

    citation_ids: Annotated[
        List[PersistedSemanticSearchCitation], pydantic.Field(alias="citationIds")
    ]

    records: Dict[str, str]
    r"""Map of source-record id (or `_key`) to a JSON-stringified record object.
    Clients must `JSON.parse` each value before reading fields.

    """

    is_shared: Annotated[bool, pydantic.Field(alias="isShared")]

    shared_with: Annotated[
        List[PersistedSemanticSearchSharedWithEntry], pydantic.Field(alias="sharedWith")
    ]

    is_archived: Annotated[bool, pydantic.Field(alias="isArchived")]

    created_at: Annotated[datetime, pydantic.Field(alias="createdAt")]

    updated_at: Annotated[datetime, pydantic.Field(alias="updatedAt")]

    share_link: Annotated[Optional[str], pydantic.Field(alias="shareLink")] = None
    r"""Set once the search has been shared via `/search/{searchId}/share`."""

    archived_by: Annotated[
        OptionalNullable[str], pydantic.Field(alias="archivedBy")
    ] = UNSET
    r"""User ID of the last user who archived this row, or `null` after
    an unarchive cleared the archive state. Absent on rows that have
    never been archived. Currently-archived rows cannot reach this
    endpoint because `buildFilter` enforces `isArchived: false`.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["shareLink", "archivedBy"])
        nullable_fields = set(["archivedBy"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    PersistedSemanticSearch.model_rebuild()
except NameError:
    pass
