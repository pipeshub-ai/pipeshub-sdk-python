"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .patlistitem import PatListItem, PatListItemTypedDict
from pipeshub_sdk.types import BaseModel
from typing import List
from typing_extensions import TypedDict


class ListPatResponseTypedDict(TypedDict):
    r"""Response body for `GET /personal-access-tokens` (`listTokens`) — the
    caller's own active tokens, capped at 100 most-recent server-side.
    Unlike the admin list, this is a flat array with no pagination
    envelope and no owner fields (it's implicitly scoped to the caller).

    """

    tokens: List[PatListItemTypedDict]


class ListPatResponse(BaseModel):
    r"""Response body for `GET /personal-access-tokens` (`listTokens`) — the
    caller's own active tokens, capped at 100 most-recent server-side.
    Unlike the admin list, this is a flat array with no pagination
    envelope and no owner fields (it's implicitly scoped to the caller).

    """

    tokens: List[PatListItem]
