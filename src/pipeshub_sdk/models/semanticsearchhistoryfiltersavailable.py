"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .semanticsearchhistorydaterange import (
    SemanticSearchHistoryDateRange,
    SemanticSearchHistoryDateRangeTypedDict,
)
from .semanticsearchhistoryfiltertoggle import (
    SemanticSearchHistoryFilterToggle,
    SemanticSearchHistoryFilterToggleTypedDict,
)
from .semanticsearchhistorypaginationfield import (
    SemanticSearchHistoryPaginationField,
    SemanticSearchHistoryPaginationFieldTypedDict,
)
from .semanticsearchhistorysortfield import (
    SemanticSearchHistorySortField,
    SemanticSearchHistorySortFieldTypedDict,
)
from pipeshub_sdk.types import BaseModel
import pydantic
from typing_extensions import Annotated, TypedDict


class SemanticSearchHistoryFiltersAvailablePaginationTypedDict(TypedDict):
    page: SemanticSearchHistoryPaginationFieldTypedDict
    limit: SemanticSearchHistoryPaginationFieldTypedDict


class SemanticSearchHistoryFiltersAvailablePagination(BaseModel):
    page: SemanticSearchHistoryPaginationField

    limit: SemanticSearchHistoryPaginationField


class SortingTypedDict(TypedDict):
    sort_by: SemanticSearchHistorySortFieldTypedDict
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """
    sort_order: SemanticSearchHistorySortFieldTypedDict
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """


class Sorting(BaseModel):
    sort_by: Annotated[SemanticSearchHistorySortField, pydantic.Field(alias="sortBy")]
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """

    sort_order: Annotated[
        SemanticSearchHistorySortField, pydantic.Field(alias="sortOrder")
    ]
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """


class DateFiltersTypedDict(TypedDict):
    date_range: SemanticSearchHistoryDateRangeTypedDict


class DateFilters(BaseModel):
    date_range: Annotated[
        SemanticSearchHistoryDateRange, pydantic.Field(alias="dateRange")
    ]


class MessageFiltersTypedDict(TypedDict):
    message_type: SemanticSearchHistoryFilterToggleTypedDict
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """


class MessageFilters(BaseModel):
    message_type: Annotated[
        SemanticSearchHistoryFilterToggle, pydantic.Field(alias="messageType")
    ]
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """


class SortingMessagesTypedDict(TypedDict):
    sort_by: SemanticSearchHistorySortFieldTypedDict
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """
    sort_order: SemanticSearchHistorySortFieldTypedDict
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """


class SortingMessages(BaseModel):
    sort_by: Annotated[SemanticSearchHistorySortField, pydantic.Field(alias="sortBy")]
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """

    sort_order: Annotated[
        SemanticSearchHistorySortField, pydantic.Field(alias="sortOrder")
    ]
    r"""Used for `available.sorting.{sortBy,sortOrder}` and
    `available.sortingMessages.{sortBy,sortOrder}`. The `applied` flag
    is present on `sorting.*` and absent on `sortingMessages.*`, so it
    is optional here.

    """


class SemanticSearchHistoryFiltersAvailableTypedDict(TypedDict):
    r"""Catalogue of filters the endpoint supports, plus their current values
    and `applied` flags. Built by `buildFiltersMetadata` (utils.ts:430-624).

    """

    shared: SemanticSearchHistoryFilterToggleTypedDict
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """
    tags: SemanticSearchHistoryFilterToggleTypedDict
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """
    min_messages: SemanticSearchHistoryFilterToggleTypedDict
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """
    search: SemanticSearchHistoryFilterToggleTypedDict
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """
    pagination: SemanticSearchHistoryFiltersAvailablePaginationTypedDict
    sorting: SortingTypedDict
    date_filters: DateFiltersTypedDict
    message_filters: MessageFiltersTypedDict
    sorting_messages: SortingMessagesTypedDict


class SemanticSearchHistoryFiltersAvailable(BaseModel):
    r"""Catalogue of filters the endpoint supports, plus their current values
    and `applied` flags. Built by `buildFiltersMetadata` (utils.ts:430-624).

    """

    shared: SemanticSearchHistoryFilterToggle
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """

    tags: SemanticSearchHistoryFilterToggle
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """

    min_messages: Annotated[
        SemanticSearchHistoryFilterToggle, pydantic.Field(alias="minMessages")
    ]
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """

    search: SemanticSearchHistoryFilterToggle
    r"""Generic \"filter X is available, current value is Y\" block used for
    `shared`, `tags`, `minMessages`, `search`, and `messageType`. Either
    `type` (free-form value) or `values` (enum of allowed strings) is
    present, not both. `current` is the caller-supplied value passed
    through from `req.query`, hence string-or-null even when `type` is
    `'number'`.

    """

    pagination: SemanticSearchHistoryFiltersAvailablePagination

    sorting: Sorting

    date_filters: Annotated[DateFilters, pydantic.Field(alias="dateFilters")]

    message_filters: Annotated[MessageFilters, pydantic.Field(alias="messageFilters")]

    sorting_messages: Annotated[
        SortingMessages, pydantic.Field(alias="sortingMessages")
    ]


try:
    Sorting.model_rebuild()
except NameError:
    pass
try:
    DateFilters.model_rebuild()
except NameError:
    pass
try:
    MessageFilters.model_rebuild()
except NameError:
    pass
try:
    SortingMessages.model_rebuild()
except NameError:
    pass
try:
    SemanticSearchHistoryFiltersAvailable.model_rebuild()
except NameError:
    pass
