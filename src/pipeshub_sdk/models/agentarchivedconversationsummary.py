"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AgentArchivedConversationSummaryTypedDict(TypedDict):
    r"""Archive counts and bounds for the current result page returned by
    `GET /agents/{agentKey}/conversations/show/archives`.

    """

    total_archived: NotRequired[int]
    r"""Total archived conversations matching the filter"""
    oldest_archive: NotRequired[datetime]
    r"""Archive timestamp of the first item in the current page. Omitted when the page is empty."""
    newest_archive: NotRequired[datetime]
    r"""Archive timestamp of the last item in the current page. Omitted when the page is empty."""


class AgentArchivedConversationSummary(BaseModel):
    r"""Archive counts and bounds for the current result page returned by
    `GET /agents/{agentKey}/conversations/show/archives`.

    """

    total_archived: Annotated[Optional[int], pydantic.Field(alias="totalArchived")] = (
        None
    )
    r"""Total archived conversations matching the filter"""

    oldest_archive: Annotated[
        Optional[datetime], pydantic.Field(alias="oldestArchive")
    ] = None
    r"""Archive timestamp of the first item in the current page. Omitted when the page is empty."""

    newest_archive: Annotated[
        Optional[datetime], pydantic.Field(alias="newestArchive")
    ] = None
    r"""Archive timestamp of the last item in the current page. Omitted when the page is empty."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["totalArchived", "oldestArchive", "newestArchive"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AgentArchivedConversationSummary.model_rebuild()
except NameError:
    pass
