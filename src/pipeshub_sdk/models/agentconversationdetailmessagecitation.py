"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .citation import Citation, CitationTypedDict
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AgentConversationDetailMessageCitationTypedDict(TypedDict):
    r"""Citation entry returned inside a conversation message after the
    handler populates `messages.citations.citationId` and rewrites each
    item to `{ citationId, citationData }`.

    """

    citation_id: NotRequired[str]
    citation_data: NotRequired[CitationTypedDict]
    r"""A populated citation document. Represents a single chunk of source
    content (e.g. a passage from a document or record) referenced by an
    AI response, together with its provenance metadata.

    """


class AgentConversationDetailMessageCitation(BaseModel):
    r"""Citation entry returned inside a conversation message after the
    handler populates `messages.citations.citationId` and rewrites each
    item to `{ citationId, citationData }`.

    """

    citation_id: Annotated[Optional[str], pydantic.Field(alias="citationId")] = None

    citation_data: Annotated[
        Optional[Citation], pydantic.Field(alias="citationData")
    ] = None
    r"""A populated citation document. Represents a single chunk of source
    content (e.g. a passage from a document or record) referenced by an
    AI response, together with its provenance metadata.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["citationId", "citationData"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AgentConversationDetailMessageCitation.model_rebuild()
except NameError:
    pass
