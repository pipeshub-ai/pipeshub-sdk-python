"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentcapabilities import AgentCapabilities, AgentCapabilitiesTypedDict
from .appliedfilters import AppliedFilters, AppliedFiltersTypedDict
from .chatattachmentref import ChatAttachmentRef, ChatAttachmentRefTypedDict
from .filters import Filters, FiltersTypedDict
from datetime import datetime
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


AgentAddMessageStreamRequestChatMode = Literal["quick",]
r"""Required execution mode. Scoped agent conversations currently
support only `quick`.

"""


AgentAddMessageStreamRequestProtocol = Literal["agui",]
r"""AG-UI is the only supported wire protocol. When present must be
`\"agui\"`. Omitting the field is equivalent — the server always
uses the AG-UI vocabulary (see `AgentMessageStreamSSEEvent`).
Kept in the schema for backward compatibility with callers that
already send it.

"""


class AgentAddMessageStreamRequestTypedDict(TypedDict):
    r"""Request body for `POST /agents/{agentKey}/conversations/{conversationId}/messages/stream`.
    `query` and `chatMode: quick` are required; all other fields are
    optional overrides. Unknown fields are stripped during validation.

    """

    query: str
    r"""User follow-up prompt to append to the existing agent conversation.
    Saved as a new `user_query` message before the upstream AI stream
    starts.

    """
    chat_mode: AgentAddMessageStreamRequestChatMode
    r"""Required execution mode. Scoped agent conversations currently
    support only `quick`.

    """
    filters: NotRequired[FiltersTypedDict]
    r"""Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
    be a valid UUID. Omit to let the agent use its stored defaults;
    send `{ \"apps\": [], \"kb\": [] }` to force no knowledge sources for this turn.

    """
    applied_filters: NotRequired[AppliedFiltersTypedDict]
    r"""UI filter state persisted on the saved user message. Not used for
    retrieval and not forwarded to the upstream agent backend.

    """
    attachments: NotRequired[List[ChatAttachmentRefTypedDict]]
    r"""Uploaded attachments to ground this turn. Each entry references a
    record id returned from the agent attachment upload endpoint.

    """
    model_key: NotRequired[str]
    r"""AI model configuration id override for this turn. Omit to use the
    agent's default model.

    """
    model_name: NotRequired[str]
    r"""Provider model name (the underlying LLM identifier)."""
    model_friendly_name: NotRequired[str]
    r"""Friendly UI label for the selected model."""
    timezone: NotRequired[str]
    r"""Client IANA timezone, such as `America/New_York`. Helps the agent
    resolve relative date references in the prompt.

    """
    current_time: NotRequired[datetime]
    r"""Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
    offset). Sent alongside `timezone` for time-aware answers.

    """
    tools: NotRequired[List[str]]
    r"""Allowed tool ids for this turn, such as `jira.create_issue`. Omit
    to let the agent use its default toolset; send `[]` to disable
    tools for this turn.

    """
    protocol: NotRequired[AgentAddMessageStreamRequestProtocol]
    r"""AG-UI is the only supported wire protocol. When present must be
    `\"agui\"`. Omitting the field is equivalent — the server always
    uses the AG-UI vocabulary (see `AgentMessageStreamSSEEvent`).
    Kept in the schema for backward compatibility with callers that
    already send it.

    """
    agent_capabilities: NotRequired[AgentCapabilitiesTypedDict]
    r"""Per-request agent capability toggles. Only meaningful when `chatMode`
    selects an agent mode; ignored otherwise. Each field falls back to its
    own `default` below when omitted — a missing flag is not uniformly
    `true`. Omitting the whole object applies every default.

    """


class AgentAddMessageStreamRequest(BaseModel):
    r"""Request body for `POST /agents/{agentKey}/conversations/{conversationId}/messages/stream`.
    `query` and `chatMode: quick` are required; all other fields are
    optional overrides. Unknown fields are stripped during validation.

    """

    query: str
    r"""User follow-up prompt to append to the existing agent conversation.
    Saved as a new `user_query` message before the upstream AI stream
    starts.

    """

    chat_mode: Annotated[
        AgentAddMessageStreamRequestChatMode, pydantic.Field(alias="chatMode")
    ]
    r"""Required execution mode. Scoped agent conversations currently
    support only `quick`.

    """

    filters: Optional[Filters] = None
    r"""Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
    be a valid UUID. Omit to let the agent use its stored defaults;
    send `{ \"apps\": [], \"kb\": [] }` to force no knowledge sources for this turn.

    """

    applied_filters: Annotated[
        Optional[AppliedFilters], pydantic.Field(alias="appliedFilters")
    ] = None
    r"""UI filter state persisted on the saved user message. Not used for
    retrieval and not forwarded to the upstream agent backend.

    """

    attachments: Optional[List[ChatAttachmentRef]] = None
    r"""Uploaded attachments to ground this turn. Each entry references a
    record id returned from the agent attachment upload endpoint.

    """

    model_key: Annotated[Optional[str], pydantic.Field(alias="modelKey")] = None
    r"""AI model configuration id override for this turn. Omit to use the
    agent's default model.

    """

    model_name: Annotated[Optional[str], pydantic.Field(alias="modelName")] = None
    r"""Provider model name (the underlying LLM identifier)."""

    model_friendly_name: Annotated[
        Optional[str], pydantic.Field(alias="modelFriendlyName")
    ] = None
    r"""Friendly UI label for the selected model."""

    timezone: Optional[str] = None
    r"""Client IANA timezone, such as `America/New_York`. Helps the agent
    resolve relative date references in the prompt.

    """

    current_time: Annotated[Optional[datetime], pydantic.Field(alias="currentTime")] = (
        None
    )
    r"""Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
    offset). Sent alongside `timezone` for time-aware answers.

    """

    tools: Optional[List[str]] = None
    r"""Allowed tool ids for this turn, such as `jira.create_issue`. Omit
    to let the agent use its default toolset; send `[]` to disable
    tools for this turn.

    """

    protocol: Optional[AgentAddMessageStreamRequestProtocol] = None
    r"""AG-UI is the only supported wire protocol. When present must be
    `\"agui\"`. Omitting the field is equivalent — the server always
    uses the AG-UI vocabulary (see `AgentMessageStreamSSEEvent`).
    Kept in the schema for backward compatibility with callers that
    already send it.

    """

    agent_capabilities: Annotated[
        Optional[AgentCapabilities], pydantic.Field(alias="agentCapabilities")
    ] = None
    r"""Per-request agent capability toggles. Only meaningful when `chatMode`
    selects an agent mode; ignored otherwise. Each field falls back to its
    own `default` below when omitted — a missing flag is not uniformly
    `true`. Omitting the whole object applies every default.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "filters",
                "appliedFilters",
                "attachments",
                "modelKey",
                "modelName",
                "modelFriendlyName",
                "timezone",
                "currentTime",
                "tools",
                "protocol",
                "agentCapabilities",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AgentAddMessageStreamRequest.model_rebuild()
except NameError:
    pass
