"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .semanticsearchappliedfilters import (
    SemanticSearchAppliedFilters,
    SemanticSearchAppliedFiltersTypedDict,
)
from .semanticsearchgraphrecord import (
    SemanticSearchGraphRecord,
    SemanticSearchGraphRecordTypedDict,
)
from .semanticsearchhit import SemanticSearchHit, SemanticSearchHitTypedDict
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SemanticSearchAiResponseTypedDict(TypedDict):
    r"""Payload returned by the AI retrieval service for a semantic search (embedded in `searchResponse`).
    Optional `virtual_to_record_map` maps each virtual record id (string key) to one resolved graph record document.
    Empty responses from the retrieval layer omit `virtual_to_record_map`; success payloads may include it alongside hits and records.

    """

    search_results: List[SemanticSearchHitTypedDict]
    records: List[SemanticSearchGraphRecordTypedDict]
    status: str
    status_code: int
    message: str
    applied_filters: NotRequired[SemanticSearchAppliedFiltersTypedDict]
    r"""Present when KB filters were applied to the search request."""
    virtual_to_record_map: NotRequired[Dict[str, SemanticSearchGraphRecordTypedDict]]
    r"""Maps virtual record id (object property name) to the accessible graph record document for that id."""


class SemanticSearchAiResponse(BaseModel):
    r"""Payload returned by the AI retrieval service for a semantic search (embedded in `searchResponse`).
    Optional `virtual_to_record_map` maps each virtual record id (string key) to one resolved graph record document.
    Empty responses from the retrieval layer omit `virtual_to_record_map`; success payloads may include it alongside hits and records.

    """

    search_results: Annotated[
        List[SemanticSearchHit], pydantic.Field(alias="searchResults")
    ]

    records: List[SemanticSearchGraphRecord]

    status: str

    status_code: int

    message: str

    applied_filters: Annotated[
        Optional[SemanticSearchAppliedFilters], pydantic.Field(alias="appliedFilters")
    ] = None
    r"""Present when KB filters were applied to the search request."""

    virtual_to_record_map: Optional[Dict[str, SemanticSearchGraphRecord]] = None
    r"""Maps virtual record id (object property name) to the accessible graph record document for that id."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["appliedFilters", "virtual_to_record_map"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SemanticSearchAiResponse.model_rebuild()
except NameError:
    pass
