"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentcapabilities import AgentCapabilities, AgentCapabilitiesTypedDict
from .filters import Filters, FiltersTypedDict
from datetime import datetime
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


RegenerateRequestProtocol = Literal["agui",]
r"""AG-UI is the only supported wire protocol. When present must be
`\"agui\"`. Omitting the field is equivalent — the server always
uses the AG-UI vocabulary. Kept in the schema for backward
compatibility with callers that already send it.

"""


class RegenerateRequestTypedDict(TypedDict):
    r"""Request body for regenerating an AI response. All fields are optional;
    when omitted the model selection and execution context from the
    original message are reused.

    Supported fields:
    - `filters` — optional `{ apps?, kb? }` filter object
    - `chatMode` — optional non-empty chat mode string
    - `modelKey`, `modelName`, `modelFriendlyName` —
    optional non-empty model override fields
    - `timezone` — optional non-empty client timezone string
    - `currentTime` — optional ISO 8601 / RFC 3339 datetime string with
    UTC `Z` or a numeric offset
    - `tools` — optional array of non-empty tool identifiers
    - `protocol` — optional inert compatibility field; when present it must
    be `agui`, which is also the protocol used when the field is omitted
    - `agentCapabilities` — optional per-request agent capability toggles

    """

    filters: NotRequired[FiltersTypedDict]
    r"""App connector instance ids and knowledge-base / record-group ids that narrow retrieval
    for a turn. For **org assistant** chat streams, send explicit `apps` / `kb` lists.
    For **agent** chat streams, send explicit id lists, or **omit** `filters` (and `tools`)
    to let the service use the agent’s stored knowledge and tool configuration. Sending
    `{ \"apps\": [], \"kb\": [] }` on an agent stream means **no** knowledge sources for that
    turn (it is not “full org default”).

    """
    model_key: NotRequired[str]
    r"""Identifier of the AI model configuration to use for regeneration.
    Typically a UUID returned by the model-management endpoints. When
    omitted, the model used for the original message is reused.

    """
    model_name: NotRequired[str]
    r"""Provider model name (e.g. the underlying LLM identifier)."""
    model_friendly_name: NotRequired[str]
    r"""Friendly display name of the selected model."""
    chat_mode: NotRequired[str]
    r"""Chat mode used for regeneration (for example `internal_search`,
    `web_search`, or the universal `agent` mode).

    """
    timezone: NotRequired[str]
    r"""IANA timezone identifier from the client. Used to provide
    time-aware context to the AI during regeneration.

    """
    current_time: NotRequired[datetime]
    r"""ISO 8601 / RFC 3339 datetime from the client (UTC `Z` or numeric
    offset). Used to anchor any relative time references in the query.

    """
    tools: NotRequired[List[str]]
    r"""Optional list of tool identifiers (fully-qualified action names
    such as `jira.create_issue`) the agent may invoke when
    regenerating. Applicable only in agent chat modes.

    """
    protocol: NotRequired[RegenerateRequestProtocol]
    r"""AG-UI is the only supported wire protocol. When present must be
    `\"agui\"`. Omitting the field is equivalent — the server always
    uses the AG-UI vocabulary. Kept in the schema for backward
    compatibility with callers that already send it.

    """
    agent_capabilities: NotRequired[AgentCapabilitiesTypedDict]
    r"""Per-request agent capability toggles. Only meaningful when `chatMode`
    selects an agent mode; ignored otherwise. Each field falls back to its
    own `default` below when omitted — a missing flag is not uniformly
    `true`. Omitting the whole object applies every default.

    """


class RegenerateRequest(BaseModel):
    r"""Request body for regenerating an AI response. All fields are optional;
    when omitted the model selection and execution context from the
    original message are reused.

    Supported fields:
    - `filters` — optional `{ apps?, kb? }` filter object
    - `chatMode` — optional non-empty chat mode string
    - `modelKey`, `modelName`, `modelFriendlyName` —
    optional non-empty model override fields
    - `timezone` — optional non-empty client timezone string
    - `currentTime` — optional ISO 8601 / RFC 3339 datetime string with
    UTC `Z` or a numeric offset
    - `tools` — optional array of non-empty tool identifiers
    - `protocol` — optional inert compatibility field; when present it must
    be `agui`, which is also the protocol used when the field is omitted
    - `agentCapabilities` — optional per-request agent capability toggles

    """

    filters: Optional[Filters] = None
    r"""App connector instance ids and knowledge-base / record-group ids that narrow retrieval
    for a turn. For **org assistant** chat streams, send explicit `apps` / `kb` lists.
    For **agent** chat streams, send explicit id lists, or **omit** `filters` (and `tools`)
    to let the service use the agent’s stored knowledge and tool configuration. Sending
    `{ \"apps\": [], \"kb\": [] }` on an agent stream means **no** knowledge sources for that
    turn (it is not “full org default”).

    """

    model_key: Annotated[Optional[str], pydantic.Field(alias="modelKey")] = None
    r"""Identifier of the AI model configuration to use for regeneration.
    Typically a UUID returned by the model-management endpoints. When
    omitted, the model used for the original message is reused.

    """

    model_name: Annotated[Optional[str], pydantic.Field(alias="modelName")] = None
    r"""Provider model name (e.g. the underlying LLM identifier)."""

    model_friendly_name: Annotated[
        Optional[str], pydantic.Field(alias="modelFriendlyName")
    ] = None
    r"""Friendly display name of the selected model."""

    chat_mode: Annotated[Optional[str], pydantic.Field(alias="chatMode")] = None
    r"""Chat mode used for regeneration (for example `internal_search`,
    `web_search`, or the universal `agent` mode).

    """

    timezone: Optional[str] = None
    r"""IANA timezone identifier from the client. Used to provide
    time-aware context to the AI during regeneration.

    """

    current_time: Annotated[Optional[datetime], pydantic.Field(alias="currentTime")] = (
        None
    )
    r"""ISO 8601 / RFC 3339 datetime from the client (UTC `Z` or numeric
    offset). Used to anchor any relative time references in the query.

    """

    tools: Optional[List[str]] = None
    r"""Optional list of tool identifiers (fully-qualified action names
    such as `jira.create_issue`) the agent may invoke when
    regenerating. Applicable only in agent chat modes.

    """

    protocol: Optional[RegenerateRequestProtocol] = None
    r"""AG-UI is the only supported wire protocol. When present must be
    `\"agui\"`. Omitting the field is equivalent — the server always
    uses the AG-UI vocabulary. Kept in the schema for backward
    compatibility with callers that already send it.

    """

    agent_capabilities: Annotated[
        Optional[AgentCapabilities], pydantic.Field(alias="agentCapabilities")
    ] = None
    r"""Per-request agent capability toggles. Only meaningful when `chatMode`
    selects an agent mode; ignored otherwise. Each field falls back to its
    own `default` below when omitted — a missing flag is not uniformly
    `true`. Omitting the whole object applies every default.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "filters",
                "modelKey",
                "modelName",
                "modelFriendlyName",
                "chatMode",
                "timezone",
                "currentTime",
                "tools",
                "protocol",
                "agentCapabilities",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    RegenerateRequest.model_rebuild()
except NameError:
    pass
