"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentlistitem import AgentListItem, AgentListItemTypedDict
from .agentlistpagination import AgentListPagination, AgentListPaginationTypedDict
from pipeshub_sdk.types import BaseModel
from typing import List
from typing_extensions import TypedDict


class AgentListResponseTypedDict(TypedDict):
    r"""Paginated response returned by `GET /agents`.

    The Node gateway forwards the Python backend response on success. If
    the backend returns a non-200 response, the gateway still returns HTTP
    200 with `success: true`, an empty `agents` array, and a zeroed
    pagination block derived from the requested `page` / `limit`.

    """

    success: bool
    agents: List[AgentListItemTypedDict]
    pagination: AgentListPaginationTypedDict
    r"""Pagination block returned by `GET /agents`."""


class AgentListResponse(BaseModel):
    r"""Paginated response returned by `GET /agents`.

    The Node gateway forwards the Python backend response on success. If
    the backend returns a non-200 response, the gateway still returns HTTP
    200 with `success: true`, an empty `agents` array, and a zeroed
    pagination block derived from the requested `page` / `limit`.

    """

    success: bool

    agents: List[AgentListItem]

    pagination: AgentListPagination
    r"""Pagination block returned by `GET /agents`."""
