"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .agentfilters import AgentFilters, AgentFiltersTypedDict
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class FiltersParsedTypedDict(TypedDict):
    r"""Server-derived read-only object parsed from the stored
    `filters` JSON by the graph provider on GET (Neo4j / Arango).
    Empty object when `filters` is missing or invalid JSON.

    """

    record_groups: NotRequired[List[str]]
    r"""Deprecated/legacy: record-group ids for connector record-group scoping (e.g. Confluence spaces, Jira projects). No longer set for KB (Collection) entries — a KB is identified by its own `connectorId`, not by an id in this list."""
    records: NotRequired[List[str]]
    r"""Individual record ids in scope."""


class FiltersParsed(BaseModel):
    r"""Server-derived read-only object parsed from the stored
    `filters` JSON by the graph provider on GET (Neo4j / Arango).
    Empty object when `filters` is missing or invalid JSON.

    """

    record_groups: Annotated[
        Optional[List[str]], pydantic.Field(alias="recordGroups")
    ] = None
    r"""Deprecated/legacy: record-group ids for connector record-group scoping (e.g. Confluence spaces, Jira projects). No longer set for KB (Collection) entries — a KB is identified by its own `connectorId`, not by an id in this list."""

    records: Optional[List[str]] = None
    r"""Individual record ids in scope."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["recordGroups", "records"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class KnowledgeTypedDict(TypedDict):
    r"""Knowledge connector / indexed scope linked to an agent, as projected by
    the graph store on `GET /agents/{agentKey}` and `GET /agents`.

    """

    key: NotRequired[str]
    connector_id: NotRequired[str]
    name: NotRequired[str]
    type: NotRequired[str]
    display_name: NotRequired[str]
    filters: NotRequired[AgentFiltersTypedDict]
    r"""Knowledge scope filter as stored on the graph edge. The Node `getAgent`
    handler proxies this field unchanged from the AI service (only `agent.id`
    is stripped). May be a JSON string (typical graph storage) or an object.
    Prefer `filtersParsed` on GET for a guaranteed parsed object with the
    same keys as the object branch below.

    """
    filters_parsed: NotRequired[FiltersParsedTypedDict]
    r"""Server-derived read-only object parsed from the stored
    `filters` JSON by the graph provider on GET (Neo4j / Arango).
    Empty object when `filters` is missing or invalid JSON.

    """


class Knowledge(BaseModel):
    r"""Knowledge connector / indexed scope linked to an agent, as projected by
    the graph store on `GET /agents/{agentKey}` and `GET /agents`.

    """

    key: Annotated[Optional[str], pydantic.Field(alias="_key")] = None

    connector_id: Annotated[Optional[str], pydantic.Field(alias="connectorId")] = None

    name: Optional[str] = None

    type: Optional[str] = None

    display_name: Annotated[Optional[str], pydantic.Field(alias="displayName")] = None

    filters: Optional[AgentFilters] = None
    r"""Knowledge scope filter as stored on the graph edge. The Node `getAgent`
    handler proxies this field unchanged from the AI service (only `agent.id`
    is stripped). May be a JSON string (typical graph storage) or an object.
    Prefer `filtersParsed` on GET for a guaranteed parsed object with the
    same keys as the object branch below.

    """

    filters_parsed: Annotated[
        Optional[FiltersParsed], pydantic.Field(alias="filtersParsed")
    ] = None
    r"""Server-derived read-only object parsed from the stored
    `filters` JSON by the graph provider on GET (Neo4j / Arango).
    Empty object when `filters` is missing or invalid JSON.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "_key",
                "connectorId",
                "name",
                "type",
                "displayName",
                "filters",
                "filtersParsed",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    FiltersParsed.model_rebuild()
except NameError:
    pass
try:
    Knowledge.model_rebuild()
except NameError:
    pass
