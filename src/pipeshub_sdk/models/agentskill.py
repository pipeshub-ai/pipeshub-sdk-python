"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pipeshub_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class AgentSkillTypedDict(TypedDict):
    r"""A skill linked to the agent, as returned by the agent detail graph
    projection. Flat by design — a skill carries no sub-entities analogous
    to a toolset's tools. Fields other than `name` are read straight off
    the skill document and are null when unset.

    """

    name: str
    r"""Unique skill name, used to reference the skill on agent create/update."""
    description: NotRequired[Nullable[str]]
    category: NotRequired[Nullable[str]]
    subcategory: NotRequired[Nullable[str]]
    version: NotRequired[Nullable[str]]
    status: NotRequired[Nullable[str]]
    r"""Lifecycle state of the skill — `active` or `deprecated`."""


class AgentSkill(BaseModel):
    r"""A skill linked to the agent, as returned by the agent detail graph
    projection. Flat by design — a skill carries no sub-entities analogous
    to a toolset's tools. Fields other than `name` are read straight off
    the skill document and are null when unset.

    """

    name: str
    r"""Unique skill name, used to reference the skill on agent create/update."""

    description: OptionalNullable[str] = UNSET

    category: OptionalNullable[str] = UNSET

    subcategory: OptionalNullable[str] = UNSET

    version: OptionalNullable[str] = UNSET

    status: OptionalNullable[str] = UNSET
    r"""Lifecycle state of the skill — `active` or `deprecated`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["description", "category", "subcategory", "version", "status"]
        )
        nullable_fields = set(
            ["description", "category", "subcategory", "version", "status"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
