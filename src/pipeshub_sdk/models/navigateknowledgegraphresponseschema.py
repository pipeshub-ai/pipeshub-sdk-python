"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .knowledgegraphnoderefschema import (
    KnowledgeGraphNodeRefSchema,
    KnowledgeGraphNodeRefSchemaTypedDict,
)
from .knowledgegraphnoderowschema import (
    KnowledgeGraphNodeRowSchema,
    KnowledgeGraphNodeRowSchemaTypedDict,
)
from pipeshub_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List
from typing_extensions import NotRequired, TypedDict


class NavigateKnowledgeGraphResponseSchemaCurrentTypedDict(TypedDict):
    r"""The node that was opened. Null at the root listing."""

    id: str
    r"""Node identifier. Pass it back as `nodeId` to open this node. When `is_record` is true it is also the Record ID accepted by GET /connectors/record/{recordId}/content."""
    name: str
    node_type: str
    r"""One of `app`, `recordGroup`, `folder`, `record`."""
    sub_type: Nullable[str]
    r"""e.g. `TICKET`, `CONFLUENCE_PAGE`, `PROJECT`, `COLLECTION`."""
    is_record: bool
    r"""True for record and folder nodes — the nodes whose content can be read via GET /connectors/record/{recordId}/content."""


class NavigateKnowledgeGraphResponseSchemaCurrent(BaseModel):
    r"""The node that was opened. Null at the root listing."""

    id: str
    r"""Node identifier. Pass it back as `nodeId` to open this node. When `is_record` is true it is also the Record ID accepted by GET /connectors/record/{recordId}/content."""

    name: str

    node_type: str
    r"""One of `app`, `recordGroup`, `folder`, `record`."""

    sub_type: Nullable[str]
    r"""e.g. `TICKET`, `CONFLUENCE_PAGE`, `PROJECT`, `COLLECTION`."""

    is_record: bool
    r"""True for record and folder nodes — the nodes whose content can be read via GET /connectors/record/{recordId}/content."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


class NavigateKnowledgeGraphResponseSchemaPaginationTypedDict(TypedDict):
    r"""Pagination envelope for a navigate listing."""

    page: int
    limit: int
    total: int
    r"""Total children of the current node, ignoring pagination."""
    has_next: bool
    has_prev: bool


class NavigateKnowledgeGraphResponseSchemaPagination(BaseModel):
    r"""Pagination envelope for a navigate listing."""

    page: int

    limit: int

    total: int
    r"""Total children of the current node, ignoring pagination."""

    has_next: bool

    has_prev: bool


class NavigateKnowledgeGraphResponseSchemaTypedDict(TypedDict):
    r"""Response returned by GET /connectors/navigate.

    Field names are snake_case, matching the knowledge graph's internal
    view models. `text` carries the same flat-text rendering PipesHub's
    own agent sees for this node; the remaining fields carry the same
    information in structured form.

    """

    current: Nullable[NavigateKnowledgeGraphResponseSchemaCurrentTypedDict]
    r"""The node that was opened. Null at the root listing."""
    breadcrumbs: List[KnowledgeGraphNodeRefSchemaTypedDict]
    r"""Root to parent, not including `current`."""
    rows: List[KnowledgeGraphNodeRowSchemaTypedDict]
    r"""The current node's children for this page. With `depth` > 1 this is a flat list of all descendants up to that depth, each row carrying its own `level`."""
    related: List[KnowledgeGraphNodeRowSchemaTypedDict]
    r"""Cross-referenced records (non-containment edges), e.g. the Confluence page linked from a Jira ticket. Page 1 only."""
    pagination: Nullable[NavigateKnowledgeGraphResponseSchemaPaginationTypedDict]
    web_url: Nullable[str]
    indexing_status: Nullable[str]
    connector: Nullable[str]
    text: str
    r"""Flat-text rendering of this view: breadcrumbs, the current node's
    metadata, the children listing with an id per row, `Related:`, and
    a closing `Next:` line naming a follow-up call.

    Shape depends on the page: pages after the first omit the header,
    breadcrumbs and related rows to stay compact, while the structured
    fields above remain complete. Capped at 25,000 bytes, with a
    truncation marker appended when exceeded.

    """
    context_block: NotRequired[Nullable[str]]
    r"""Type-specific metadata for the current node when it is a record — for a ticket, status, assignee, priority and dates."""


class NavigateKnowledgeGraphResponseSchema(BaseModel):
    r"""Response returned by GET /connectors/navigate.

    Field names are snake_case, matching the knowledge graph's internal
    view models. `text` carries the same flat-text rendering PipesHub's
    own agent sees for this node; the remaining fields carry the same
    information in structured form.

    """

    current: Nullable[NavigateKnowledgeGraphResponseSchemaCurrent]
    r"""The node that was opened. Null at the root listing."""

    breadcrumbs: List[KnowledgeGraphNodeRefSchema]
    r"""Root to parent, not including `current`."""

    rows: List[KnowledgeGraphNodeRowSchema]
    r"""The current node's children for this page. With `depth` > 1 this is a flat list of all descendants up to that depth, each row carrying its own `level`."""

    related: List[KnowledgeGraphNodeRowSchema]
    r"""Cross-referenced records (non-containment edges), e.g. the Confluence page linked from a Jira ticket. Page 1 only."""

    pagination: Nullable[NavigateKnowledgeGraphResponseSchemaPagination]

    web_url: Nullable[str]

    indexing_status: Nullable[str]

    connector: Nullable[str]

    text: str
    r"""Flat-text rendering of this view: breadcrumbs, the current node's
    metadata, the children listing with an id per row, `Related:`, and
    a closing `Next:` line naming a follow-up call.

    Shape depends on the page: pages after the first omit the header,
    breadcrumbs and related rows to stay compact, while the structured
    fields above remain complete. Capped at 25,000 bytes, with a
    truncation marker appended when exceeded.

    """

    context_block: OptionalNullable[str] = UNSET
    r"""Type-specific metadata for the current node when it is a record — for a ticket, status, assignee, priority and dates."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["context_block"])
        nullable_fields = set(
            [
                "current",
                "pagination",
                "web_url",
                "indexing_status",
                "connector",
                "context_block",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
