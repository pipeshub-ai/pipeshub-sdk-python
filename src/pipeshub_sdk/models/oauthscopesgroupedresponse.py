"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .oauthscopeinfo import OAuthScopeInfo, OAuthScopeInfoTypedDict
from pipeshub_sdk.types import BaseModel
from typing import Dict, List
from typing_extensions import TypedDict


class OAuthScopesGroupedResponseTypedDict(TypedDict):
    r"""OAuth scopes available to the signed-in user for app registration, grouped by category label.
    Category keys are UI labels (e.g. `Identity`, `Knowledge Base`); each maps to a list of scopes in that group.
    Categories defined in server config may appear with an **empty array** when every scope in that category is restricted for the caller's role (e.g. non–org-admin users never receive admin-only scopes).

    """

    scopes: Dict[str, List[OAuthScopeInfoTypedDict]]
    r"""Map of category display name to scopes in that category"""


class OAuthScopesGroupedResponse(BaseModel):
    r"""OAuth scopes available to the signed-in user for app registration, grouped by category label.
    Category keys are UI labels (e.g. `Identity`, `Knowledge Base`); each maps to a list of scopes in that group.
    Categories defined in server config may appear with an **empty array** when every scope in that category is restricted for the caller's role (e.g. non–org-admin users never receive admin-only scopes).

    """

    scopes: Dict[str, List[OAuthScopeInfo]]
    r"""Map of category display name to scopes in that category"""
