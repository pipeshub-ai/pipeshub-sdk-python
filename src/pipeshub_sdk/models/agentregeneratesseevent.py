"""Code originally generated by Speakeasy (https://speakeasy.com)."""

from __future__ import annotations
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL, UnrecognizedStr
from pydantic import model_serializer
from typing import Any, Literal, Optional, Union
from typing_extensions import NotRequired, TypedDict


AgentRegenerateSSEEventEvent = Union[
    Literal[
        "connected",
        "status",
        "tool_calls",
        "tool_call",
        "tool_success",
        "tool_error",
        "tool_result",
        "tool_execution_complete",
        "answer_chunk",
        "restreaming",
        "metadata",
        "complete",
        "error",
    ],
    UnrecognizedStr,
]


class AgentRegenerateSSEEventTypedDict(TypedDict):
    r"""SSE event envelope for `POST /agents/{agentKey}/conversations/{conversationId}/message/{messageId}/regenerate`.

    Stable events:

    - `connected` confirms the stream is open.
    - `complete` returns the updated conversation plus request metadata
    after the regenerated bot response is persisted.
    - `error` returns a failure message. Conversation lookup failures,
    unauthorized conversation access, and regenerate rule failures such
    as \"not the last message\" are reported here after the stream starts.

    Other events are forwarded from the agent backend and should be
    treated as informational updates.

    """

    event: NotRequired[AgentRegenerateSSEEventEvent]
    data: NotRequired[Any]
    r"""JSON-encoded event payload. Shape depends on `event`."""


class AgentRegenerateSSEEvent(BaseModel):
    r"""SSE event envelope for `POST /agents/{agentKey}/conversations/{conversationId}/message/{messageId}/regenerate`.

    Stable events:

    - `connected` confirms the stream is open.
    - `complete` returns the updated conversation plus request metadata
    after the regenerated bot response is persisted.
    - `error` returns a failure message. Conversation lookup failures,
    unauthorized conversation access, and regenerate rule failures such
    as \"not the last message\" are reported here after the stream starts.

    Other events are forwarded from the agent backend and should be
    treated as informational updates.

    """

    event: Optional[AgentRegenerateSSEEventEvent] = None

    data: Optional[Any] = None
    r"""JSON-encoded event payload. Shape depends on `event`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["event", "data"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
