"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pipeshub_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class KnowledgeGraphNodeRowSchemaTypedDict(TypedDict):
    r"""One row in a navigate listing. Carries the identity fields of
    KnowledgeGraphNodeRefSchema (inlined rather than composed, so this
    object can stay closed) plus display detail.

    """

    id: str
    name: str
    node_type: str
    r"""One of `recordGroup`, `folder`, `record`. Listings never contain `app` nodes."""
    sub_type: Nullable[str]
    is_record: bool
    has_children: bool
    r"""Whether this node can itself be opened with `nodeId`."""
    detail: Nullable[str]
    r"""e.g. size, indexing status, or relationship type."""
    web_url: NotRequired[Nullable[str]]
    r"""Link to the record in its source system. Records that carry a relative URL (uploaded/KB records) are resolved against the configured frontend public URL (see POST /configurationManager/frontendPublicUrl). The value is null when that host is not configured."""
    indexing_status: NotRequired[Nullable[str]]
    source_created_at: NotRequired[Nullable[int]]
    r"""Creation time in the source system, epoch milliseconds. Null when the underlying node did not carry one."""
    source_modified_at: NotRequired[Nullable[int]]
    level: NotRequired[int]
    r"""1-based nesting depth from the navigated parent. Always 1 for a `depth=1` listing; 2 or 3 for deeper rows when `depth` > 1."""
    context_summary: NotRequired[Nullable[str]]


class KnowledgeGraphNodeRowSchema(BaseModel):
    r"""One row in a navigate listing. Carries the identity fields of
    KnowledgeGraphNodeRefSchema (inlined rather than composed, so this
    object can stay closed) plus display detail.

    """

    id: str

    name: str

    node_type: str
    r"""One of `recordGroup`, `folder`, `record`. Listings never contain `app` nodes."""

    sub_type: Nullable[str]

    is_record: bool

    has_children: bool
    r"""Whether this node can itself be opened with `nodeId`."""

    detail: Nullable[str]
    r"""e.g. size, indexing status, or relationship type."""

    web_url: OptionalNullable[str] = UNSET
    r"""Link to the record in its source system. Records that carry a relative URL (uploaded/KB records) are resolved against the configured frontend public URL (see POST /configurationManager/frontendPublicUrl). The value is null when that host is not configured."""

    indexing_status: OptionalNullable[str] = UNSET

    source_created_at: OptionalNullable[int] = UNSET
    r"""Creation time in the source system, epoch milliseconds. Null when the underlying node did not carry one."""

    source_modified_at: OptionalNullable[int] = UNSET

    level: Optional[int] = None
    r"""1-based nesting depth from the navigated parent. Always 1 for a `depth=1` listing; 2 or 3 for deeper rows when `depth` > 1."""

    context_summary: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "web_url",
                "indexing_status",
                "source_created_at",
                "source_modified_at",
                "level",
                "context_summary",
            ]
        )
        nullable_fields = set(
            [
                "sub_type",
                "detail",
                "web_url",
                "indexing_status",
                "source_created_at",
                "source_modified_at",
                "context_summary",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
