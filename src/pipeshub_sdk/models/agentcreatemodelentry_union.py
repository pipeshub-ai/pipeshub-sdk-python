"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class AgentCreateModelEntryTypedDict(TypedDict):
    model_key: str
    model_name: NotRequired[str]
    provider: NotRequired[str]
    is_reasoning: NotRequired[bool]


class AgentCreateModelEntry(BaseModel):
    model_key: Annotated[str, pydantic.Field(alias="modelKey")]

    model_name: Annotated[Optional[str], pydantic.Field(alias="modelName")] = None

    provider: Optional[str] = None

    is_reasoning: Annotated[Optional[bool], pydantic.Field(alias="isReasoning")] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["modelName", "provider", "isReasoning"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


AgentCreateModelEntryUnionTypedDict = TypeAliasType(
    "AgentCreateModelEntryUnionTypedDict", Union[AgentCreateModelEntryTypedDict, str]
)
r"""Accepted model entry for `POST /agents/create`.
The gateway accepts either a non-empty string model entry or an object entry
with a required `modelKey`.

The `models` array itself is optional and may be empty (the agent then uses
the organization's default LLM). When the array is non-empty, it must include
at least one object entry with `isReasoning: true`. String-only entries are
schema-valid but, if present without any reasoning-flagged object entry, are
rejected at the gateway with HTTP 400.

"""


AgentCreateModelEntryUnion = TypeAliasType(
    "AgentCreateModelEntryUnion", Union[AgentCreateModelEntry, str]
)
r"""Accepted model entry for `POST /agents/create`.
The gateway accepts either a non-empty string model entry or an object entry
with a required `modelKey`.

The `models` array itself is optional and may be empty (the agent then uses
the organization's default LLM). When the array is non-empty, it must include
at least one object entry with `isReasoning: true`. String-only entries are
schema-valid but, if present without any reasoning-flagged object entry, are
rejected at the gateway with HTTP 400.

"""


try:
    AgentCreateModelEntry.model_rebuild()
except NameError:
    pass
