"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .indexingstatusfilter import IndexingStatusFilter
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ReindexRecordRequestBodyTypedDict(TypedDict):
    r"""Optional body for single-record reindex."""

    depth: NotRequired[int]
    r"""Child traversal depth (`0` = record only; higher values include
    descendants; `100` is used by clients for folder-like reindex).

    """
    force: NotRequired[bool]
    r"""Force reindex even when the connector considers the record unchanged."""
    status_filters: NotRequired[List[IndexingStatusFilter]]
    r"""When set, only records whose indexing status matches one of these
    values are reindexed (applies to the record and its descendants per `depth`).

    """


class ReindexRecordRequestBody(BaseModel):
    r"""Optional body for single-record reindex."""

    depth: Optional[int] = 0
    r"""Child traversal depth (`0` = record only; higher values include
    descendants; `100` is used by clients for folder-like reindex).

    """

    force: Optional[bool] = False
    r"""Force reindex even when the connector considers the record unchanged."""

    status_filters: Annotated[
        Optional[List[IndexingStatusFilter]], pydantic.Field(alias="statusFilters")
    ] = None
    r"""When set, only records whose indexing status matches one of these
    values are reindexed (applies to the record and its descendants per `depth`).

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["depth", "force", "statusFilters"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ReindexRecordRequestBody.model_rebuild()
except NameError:
    pass
