"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class AdminPatListItemTypedDict(TypedDict):
    r"""A personal access token as seen by an org admin — extends
    `PatListItem` with who it belongs to, since an admin is looking
    across every member's tokens rather than just their own.

    """

    id: str
    r"""Token ID"""
    name: str
    r"""Token name"""
    scopes: List[str]
    r"""Granted scopes"""
    created_at: datetime
    expires_at: datetime
    r"""Expiry timestamp. A `\"never\"`-expiry token is stored as a
    ~100-year-out date, not a literal null — treat anything decades
    out as \"never\" rather than a real deadline.

    """
    user_id: str
    r"""ID of the user who created this token"""
    owner_deleted: bool
    r"""True if the owning user has been removed from the org (or no
    longer resolves at all). The token still appears — a deleted
    user's tokens stop authenticating automatically, but stay
    visible here so an admin can audit/clean them up.

    """
    last_used_at: NotRequired[datetime]
    r"""Last time this token successfully authenticated a request.
    Throttled server-side to update at most once per 5 minutes per
    token; absent if the token has never been used.

    """
    owner_email: NotRequired[str]
    r"""Owner's email. Populated even when `ownerDeleted` is true
    (last-known value), for auditing.

    """
    owner_full_name: NotRequired[str]
    r"""Owner's full name (same last-known-value behavior as `ownerEmail`)"""


class AdminPatListItem(BaseModel):
    r"""A personal access token as seen by an org admin — extends
    `PatListItem` with who it belongs to, since an admin is looking
    across every member's tokens rather than just their own.

    """

    id: str
    r"""Token ID"""

    name: str
    r"""Token name"""

    scopes: List[str]
    r"""Granted scopes"""

    created_at: Annotated[datetime, pydantic.Field(alias="createdAt")]

    expires_at: Annotated[datetime, pydantic.Field(alias="expiresAt")]
    r"""Expiry timestamp. A `\"never\"`-expiry token is stored as a
    ~100-year-out date, not a literal null — treat anything decades
    out as \"never\" rather than a real deadline.

    """

    user_id: Annotated[str, pydantic.Field(alias="userId")]
    r"""ID of the user who created this token"""

    owner_deleted: Annotated[bool, pydantic.Field(alias="ownerDeleted")]
    r"""True if the owning user has been removed from the org (or no
    longer resolves at all). The token still appears — a deleted
    user's tokens stop authenticating automatically, but stay
    visible here so an admin can audit/clean them up.

    """

    last_used_at: Annotated[Optional[datetime], pydantic.Field(alias="lastUsedAt")] = (
        None
    )
    r"""Last time this token successfully authenticated a request.
    Throttled server-side to update at most once per 5 minutes per
    token; absent if the token has never been used.

    """

    owner_email: Annotated[Optional[str], pydantic.Field(alias="ownerEmail")] = None
    r"""Owner's email. Populated even when `ownerDeleted` is true
    (last-known value), for auditing.

    """

    owner_full_name: Annotated[Optional[str], pydantic.Field(alias="ownerFullName")] = (
        None
    )
    r"""Owner's full name (same last-known-value behavior as `ownerEmail`)"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["lastUsedAt", "ownerEmail", "ownerFullName"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AdminPatListItem.model_rebuild()
except NameError:
    pass
