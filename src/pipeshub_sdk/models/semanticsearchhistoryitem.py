"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .persistedsemanticsearchsharedwithentry import (
    PersistedSemanticSearchSharedWithEntry,
    PersistedSemanticSearchSharedWithEntryTypedDict,
)
from datetime import datetime
from pipeshub_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class SemanticSearchHistoryItemTypedDict(TypedDict):
    r"""One persisted search row as returned by `GET /search`. Mirrors the
    persisted search document except `citationIds` is an array of
    ObjectId strings (citations are not populated on the list endpoint,
    unlike `GET /search/{searchId}`).

    `shareLink` is absent from the JSON when the search has not been
    shared. `archivedBy` is `null` on rows that were previously archived
    and then unarchived, and absent on rows that have never been
    archived. The list endpoint already filters out currently-archived
    rows, so a string user-id value never appears here in practice.

    """

    id: str
    v: int
    query: str
    limit: int
    org_id: str
    user_id: str
    citation_ids: List[str]
    records: Dict[str, str]
    r"""Map of source-record id (or `_key`) to `JSON.stringify(<record>)`.
    Clients must `JSON.parse` each value to recover the underlying
    record object. Populated at write-time in the POST `/search` handler.

    """
    is_shared: bool
    shared_with: List[PersistedSemanticSearchSharedWithEntryTypedDict]
    is_archived: bool
    created_at: datetime
    updated_at: datetime
    share_link: NotRequired[str]
    r"""Set once the search has been shared via `/search/{searchId}/share`."""
    archived_by: NotRequired[Nullable[str]]
    r"""User ID of the last user who archived this row, or `null` after
    an unarchive cleared the archive state. Absent on rows that have
    never been archived.

    """


class SemanticSearchHistoryItem(BaseModel):
    r"""One persisted search row as returned by `GET /search`. Mirrors the
    persisted search document except `citationIds` is an array of
    ObjectId strings (citations are not populated on the list endpoint,
    unlike `GET /search/{searchId}`).

    `shareLink` is absent from the JSON when the search has not been
    shared. `archivedBy` is `null` on rows that were previously archived
    and then unarchived, and absent on rows that have never been
    archived. The list endpoint already filters out currently-archived
    rows, so a string user-id value never appears here in practice.

    """

    id: Annotated[str, pydantic.Field(alias="_id")]

    v: Annotated[int, pydantic.Field(alias="__v")]

    query: str

    limit: int

    org_id: Annotated[str, pydantic.Field(alias="orgId")]

    user_id: Annotated[str, pydantic.Field(alias="userId")]

    citation_ids: Annotated[List[str], pydantic.Field(alias="citationIds")]

    records: Dict[str, str]
    r"""Map of source-record id (or `_key`) to `JSON.stringify(<record>)`.
    Clients must `JSON.parse` each value to recover the underlying
    record object. Populated at write-time in the POST `/search` handler.

    """

    is_shared: Annotated[bool, pydantic.Field(alias="isShared")]

    shared_with: Annotated[
        List[PersistedSemanticSearchSharedWithEntry], pydantic.Field(alias="sharedWith")
    ]

    is_archived: Annotated[bool, pydantic.Field(alias="isArchived")]

    created_at: Annotated[datetime, pydantic.Field(alias="createdAt")]

    updated_at: Annotated[datetime, pydantic.Field(alias="updatedAt")]

    share_link: Annotated[Optional[str], pydantic.Field(alias="shareLink")] = None
    r"""Set once the search has been shared via `/search/{searchId}/share`."""

    archived_by: Annotated[
        OptionalNullable[str], pydantic.Field(alias="archivedBy")
    ] = UNSET
    r"""User ID of the last user who archived this row, or `null` after
    an unarchive cleared the archive state. Absent on rows that have
    never been archived.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["shareLink", "archivedBy"])
        nullable_fields = set(["archivedBy"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    SemanticSearchHistoryItem.model_rebuild()
except NameError:
    pass
