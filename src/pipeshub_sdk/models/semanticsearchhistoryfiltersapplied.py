"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .semanticsearchhistoryapplieddaterange import (
    SemanticSearchHistoryAppliedDateRange,
    SemanticSearchHistoryAppliedDateRangeTypedDict,
)
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL, UnrecognizedStr
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypedDict


Filter = Union[
    Literal[
        "page",
        "limit",
        "search",
        "shared",
        "tags",
        "minMessages",
        "sortBy",
        "sortOrder",
        "startDate",
        "endDate",
        "messageType",
        "dateRange",
    ],
    UnrecognizedStr,
]


class ValuesTypedDict(TypedDict):
    page: NotRequired[int]
    limit: NotRequired[int]
    search: NotRequired[str]
    shared: NotRequired[str]
    tags: NotRequired[str]
    min_messages: NotRequired[str]
    sort_by: NotRequired[str]
    sort_order: NotRequired[str]
    start_date: NotRequired[str]
    end_date: NotRequired[str]
    message_type: NotRequired[str]
    date_range: NotRequired[SemanticSearchHistoryAppliedDateRangeTypedDict]
    r"""Echoed back only when the caller passed `startDate` and/or `endDate`.
    Each bound is an ISO 8601 string when set; the field is absent when
    the corresponding query param was omitted (utils.ts:480-486 reads
    `appliedFilters.createdAt.$gte?.toISOString()` directly, so missing
    bounds become `undefined` and drop out of the JSON).

    """


class Values(BaseModel):
    page: Optional[int] = None

    limit: Optional[int] = None

    search: Optional[str] = None

    shared: Optional[str] = None

    tags: Optional[str] = None

    min_messages: Annotated[Optional[str], pydantic.Field(alias="minMessages")] = None

    sort_by: Annotated[Optional[str], pydantic.Field(alias="sortBy")] = None

    sort_order: Annotated[Optional[str], pydantic.Field(alias="sortOrder")] = None

    start_date: Annotated[Optional[str], pydantic.Field(alias="startDate")] = None

    end_date: Annotated[Optional[str], pydantic.Field(alias="endDate")] = None

    message_type: Annotated[Optional[str], pydantic.Field(alias="messageType")] = None

    date_range: Annotated[
        Optional[SemanticSearchHistoryAppliedDateRange],
        pydantic.Field(alias="dateRange"),
    ] = None
    r"""Echoed back only when the caller passed `startDate` and/or `endDate`.
    Each bound is an ISO 8601 string when set; the field is absent when
    the corresponding query param was omitted (utils.ts:480-486 reads
    `appliedFilters.createdAt.$gte?.toISOString()` directly, so missing
    bounds become `undefined` and drop out of the JSON).

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "page",
                "limit",
                "search",
                "shared",
                "tags",
                "minMessages",
                "sortBy",
                "sortOrder",
                "startDate",
                "endDate",
                "messageType",
                "dateRange",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SemanticSearchHistoryFiltersAppliedTypedDict(TypedDict):
    r"""Echo of which filters the caller actually supplied, built by
    `buildFiltersMetadata` (utils.ts:430-486). `page` and `limit` always
    appear because they are normalised to defaults before being recorded,
    so `filters` is never empty and `values` always contains at least
    `{ page, limit }`. Other keys appear only when the matching query
    param was non-empty (or, for `dateRange`, when `createdAt` was set
    on the Mongo filter).

    `values` keys are scalar strings rather than typed primitives
    (`'true'`/`'false'`, `'5'`, etc.) because they are passed through
    from `req.query` as Express parsed them — only `page` and `limit`
    are coerced to integers via `safeParsePagination`.

    """

    filters: List[Filter]
    values: ValuesTypedDict


class SemanticSearchHistoryFiltersApplied(BaseModel):
    r"""Echo of which filters the caller actually supplied, built by
    `buildFiltersMetadata` (utils.ts:430-486). `page` and `limit` always
    appear because they are normalised to defaults before being recorded,
    so `filters` is never empty and `values` always contains at least
    `{ page, limit }`. Other keys appear only when the matching query
    param was non-empty (or, for `dateRange`, when `createdAt` was set
    on the Mongo filter).

    `values` keys are scalar strings rather than typed primitives
    (`'true'`/`'false'`, `'5'`, etc.) because they are passed through
    from `req.query` as Express parsed them — only `page` and `limit`
    are coerced to integers via `safeParsePagination`.

    """

    filters: List[Filter]

    values: Values


try:
    Values.model_rebuild()
except NameError:
    pass
