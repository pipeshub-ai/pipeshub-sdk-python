"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pipeshub_sdk.types import BaseModel, UNSET_SENTINEL
from pipeshub_sdk.utils import FieldMetadata, QueryParamMetadata
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


SearchHistorySortBy = Literal[
    "createdAt",
    "lastActivityAt",
    "title",
]
r"""Field used to sort results. Any value other than `createdAt`,
`lastActivityAt`, or `title` is treated as `lastActivityAt`.

"""


SearchHistorySortOrder = Literal[
    "asc",
    "desc",
]
r"""Sort direction applied to `sortBy`."""


SearchHistoryShared = Literal[
    "true",
    "false",
    "1",
    "0",
]
r"""Filter results by their shared status. Accepted values are
`'true'` / `'1'` (return only shared searches) and
`'false'` / `'0'` (exclude shared searches). Matching is
case-insensitive and surrounding whitespace is trimmed.

"""


class SearchHistoryRequestTypedDict(TypedDict):
    page: NotRequired[int]
    r"""Page number to return. Must be within `[1, 1000]`."""
    limit: NotRequired[int]
    r"""Number of items per page. Values are clamped to the range `[1, 100]`."""
    sort_by: NotRequired[SearchHistorySortBy]
    r"""Field used to sort results. Any value other than `createdAt`,
    `lastActivityAt`, or `title` is treated as `lastActivityAt`.

    """
    sort_order: NotRequired[SearchHistorySortOrder]
    r"""Sort direction applied to `sortBy`."""
    search: NotRequired[str]
    r"""Case-insensitive substring to match against a search's title and
    message content. Regex metacharacters are escaped automatically.
    Values longer than 1000 characters are rejected with `400`.

    """
    shared: NotRequired[SearchHistoryShared]
    r"""Filter results by their shared status. Accepted values are
    `'true'` / `'1'` (return only shared searches) and
    `'false'` / `'0'` (exclude shared searches). Matching is
    case-insensitive and surrounding whitespace is trimmed.

    """
    start_date: NotRequired[datetime]
    r"""ISO 8601 timestamp used as the lower bound for a search's creation date."""
    end_date: NotRequired[datetime]
    r"""ISO 8601 timestamp used as the upper bound for a search's creation date."""


class SearchHistoryRequest(BaseModel):
    page: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = 1
    r"""Page number to return. Must be within `[1, 1000]`."""

    limit: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = 20
    r"""Number of items per page. Values are clamped to the range `[1, 100]`."""

    sort_by: Annotated[
        Optional[SearchHistorySortBy],
        pydantic.Field(alias="sortBy"),
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = "lastActivityAt"
    r"""Field used to sort results. Any value other than `createdAt`,
    `lastActivityAt`, or `title` is treated as `lastActivityAt`.

    """

    sort_order: Annotated[
        Optional[SearchHistorySortOrder],
        pydantic.Field(alias="sortOrder"),
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = "desc"
    r"""Sort direction applied to `sortBy`."""

    search: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Case-insensitive substring to match against a search's title and
    message content. Regex metacharacters are escaped automatically.
    Values longer than 1000 characters are rejected with `400`.

    """

    shared: Annotated[
        Optional[SearchHistoryShared],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Filter results by their shared status. Accepted values are
    `'true'` / `'1'` (return only shared searches) and
    `'false'` / `'0'` (exclude shared searches). Matching is
    case-insensitive and surrounding whitespace is trimmed.

    """

    start_date: Annotated[
        Optional[datetime],
        pydantic.Field(alias="startDate"),
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""ISO 8601 timestamp used as the lower bound for a search's creation date."""

    end_date: Annotated[
        Optional[datetime],
        pydantic.Field(alias="endDate"),
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""ISO 8601 timestamp used as the upper bound for a search's creation date."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "page",
                "limit",
                "sortBy",
                "sortOrder",
                "search",
                "shared",
                "startDate",
                "endDate",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SearchHistoryInternalServerErrorErrorTypedDict(TypedDict):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_INTERNAL_SERVER_ERROR` for an
    explicit server-side failure, or `INTERNAL_ERROR`
    for an unhandled exception coerced by the global
    error middleware.

    """
    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryInternalServerErrorError(BaseModel):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_INTERNAL_SERVER_ERROR` for an
    explicit server-side failure, or `INTERNAL_ERROR`
    for an unhandled exception coerced by the global
    error middleware.

    """

    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryForbiddenErrorTypedDict(TypedDict):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_FORBIDDEN` (the token is valid but
    does not carry the `semantic:read` scope).

    """
    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryForbiddenError(BaseModel):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_FORBIDDEN` (the token is valid but
    does not carry the `semantic:read` scope).

    """

    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryUnauthorizedErrorTypedDict(TypedDict):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_UNAUTHORIZED` (missing, invalid, or
    expired bearer token, user no longer exists, or the
    session has been invalidated).

    """
    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryUnauthorizedError(BaseModel):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is `HTTP_UNAUTHORIZED` (missing, invalid, or
    expired bearer token, user no longer exists, or the
    session has been invalidated).

    """

    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryBadRequestErrorTypedDict(TypedDict):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is either `VALIDATION_ERROR` (request failed
    schema validation) or `HTTP_BAD_REQUEST` (semantic
    validation failed — malformed date, value over the
    allowed length, or XSS-guard trip).

    """
    message: str
    r"""Human-readable description of the failure."""


class SearchHistoryBadRequestError(BaseModel):
    r"""Error payload."""

    code: str
    r"""Machine-readable error code. For this status the
    value is either `VALIDATION_ERROR` (request failed
    schema validation) or `HTTP_BAD_REQUEST` (semantic
    validation failed — malformed date, value over the
    allowed length, or XSS-guard trip).

    """

    message: str
    r"""Human-readable description of the failure."""
