"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""
# @generated-id: f5a60115ca85

from .basesdk import BaseSDK
from pipeshub_sdk import errors, models, utils
from pipeshub_sdk._hooks import HookContext
from pipeshub_sdk.types import OptionalNullable, UNSET
from pipeshub_sdk.utils import get_security_from_env
from pipeshub_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Mapping, Optional


class KnowledgeHub(BaseSDK):
    r"""Unified browse API for root and child nodes (apps, record groups, folders, records) with filtering and search"""

    def get_knowledge_hub_root_nodes(
        self,
        *,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[str] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubRootNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        flattened: Optional[bool] = False,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub root nodes

        Retrieve root-level nodes (Apps) or search across all nodes for unified knowledge hub browsing.<br><br>
        <b>Overview:</b><br>
        Provides a unified view across all knowledge sources - Collections, connectors, and apps. Use for building file browser UIs.<br><br>
        <b>Node Types:</b><br>
        <ul>
        <li><b>Collection:</b> Local collections (formerly Knowledge Bases)</li>
        <li><b>Connector:</b> External connector instances</li>
        <li><b>App:</b> Connected applications</li>
        </ul>


        :param only_containers: Only return nodes with children (for sidebar)
        :param page: Page number (1-indexed)
        :param limit: Items per page
        :param sort_by: Sort field (name, createdAt, updatedAt, size, type)
        :param sort_order: Sort order (asc or desc)
        :param q: Full-text search query
        :param node_types: Filter by node types (comma-separated)
        :param record_types: Comma-separated record types
        :param origins: Comma-separated origins (COLLECTION, CONNECTOR)
        :param connector_ids: Comma-separated connector instance IDs
        :param indexing_status: Comma-separated indexing statuses
        :param created_at: Created date range (gte:timestamp,lte:timestamp)
        :param updated_at: Updated date range (gte:timestamp,lte:timestamp)
        :param size: Size range (gte:bytes,lte:bytes)
        :param flattened: Return flattened view with all nested children
        :param include: Comma-separated includes (breadcrumbs, counts, availableFilters, permissions)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubRootNodesRequest(
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            flattened=flattened,
            include=include,
        )

        req = self._build_request(
            method="GET",
            path="/api/v1/knowledgeBase/knowledge-hub/nodes",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubRootNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_knowledge_hub_root_nodes_async(
        self,
        *,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[str] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubRootNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        flattened: Optional[bool] = False,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub root nodes

        Retrieve root-level nodes (Apps) or search across all nodes for unified knowledge hub browsing.<br><br>
        <b>Overview:</b><br>
        Provides a unified view across all knowledge sources - Collections, connectors, and apps. Use for building file browser UIs.<br><br>
        <b>Node Types:</b><br>
        <ul>
        <li><b>Collection:</b> Local collections (formerly Knowledge Bases)</li>
        <li><b>Connector:</b> External connector instances</li>
        <li><b>App:</b> Connected applications</li>
        </ul>


        :param only_containers: Only return nodes with children (for sidebar)
        :param page: Page number (1-indexed)
        :param limit: Items per page
        :param sort_by: Sort field (name, createdAt, updatedAt, size, type)
        :param sort_order: Sort order (asc or desc)
        :param q: Full-text search query
        :param node_types: Filter by node types (comma-separated)
        :param record_types: Comma-separated record types
        :param origins: Comma-separated origins (COLLECTION, CONNECTOR)
        :param connector_ids: Comma-separated connector instance IDs
        :param indexing_status: Comma-separated indexing statuses
        :param created_at: Created date range (gte:timestamp,lte:timestamp)
        :param updated_at: Updated date range (gte:timestamp,lte:timestamp)
        :param size: Size range (gte:bytes,lte:bytes)
        :param flattened: Return flattened view with all nested children
        :param include: Comma-separated includes (breadcrumbs, counts, availableFilters, permissions)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubRootNodesRequest(
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            flattened=flattened,
            include=include,
        )

        req = self._build_request_async(
            method="GET",
            path="/api/v1/knowledgeBase/knowledge-hub/nodes",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubRootNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_knowledge_hub_child_nodes(
        self,
        *,
        parent_type: models.ParentType,
        parent_id: str,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[str] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubChildNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        flattened: Optional[bool] = False,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub child nodes

        Get children of a specific node. Retrieve child nodes under a specific parent in the knowledge hub tree.<br><br>
        <b>Navigation:</b><br>
        Use this to drill down into collections, folders, and connector hierarchies.<br><br>
        parent_type must be one of: app, recordGroup, folder, record.


        :param parent_type: Type of parent node (app, recordGroup, folder, record)
        :param parent_id: ID of parent node (UUID or knowledgeBase_<orgId> for Collection app)
        :param only_containers: Only return nodes with children (for sidebar)
        :param page: Page number (1-indexed)
        :param limit: Items per page
        :param sort_by: Sort field (name, createdAt, updatedAt, size, type)
        :param sort_order: Sort order (asc or desc)
        :param q: Full-text search query
        :param node_types: Filter by node types (comma-separated)
        :param record_types: Comma-separated record types
        :param origins: Comma-separated origins (COLLECTION, CONNECTOR)
        :param connector_ids: Comma-separated connector instance IDs
        :param indexing_status: Comma-separated indexing statuses
        :param created_at: Created date range (gte:timestamp,lte:timestamp)
        :param updated_at: Updated date range (gte:timestamp,lte:timestamp)
        :param size: Size range (gte:bytes,lte:bytes)
        :param flattened: Return flattened view with all nested children
        :param include: Comma-separated includes (breadcrumbs, counts, availableFilters, permissions)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubChildNodesRequest(
            parent_type=parent_type,
            parent_id=parent_id,
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            flattened=flattened,
            include=include,
        )

        req = self._build_request(
            method="GET",
            path="/api/v1/knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubChildNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_knowledge_hub_child_nodes_async(
        self,
        *,
        parent_type: models.ParentType,
        parent_id: str,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[str] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubChildNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        flattened: Optional[bool] = False,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub child nodes

        Get children of a specific node. Retrieve child nodes under a specific parent in the knowledge hub tree.<br><br>
        <b>Navigation:</b><br>
        Use this to drill down into collections, folders, and connector hierarchies.<br><br>
        parent_type must be one of: app, recordGroup, folder, record.


        :param parent_type: Type of parent node (app, recordGroup, folder, record)
        :param parent_id: ID of parent node (UUID or knowledgeBase_<orgId> for Collection app)
        :param only_containers: Only return nodes with children (for sidebar)
        :param page: Page number (1-indexed)
        :param limit: Items per page
        :param sort_by: Sort field (name, createdAt, updatedAt, size, type)
        :param sort_order: Sort order (asc or desc)
        :param q: Full-text search query
        :param node_types: Filter by node types (comma-separated)
        :param record_types: Comma-separated record types
        :param origins: Comma-separated origins (COLLECTION, CONNECTOR)
        :param connector_ids: Comma-separated connector instance IDs
        :param indexing_status: Comma-separated indexing statuses
        :param created_at: Created date range (gte:timestamp,lte:timestamp)
        :param updated_at: Updated date range (gte:timestamp,lte:timestamp)
        :param size: Size range (gte:bytes,lte:bytes)
        :param flattened: Return flattened view with all nested children
        :param include: Comma-separated includes (breadcrumbs, counts, availableFilters, permissions)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubChildNodesRequest(
            parent_type=parent_type,
            parent_id=parent_id,
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            flattened=flattened,
            include=include,
        )

        req = self._build_request_async(
            method="GET",
            path="/api/v1/knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubChildNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
