"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from pipeshub_sdk import errors, models, utils
from pipeshub_sdk._hooks import HookContext
from pipeshub_sdk.types import OptionalNullable, UNSET
from pipeshub_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Mapping, Optional


class OAuthProvider(BaseSDK):
    r"""PipesHub OAuth 2.0 Authorization Server implementing RFC 6749, RFC 7636 (PKCE), and OpenID Connect.

    **Supported Grant Types:**
    - `authorization_code` - Standard OAuth flow with PKCE support
    - `client_credentials` - Machine-to-machine authentication
    - `refresh_token` - Token refresh for long-lived access

    **Security Features:**
    - PKCE (Proof Key for Code Exchange) for public clients
    - State parameter for CSRF protection
    - Configurable token lifetimes
    - Token revocation and introspection

    **OpenID Connect:**
    - ID tokens with standard claims
    - UserInfo endpoint for profile data
    - Discovery endpoint for automatic configuration

    **Machine tokens (`client_credentials`) — gateway and downstream identity:**
    Access tokens may encode **`userId === client_id`**. The **Node.js API gateway** resolves the effective user to the OAuth **app creator**: first using the JWT **`createdBy`** claim when present, otherwise by loading the OAuth app by **`client_id`** from the registry. After verification it sets the authenticated session to that creator and may attach **`x-oauth-user-id`** (resolved user id) on **outbound** HTTP calls to Python microservices so authorization and retrieval match the same principal. **Inbound** `x-oauth-user-id` from external clients is **removed** at the gateway to prevent spoofing.

    **Python services:** Validate `Authorization: Bearer` as today. When **`x-oauth-user-id`** is present (normally only when the Node gateway added it), use it as the effective **`userId`** for scopes and user-scoped logic; otherwise use the JWT payload’s **`userId`** as-is (which may still equal **`client_id`** if the caller bypassed the gateway).

    **Operational note:** Prefer tokens whose JWT already carries the creator as **`userId`**; use **`POST /oauth-clients/{appId}/revoke-all-tokens`** and obtain new tokens from **`POST /oauth2/token`** when rotating integrations.

    """

    def oauth_token(
        self,
        *,
        grant_type: models.GrantType,
        code: Optional[str] = None,
        redirect_uri: Optional[str] = None,
        client_id: Optional[str] = None,
        client_secret: Optional[str] = None,
        refresh_token: Optional[str] = None,
        scope: Optional[str] = None,
        code_verifier: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.OAuthTokenResponse:
        r"""Exchange authorization code for tokens

        OAuth 2.0 Token Endpoint (RFC 6749 Section 4.1.3).

        Exchanges an authorization code, client credentials, or refresh token for access tokens.

        **Grant Types:**
        - `authorization_code`: Exchange auth code for tokens (user-based)
        - `client_credentials`: Get tokens for machine-to-machine auth
        - `refresh_token`: Get new access token using refresh token

        For **`client_credentials`**, access tokens represent the **OAuth app creator** (the user who registered the client). The JWT may encode **`userId === client_id`**; the **Node API gateway** resolves the creator (**`createdBy`** claim or OAuth app lookup) and must not trust client-supplied **`x-oauth-user-id`** (stripped on ingress). Downstream Python services accept **`x-oauth-user-id`** only when set by the gateway — see **OAuth Provider** tag.

        **Client Authentication:**
        Can be provided via:
        - HTTP Basic auth: `Authorization: Basic base64(client_id:client_secret)`
        - Request body: `client_id` and `client_secret` parameters

        **PKCE Verification:**
        If authorization used PKCE, the `code_verifier` must be provided and will be
        verified against the stored code challenge.


        :param grant_type: OAuth grant type:
            - `authorization_code`: Exchange auth code for tokens
            - `client_credentials`: Machine-to-machine auth
            - `refresh_token`: Get new access token using refresh token

        :param code: Authorization code (required for authorization_code grant)
        :param redirect_uri: Redirect URI (required for authorization_code grant)
        :param client_id: Client ID (can also be sent via Basic auth header)
        :param client_secret: Client secret (can also be sent via Basic auth header)
        :param refresh_token: Refresh token (required for refresh_token grant)
        :param scope: Requested scopes (optional, defaults to original grant scopes)
        :param code_verifier: PKCE code verifier (RFC 7636). Required if code_challenge was used.
            Must be 43-128 characters from [A-Za-z0-9-._~]

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthTokenRequest(
            grant_type=grant_type,
            code=code,
            redirect_uri=redirect_uri,
            client_id=client_id,
            client_secret=client_secret,
            refresh_token=refresh_token,
            scope=scope,
            code_verifier=code_verifier,
        )

        req = self._build_request(
            method="POST",
            path="/oauth2/token",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthTokenRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthToken",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["400", "401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.OAuthTokenResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def oauth_token_async(
        self,
        *,
        grant_type: models.GrantType,
        code: Optional[str] = None,
        redirect_uri: Optional[str] = None,
        client_id: Optional[str] = None,
        client_secret: Optional[str] = None,
        refresh_token: Optional[str] = None,
        scope: Optional[str] = None,
        code_verifier: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.OAuthTokenResponse:
        r"""Exchange authorization code for tokens

        OAuth 2.0 Token Endpoint (RFC 6749 Section 4.1.3).

        Exchanges an authorization code, client credentials, or refresh token for access tokens.

        **Grant Types:**
        - `authorization_code`: Exchange auth code for tokens (user-based)
        - `client_credentials`: Get tokens for machine-to-machine auth
        - `refresh_token`: Get new access token using refresh token

        For **`client_credentials`**, access tokens represent the **OAuth app creator** (the user who registered the client). The JWT may encode **`userId === client_id`**; the **Node API gateway** resolves the creator (**`createdBy`** claim or OAuth app lookup) and must not trust client-supplied **`x-oauth-user-id`** (stripped on ingress). Downstream Python services accept **`x-oauth-user-id`** only when set by the gateway — see **OAuth Provider** tag.

        **Client Authentication:**
        Can be provided via:
        - HTTP Basic auth: `Authorization: Basic base64(client_id:client_secret)`
        - Request body: `client_id` and `client_secret` parameters

        **PKCE Verification:**
        If authorization used PKCE, the `code_verifier` must be provided and will be
        verified against the stored code challenge.


        :param grant_type: OAuth grant type:
            - `authorization_code`: Exchange auth code for tokens
            - `client_credentials`: Machine-to-machine auth
            - `refresh_token`: Get new access token using refresh token

        :param code: Authorization code (required for authorization_code grant)
        :param redirect_uri: Redirect URI (required for authorization_code grant)
        :param client_id: Client ID (can also be sent via Basic auth header)
        :param client_secret: Client secret (can also be sent via Basic auth header)
        :param refresh_token: Refresh token (required for refresh_token grant)
        :param scope: Requested scopes (optional, defaults to original grant scopes)
        :param code_verifier: PKCE code verifier (RFC 7636). Required if code_challenge was used.
            Must be 43-128 characters from [A-Za-z0-9-._~]

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthTokenRequest(
            grant_type=grant_type,
            code=code,
            redirect_uri=redirect_uri,
            client_id=client_id,
            client_secret=client_secret,
            refresh_token=refresh_token,
            scope=scope,
            code_verifier=code_verifier,
        )

        req = self._build_request_async(
            method="POST",
            path="/oauth2/token",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthTokenRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthToken",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["400", "401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.OAuthTokenResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def oauth_revoke(
        self,
        *,
        token: str,
        client_id: str,
        token_type_hint: Optional[models.OAuthRevokeRequestTokenTypeHint] = None,
        client_secret: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Revoke an access or refresh token

        OAuth 2.0 Token Revocation Endpoint (RFC 7009).

        Revokes an access token or refresh token, preventing further use.
        Revoking a refresh token also invalidates associated access tokens.

        **Use Cases:**
        - User logs out of third-party app
        - User revokes app access from account settings
        - Security incident response

        **Note:** Returns 200 OK even if token was already revoked or invalid
        (per RFC 7009, to prevent token enumeration).


        :param token: The token to revoke
        :param client_id: Client ID
        :param token_type_hint: Hint about token type (optional, improves performance)
        :param client_secret: Client secret
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthRevokeRequest(
            token=token,
            token_type_hint=token_type_hint,
            client_id=client_id,
            client_secret=client_secret,
        )

        req = self._build_request(
            method="POST",
            path="/oauth2/revoke",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthRevokeRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthRevoke",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def oauth_revoke_async(
        self,
        *,
        token: str,
        client_id: str,
        token_type_hint: Optional[models.OAuthRevokeRequestTokenTypeHint] = None,
        client_secret: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Revoke an access or refresh token

        OAuth 2.0 Token Revocation Endpoint (RFC 7009).

        Revokes an access token or refresh token, preventing further use.
        Revoking a refresh token also invalidates associated access tokens.

        **Use Cases:**
        - User logs out of third-party app
        - User revokes app access from account settings
        - Security incident response

        **Note:** Returns 200 OK even if token was already revoked or invalid
        (per RFC 7009, to prevent token enumeration).


        :param token: The token to revoke
        :param client_id: Client ID
        :param token_type_hint: Hint about token type (optional, improves performance)
        :param client_secret: Client secret
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthRevokeRequest(
            token=token,
            token_type_hint=token_type_hint,
            client_id=client_id,
            client_secret=client_secret,
        )

        req = self._build_request_async(
            method="POST",
            path="/oauth2/revoke",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthRevokeRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthRevoke",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def oauth_introspect(
        self,
        *,
        token: str,
        client_id: str,
        token_type_hint: Optional[models.OAuthIntrospectRequestTokenTypeHint] = None,
        client_secret: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.OAuthIntrospectResponse:
        r"""Introspect a token

        OAuth 2.0 Token Introspection Endpoint (RFC 7662).

        Check if a token is active and retrieve its metadata.

        **Use Cases:**
        - Resource servers validating tokens
        - Debugging token issues
        - Checking token scopes before processing requests

        **Response:**
        - Active token: Returns `active: true` with token metadata
        - Invalid/expired/revoked token: Returns only `active: false`


        :param token: The token to introspect
        :param client_id: Client ID
        :param token_type_hint: Hint about token type
        :param client_secret: Client secret
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthIntrospectRequest(
            token=token,
            token_type_hint=token_type_hint,
            client_id=client_id,
            client_secret=client_secret,
        )

        req = self._build_request(
            method="POST",
            path="/oauth2/introspect",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthIntrospectRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthIntrospect",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.OAuthIntrospectResponse, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def oauth_introspect_async(
        self,
        *,
        token: str,
        client_id: str,
        token_type_hint: Optional[models.OAuthIntrospectRequestTokenTypeHint] = None,
        client_secret: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.OAuthIntrospectResponse:
        r"""Introspect a token

        OAuth 2.0 Token Introspection Endpoint (RFC 7662).

        Check if a token is active and retrieve its metadata.

        **Use Cases:**
        - Resource servers validating tokens
        - Debugging token issues
        - Checking token scopes before processing requests

        **Response:**
        - Active token: Returns `active: true` with token metadata
        - Invalid/expired/revoked token: Returns only `active: false`


        :param token: The token to introspect
        :param client_id: Client ID
        :param token_type_hint: Hint about token type
        :param client_secret: Client secret
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.OAuthIntrospectRequest(
            token=token,
            token_type_hint=token_type_hint,
            client_id=client_id,
            client_secret=client_secret,
        )

        req = self._build_request_async(
            method="POST",
            path="/oauth2/introspect",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=False,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.OAuthIntrospectRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="oauthIntrospect",
                oauth2_scopes=[],
                security_source=None,
            ),
            request=req,
            error_status_codes=["401", "429", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.OAuthIntrospectResponse, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthErrorResponseData, http_res
            )
            raise errors.OAuthErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/json"):
            response_data = unmarshal_json_response(
                errors.OAuthClientManagementRateLimitErrorData, http_res
            )
            raise errors.OAuthClientManagementRateLimitError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
