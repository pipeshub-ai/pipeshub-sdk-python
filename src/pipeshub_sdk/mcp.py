"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from pipeshub_sdk import errors, models, utils
from pipeshub_sdk._hooks import HookContext
from pipeshub_sdk.types import OptionalNullable, UNSET
from pipeshub_sdk.utils import get_security_from_env
from pipeshub_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Mapping, Optional, Union


class Mcp(BaseSDK):
    r"""Model Context Protocol (MCP) endpoints for AI tool integration.

    PipesHub exposes a Streamable HTTP MCP server that allows MCP-compatible clients
    (such as Claude Desktop, Cursor, or custom agents) to interact with PipesHub tools
    including search, conversations, knowledge base management, and connector operations.

    **Transport:** Streamable HTTP (JSON-RPC over HTTP)
    **Base Path:** `/mcp` (not under `/api/v1`)

    **Authentication:**
    All MCP requests require a valid Bearer token or OAuth 2.0 access token.

    **Stateless Mode:**
    The server operates in stateless mode — each request creates an independent MCP session.

    """

    def handle_mcp_request(
        self,
        *,
        jsonrpc: models.JsonrpcRequest,
        method: str,
        id: Optional[Union[models.IDRequest, models.IDRequestTypedDict]] = None,
        params: Optional[Union[models.Params, models.ParamsTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.HandleMCPRequestResponse:
        r"""Handle MCP JSON-RPC request

        Main MCP endpoint that handles all JSON-RPC requests including:
        - `initialize` — Negotiate capabilities and protocol version
        - `tools/list` — List available tools
        - `tools/call` — Execute a tool (search, conversations, KB management, etc.)

        The server operates in stateless mode with Streamable HTTP transport.
        Each request creates an independent MCP server session.


        :param jsonrpc:
        :param method: MCP method (e.g. initialize, tools/list, tools/call)
        :param id:
        :param params:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = models.HANDLE_MCP_REQUEST_OP_SERVERS[0]
            url_variables = {
                "instance_url": "https://app.pipeshub.com",
            }

        request = models.HandleMCPRequestRequest(
            jsonrpc=jsonrpc,
            id=id,
            method=method,
            params=utils.get_pydantic_model(params, Optional[models.Params]),
        )

        req = self._build_request(
            method="POST",
            path="/mcp",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.HandleMCPRequestRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="handleMCPRequest",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.HandleMCPRequestResponse, http_res)
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def handle_mcp_request_async(
        self,
        *,
        jsonrpc: models.JsonrpcRequest,
        method: str,
        id: Optional[Union[models.IDRequest, models.IDRequestTypedDict]] = None,
        params: Optional[Union[models.Params, models.ParamsTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.HandleMCPRequestResponse:
        r"""Handle MCP JSON-RPC request

        Main MCP endpoint that handles all JSON-RPC requests including:
        - `initialize` — Negotiate capabilities and protocol version
        - `tools/list` — List available tools
        - `tools/call` — Execute a tool (search, conversations, KB management, etc.)

        The server operates in stateless mode with Streamable HTTP transport.
        Each request creates an independent MCP server session.


        :param jsonrpc:
        :param method: MCP method (e.g. initialize, tools/list, tools/call)
        :param id:
        :param params:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = models.HANDLE_MCP_REQUEST_OP_SERVERS[0]
            url_variables = {
                "instance_url": "https://app.pipeshub.com",
            }

        request = models.HandleMCPRequestRequest(
            jsonrpc=jsonrpc,
            id=id,
            method=method,
            params=utils.get_pydantic_model(params, Optional[models.Params]),
        )

        req = self._build_request_async(
            method="POST",
            path="/mcp",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.HandleMCPRequestRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="handleMCPRequest",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.HandleMCPRequestResponse, http_res)
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def handle_mcp_streaming_request(
        self,
        *,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> str:
        r"""MCP SSE streaming endpoint

        Used by MCP clients for SSE (Server-Sent Events) streaming.
        In stateless mode, the transport returns 405 Method Not Allowed
        since each request is handled independently via POST.


        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = models.HANDLE_MCP_STREAMING_REQUEST_OP_SERVERS[0]
            url_variables = {
                "instance_url": "https://app.pipeshub.com",
            }
        req = self._build_request(
            method="GET",
            path="/mcp",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="handleMCPStreamingRequest",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "405", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return http_res.text
        if utils.match_response(http_res, ["401", "405", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def handle_mcp_streaming_request_async(
        self,
        *,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> str:
        r"""MCP SSE streaming endpoint

        Used by MCP clients for SSE (Server-Sent Events) streaming.
        In stateless mode, the transport returns 405 Method Not Allowed
        since each request is handled independently via POST.


        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = models.HANDLE_MCP_STREAMING_REQUEST_OP_SERVERS[0]
            url_variables = {
                "instance_url": "https://app.pipeshub.com",
            }
        req = self._build_request_async(
            method="GET",
            path="/mcp",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="handleMCPStreamingRequest",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "405", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return http_res.text
        if utils.match_response(http_res, ["401", "405", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
