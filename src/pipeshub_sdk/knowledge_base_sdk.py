"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from pipeshub_sdk import errors, models, utils
from pipeshub_sdk._hooks import HookContext
from pipeshub_sdk.types import Nullable, OptionalNullable, UNSET
from pipeshub_sdk.utils import eventstreaming, get_security_from_env
from pipeshub_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, List, Mapping, Optional, Union


class KnowledgeBaseSDK(BaseSDK):
    r"""Knowledge base management operations"""

    def create_knowledge_base(
        self,
        *,
        kb_name: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeBaseCreateResponse:
        r"""Create a new knowledge base

        Create a new knowledge base for organizing and managing documents within your organization.

        **Overview:**

        A knowledge base is a container for organizing related documents, files, and content. It provides a central location for teams to collaborate on shared information.

        **Features:**

        - Hierarchical folder structure support
        - Role-based access control (OWNER, WRITER, READER)
        - Full-text search across all records
        - Integration with external connectors (Google Drive, OneDrive, etc.)
        - Automatic content indexing for AI-powered search

        **Naming Rules:**

        - Name must be 1-255 characters
        - Special characters and HTML tags are sanitized
        - Names don't need to be unique within organization

        **Creator Permissions:**

        The user creating the KB automatically becomes the OWNER with full administrative rights.


        :param kb_name: Name of the knowledge base
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateKnowledgeBaseRequest(
            kb_name=kb_name,
        )

        req = self._build_request(
            method="POST",
            path="/knowledgeBase",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateKnowledgeBaseRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createKnowledgeBase",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeBaseCreateResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def create_knowledge_base_async(
        self,
        *,
        kb_name: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeBaseCreateResponse:
        r"""Create a new knowledge base

        Create a new knowledge base for organizing and managing documents within your organization.

        **Overview:**

        A knowledge base is a container for organizing related documents, files, and content. It provides a central location for teams to collaborate on shared information.

        **Features:**

        - Hierarchical folder structure support
        - Role-based access control (OWNER, WRITER, READER)
        - Full-text search across all records
        - Integration with external connectors (Google Drive, OneDrive, etc.)
        - Automatic content indexing for AI-powered search

        **Naming Rules:**

        - Name must be 1-255 characters
        - Special characters and HTML tags are sanitized
        - Names don't need to be unique within organization

        **Creator Permissions:**

        The user creating the KB automatically becomes the OWNER with full administrative rights.


        :param kb_name: Name of the knowledge base
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateKnowledgeBaseRequest(
            kb_name=kb_name,
        )

        req = self._build_request_async(
            method="POST",
            path="/knowledgeBase",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateKnowledgeBaseRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createKnowledgeBase",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeBaseCreateResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def list_knowledge_bases(
        self,
        *,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        search: Optional[str] = None,
        permissions: Optional[str] = None,
        sort_by: Optional[models.ListKnowledgeBasesSortBy] = "name",
        sort_order: Optional[models.ListKnowledgeBasesSortOrder] = "asc",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetAllKnowledgeBaseResponseSchema:
        r"""List all knowledge bases

        Retrieve a paginated list of all knowledge bases accessible to the authenticated user.

        **Overview:**

        Returns knowledge bases where the user has at least READER permission. Results include the user's role for each KB.

        **Filtering:**

        - **search:** Full-text search on KB names (max 1000 chars)
        - **permissions:** Filter by user's role (comma-separated: OWNER, WRITER, READER)

        **Sorting Options:**

        - `name` — Alphabetical by KB name
        - `createdAtTimestamp` — By creation date
        - `updatedAtTimestamp` — By last modification
        - `userRole` — By permission level

        **Performance:**

        Uses efficient pagination with limit/offset. For large result sets, use smaller page sizes.

        **Query parameters:**

        Only `page`, `limit`, `search`, `permissions`, `sortBy`, and `sortOrder` are allowed; unknown query keys are rejected.


        :param page: Page number (1-indexed). Omitted values default to 1.
        :param limit: Results per page (max 100). Omitted values default to 20.
        :param search: Search KB names (max 1000 chars). Rejected if it contains HTML/script tags,
            event handlers, `javascript:`, or format specifiers (validated in Zod + controller).

        :param permissions: Comma-separated permission roles to filter by. Each token must be one of:
            OWNER, WRITER, READER.

        :param sort_by: Field to sort by.
        :param sort_order: Sort direction.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ListKnowledgeBasesRequest(
            page=page,
            limit=limit,
            search=search,
            permissions=permissions,
            sort_by=sort_by,
            sort_order=sort_order,
        )

        req = self._build_request(
            method="GET",
            path="/knowledgeBase",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listKnowledgeBases",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.GetAllKnowledgeBaseResponseSchema, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def list_knowledge_bases_async(
        self,
        *,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        search: Optional[str] = None,
        permissions: Optional[str] = None,
        sort_by: Optional[models.ListKnowledgeBasesSortBy] = "name",
        sort_order: Optional[models.ListKnowledgeBasesSortOrder] = "asc",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetAllKnowledgeBaseResponseSchema:
        r"""List all knowledge bases

        Retrieve a paginated list of all knowledge bases accessible to the authenticated user.

        **Overview:**

        Returns knowledge bases where the user has at least READER permission. Results include the user's role for each KB.

        **Filtering:**

        - **search:** Full-text search on KB names (max 1000 chars)
        - **permissions:** Filter by user's role (comma-separated: OWNER, WRITER, READER)

        **Sorting Options:**

        - `name` — Alphabetical by KB name
        - `createdAtTimestamp` — By creation date
        - `updatedAtTimestamp` — By last modification
        - `userRole` — By permission level

        **Performance:**

        Uses efficient pagination with limit/offset. For large result sets, use smaller page sizes.

        **Query parameters:**

        Only `page`, `limit`, `search`, `permissions`, `sortBy`, and `sortOrder` are allowed; unknown query keys are rejected.


        :param page: Page number (1-indexed). Omitted values default to 1.
        :param limit: Results per page (max 100). Omitted values default to 20.
        :param search: Search KB names (max 1000 chars). Rejected if it contains HTML/script tags,
            event handlers, `javascript:`, or format specifiers (validated in Zod + controller).

        :param permissions: Comma-separated permission roles to filter by. Each token must be one of:
            OWNER, WRITER, READER.

        :param sort_by: Field to sort by.
        :param sort_order: Sort direction.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ListKnowledgeBasesRequest(
            page=page,
            limit=limit,
            search=search,
            permissions=permissions,
            sort_by=sort_by,
            sort_order=sort_order,
        )

        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listKnowledgeBases",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.GetAllKnowledgeBaseResponseSchema, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_knowledge_base(
        self,
        *,
        kb_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetKnowledgeBaseByID:
        r"""Get knowledge base by ID

        Retrieve detailed information about a specific knowledge base.

        **Overview:**

        Returns complete KB metadata including name, timestamps, root-level folders, and the requesting user's role.

        **Access Control:**

        User must have at least READER permission to view KB details.


        :param kb_id: Knowledge base ID (non-empty string)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeBaseRequest(
            kb_id=kb_id,
        )

        req = self._build_request(
            method="GET",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeBase",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetKnowledgeBaseByID, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_knowledge_base_async(
        self,
        *,
        kb_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetKnowledgeBaseByID:
        r"""Get knowledge base by ID

        Retrieve detailed information about a specific knowledge base.

        **Overview:**

        Returns complete KB metadata including name, timestamps, root-level folders, and the requesting user's role.

        **Access Control:**

        User must have at least READER permission to view KB details.


        :param kb_id: Knowledge base ID (non-empty string)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeBaseRequest(
            kb_id=kb_id,
        )

        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeBase",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetKnowledgeBaseByID, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def update_knowledge_base(
        self,
        *,
        kb_id: str,
        kb_name: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateKnowledgeBaseByID:
        r"""Update knowledge base

        Update a knowledge base's name.

        **Required permission:**

        User must have one of `OWNER` or `WRITER` on the knowledge base.

        **Validation:**

        - `kbId` path parameter must be a valid UUID (`updateKBSchema`)
        - When provided, `kbName` must be 1–255 characters
        - XSS and format-specifier checks are applied to `kbName` in the gateway controller


        :param kb_id: Knowledge base ID (UUID)
        :param kb_name: New name for the knowledge base
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateKnowledgeBaseRequest(
            kb_id=kb_id,
            body=models.UpdateKnowledgeBaseRequestBody(
                kb_name=kb_name,
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.UpdateKnowledgeBaseRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateKnowledgeBase",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateKnowledgeBaseByID, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_knowledge_base_async(
        self,
        *,
        kb_id: str,
        kb_name: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateKnowledgeBaseByID:
        r"""Update knowledge base

        Update a knowledge base's name.

        **Required permission:**

        User must have one of `OWNER` or `WRITER` on the knowledge base.

        **Validation:**

        - `kbId` path parameter must be a valid UUID (`updateKBSchema`)
        - When provided, `kbName` must be 1–255 characters
        - XSS and format-specifier checks are applied to `kbName` in the gateway controller


        :param kb_id: Knowledge base ID (UUID)
        :param kb_name: New name for the knowledge base
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateKnowledgeBaseRequest(
            kb_id=kb_id,
            body=models.UpdateKnowledgeBaseRequestBody(
                kb_name=kb_name,
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.UpdateKnowledgeBaseRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateKnowledgeBase",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateKnowledgeBaseByID, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_knowledge_base(
        self,
        *,
        kb_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteKnowledgeBaseByID:
        r"""Delete knowledge base

        Permanently delete a knowledge base and all its contents.

        **Required permission:**

        User must have `OWNER` role on the knowledge base.

        **What gets deleted:**

        - All folders within the KB
        - All records and their indexed content
        - All permission grants
        - Associated storage files

        **Warning:** This action is irreversible. Consider exporting data before deletion.


        :param kb_id: Knowledge base ID (non-empty string)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteKnowledgeBaseRequest(
            kb_id=kb_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteKnowledgeBase",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteKnowledgeBaseByID, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_knowledge_base_async(
        self,
        *,
        kb_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteKnowledgeBaseByID:
        r"""Delete knowledge base

        Permanently delete a knowledge base and all its contents.

        **Required permission:**

        User must have `OWNER` role on the knowledge base.

        **What gets deleted:**

        - All folders within the KB
        - All records and their indexed content
        - All permission grants
        - Associated storage files

        **Warning:** This action is irreversible. Consider exporting data before deletion.


        :param kb_id: Knowledge base ID (non-empty string)
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteKnowledgeBaseRequest(
            kb_id=kb_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/knowledgeBase/{kbId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteKnowledgeBase",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteKnowledgeBaseByID, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_record_by_id(
        self,
        *,
        record_id: str,
        convert_to: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetRecordByIDResponseSchema:
        r"""Get record by ID

        Retrieve detailed information about a specific record.

        **Overview:**

        Returns complete record metadata including name, type, indexing status, storage information, and version history.

        **File conversion:**

        Use the optional `convertTo` parameter to request file format conversion (e.g., PDF to text). Supported conversions include PPT to PDF and PPTX to PDF.


        :param record_id: Record ID
        :param convert_to: Optional format to convert the file to (e.g., PDF to text). Supported conversions include PPT to PDF and PPTX to PDF.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetRecordByIDRequest(
            record_id=record_id,
            convert_to=convert_to,
        )

        req = self._build_request(
            method="GET",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getRecordById",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetRecordByIDResponseSchema, http_res)
        if utils.match_response(http_res, ["400", "401", "403"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_record_by_id_async(
        self,
        *,
        record_id: str,
        convert_to: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetRecordByIDResponseSchema:
        r"""Get record by ID

        Retrieve detailed information about a specific record.

        **Overview:**

        Returns complete record metadata including name, type, indexing status, storage information, and version history.

        **File conversion:**

        Use the optional `convertTo` parameter to request file format conversion (e.g., PDF to text). Supported conversions include PPT to PDF and PPTX to PDF.


        :param record_id: Record ID
        :param convert_to: Optional format to convert the file to (e.g., PDF to text). Supported conversions include PPT to PDF and PPTX to PDF.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetRecordByIDRequest(
            record_id=record_id,
            convert_to=convert_to,
        )

        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getRecordById",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetRecordByIDResponseSchema, http_res)
        if utils.match_response(http_res, ["400", "401", "403"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def update_record(
        self,
        *,
        record_id: str,
        record_name: Optional[str] = None,
        file: Optional[
            Union[models.UpdateRecordFile, models.UpdateRecordFileTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateRecordResponse:
        r"""Update record

        Update a record's name and/or file content.

        **Overview:**

        Allows updating the display name and optionally replacing the file content. Triggers re-indexing when content changes.

        **Required permission:**

        WRITER or higher

        **Updating file content:**

        Include a new file in the request to replace the existing content. The file extension must match the original.

        **Side effects:**

        - Updates `updatedAtTimestamp`
        - Increments version if file content changed
        - Triggers re-indexing for content changes


        :param record_id: Record ID
        :param record_name: New name for the record
        :param file: Replacement file content
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateRecordRequest(
            record_id=record_id,
            body=models.UpdateRecordRequestBody(
                record_name=record_name,
                file=utils.get_pydantic_model(file, Optional[models.UpdateRecordFile]),
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "multipart",
                Optional[models.UpdateRecordRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateRecord",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateRecordResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "403", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_record_async(
        self,
        *,
        record_id: str,
        record_name: Optional[str] = None,
        file: Optional[
            Union[models.UpdateRecordFile, models.UpdateRecordFileTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateRecordResponse:
        r"""Update record

        Update a record's name and/or file content.

        **Overview:**

        Allows updating the display name and optionally replacing the file content. Triggers re-indexing when content changes.

        **Required permission:**

        WRITER or higher

        **Updating file content:**

        Include a new file in the request to replace the existing content. The file extension must match the original.

        **Side effects:**

        - Updates `updatedAtTimestamp`
        - Increments version if file content changed
        - Triggers re-indexing for content changes


        :param record_id: Record ID
        :param record_name: New name for the record
        :param file: Replacement file content
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateRecordRequest(
            record_id=record_id,
            body=models.UpdateRecordRequestBody(
                record_name=record_name,
                file=utils.get_pydantic_model(file, Optional[models.UpdateRecordFile]),
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "multipart",
                Optional[models.UpdateRecordRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateRecord",
                oauth2_scopes=[],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateRecordResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "403", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_record(
        self,
        *,
        record_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteRecordResponseSchema:
        r"""Delete record

        Permanently delete a record from the knowledge base.

        **Required permission:**

        WRITER or higher

        **What gets deleted:**

        - Record metadata
        - Associated storage file
        - Indexed content and embeddings

        **Warning:** This action is irreversible.


        :param record_id: Record ID
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteRecordRequest(
            record_id=record_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteRecord",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteRecordResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_record_async(
        self,
        *,
        record_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteRecordResponseSchema:
        r"""Delete record

        Permanently delete a record from the knowledge base.

        **Required permission:**

        WRITER or higher

        **What gets deleted:**

        - Record metadata
        - Associated storage file
        - Indexed content and embeddings

        **Warning:** This action is irreversible.


        :param record_id: Record ID
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteRecordRequest(
            record_id=record_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/knowledgeBase/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteRecord",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteRecordResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def create_folder(
        self,
        *,
        kb_id: str,
        folder_name: str,
        folder_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderCreateResponseSchema:
        r"""Create folder

        Create a folder in a knowledge base. Omit `folderId` to create at the KB root;
        pass `folderId` as a query parameter to create a nested subfolder inside an
        existing parent folder.

        **Required permission:** WRITER or higher

        **Folder features:**

        - Organize records hierarchically
        - Support nested subfolders (unlimited depth)
        - Inherit parent KB permissions

        **Naming rules:**

        - 1–255 characters
        - XSS protection applied
        - Spaces and special characters allowed
        - Duplicate names rejected within the same parent (`409`)

        **Response:** Returns `id` and `name` for the created folder.


        :param kb_id: Knowledge base ID
        :param folder_name: Name of the folder
        :param folder_id: Parent folder ID. Omit to create at the knowledge base root.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.CreateFolderRequestBody(
                folder_name=folder_name,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/knowledgeBase/{kbId}/folder",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.CreateFolderRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createFolder",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "409", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderCreateResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def create_folder_async(
        self,
        *,
        kb_id: str,
        folder_name: str,
        folder_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderCreateResponseSchema:
        r"""Create folder

        Create a folder in a knowledge base. Omit `folderId` to create at the KB root;
        pass `folderId` as a query parameter to create a nested subfolder inside an
        existing parent folder.

        **Required permission:** WRITER or higher

        **Folder features:**

        - Organize records hierarchically
        - Support nested subfolders (unlimited depth)
        - Inherit parent KB permissions

        **Naming rules:**

        - 1–255 characters
        - XSS protection applied
        - Spaces and special characters allowed
        - Duplicate names rejected within the same parent (`409`)

        **Response:** Returns `id` and `name` for the created folder.


        :param kb_id: Knowledge base ID
        :param folder_name: Name of the folder
        :param folder_id: Parent folder ID. Omit to create at the knowledge base root.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.CreateFolderRequestBody(
                folder_name=folder_name,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/knowledgeBase/{kbId}/folder",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.CreateFolderRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createFolder",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "409", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderCreateResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def update_folder(
        self,
        *,
        kb_id: str,
        folder_id: str,
        folder_name: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderUpdateResponseSchema:
        r"""Update folder

        Rename a folder.

        **Required permission:** WRITER or higher


        :param kb_id:
        :param folder_id:
        :param folder_name:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.UpdateFolderRequestBody(
                folder_name=folder_name,
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/knowledgeBase/{kbId}/folder/{folderId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.UpdateFolderRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateFolder",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "409", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderUpdateResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_folder_async(
        self,
        *,
        kb_id: str,
        folder_id: str,
        folder_name: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderUpdateResponseSchema:
        r"""Update folder

        Rename a folder.

        **Required permission:** WRITER or higher


        :param kb_id:
        :param folder_id:
        :param folder_name:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UpdateFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.UpdateFolderRequestBody(
                folder_name=folder_name,
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/knowledgeBase/{kbId}/folder/{folderId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models.UpdateFolderRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateFolder",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "409", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderUpdateResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_folder(
        self,
        *,
        kb_id: str,
        folder_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderDeleteResponseSchema:
        r"""Delete folder

        Delete a folder and all its contents.

        **Required permission:** WRITER or higher

        **Cascade delete:**

        All subfolders and records within will be permanently deleted.

        **Warning:** This action is irreversible.


        :param kb_id:
        :param folder_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/knowledgeBase/{kbId}/folder/{folderId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteFolder",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderDeleteResponseSchema, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_folder_async(
        self,
        *,
        kb_id: str,
        folder_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.FolderDeleteResponseSchema:
        r"""Delete folder

        Delete a folder and all its contents.

        **Required permission:** WRITER or higher

        **Cascade delete:**

        All subfolders and records within will be permanently deleted.

        **Warning:** This action is irreversible.


        :param kb_id:
        :param folder_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.DeleteFolderRequest(
            kb_id=kb_id,
            folder_id=folder_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/knowledgeBase/{kbId}/folder/{folderId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteFolder",
                oauth2_scopes=["kb:delete"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.FolderDeleteResponseSchema, http_res)
        if utils.match_response(http_res, ["401", "403", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def upload_records(
        self,
        *,
        kb_id: str,
        files: Union[
            List[models.UploadRecordsFile], List[models.UploadRecordsFileTypedDict]
        ],
        folder_id: Optional[str] = None,
        files_metadata: Optional[str] = None,
        is_versioned: Optional[bool] = True,
        record_type: Optional[str] = "FILE",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStream[models.UploadStreamSSEEvent]:
        r"""Upload files to knowledge base or folder

        Upload one or more files to a knowledge base root or to a specific folder.

        **Overview**

        Batch upload multiple files in a single request. Each file becomes a new record with automatic content indexing.
        Omit the `folderId` query parameter to upload to the KB root; include it to upload into that folder.

        **Upload Limits**

        - **Max files per request:** 1000
        - **Default max file size:** 30MB (configurable via platform settings)
        - Use `GET /knowledgeBase/limits` to check current limits

        **Supported File Types**

        Documents (PDF, DOCX, DOC, XLS, XLSX, PPT, PPTX, TXT, CSV, MD), Images (PNG, JPG, JPEG, SVG, WebP), Web (HTML, HTM), and Google Workspace formats.

        **File Metadata**

        Use `files_metadata` to provide additional info like file paths and last modified timestamps.

        **Versioning**

        Set `isVersioned: true` to enable version tracking for uploaded files.

        **Streaming response**

        This endpoint responds with `Content-Type: text/event-stream`.
        The upload and its per-file progress are a single request: the body streams
        a `file:succeeded` or `file:failed` event per file
        (including files rejected up front for size/type), followed by a final
        `done` summary, then closes. See the
        `UploadStreamSSEEvent` schema for the event/payload contract.


        :param kb_id: Knowledge base ID
        :param files: Files to upload (max 1000)
        :param folder_id: Target folder ID. Omit to upload to the KB root.
        :param files_metadata: JSON array with file_path and last_modified for each file
        :param is_versioned: Enable version tracking
        :param record_type: Type of records to create
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UploadRecordsRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.UploadRecordsRequestBody(
                files=utils.get_pydantic_model(files, List[models.UploadRecordsFile]),
                files_metadata=files_metadata,
                is_versioned=is_versioned,
                record_type=record_type,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/knowledgeBase/{kbId}/upload",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "multipart", models.UploadRecordsRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadRecords",
                oauth2_scopes=["kb:upload"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "413",
                "429",
                "4XX",
                "500",
                "5XX",
            ],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStream(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models.UploadStreamSSEEvent),
                client_ref=self,
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "413", "429", "4XX"], "*"
        ):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = utils.stream_to_text(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    async def upload_records_async(
        self,
        *,
        kb_id: str,
        files: Union[
            List[models.UploadRecordsFile], List[models.UploadRecordsFileTypedDict]
        ],
        folder_id: Optional[str] = None,
        files_metadata: Optional[str] = None,
        is_versioned: Optional[bool] = True,
        record_type: Optional[str] = "FILE",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStreamAsync[models.UploadStreamSSEEvent]:
        r"""Upload files to knowledge base or folder

        Upload one or more files to a knowledge base root or to a specific folder.

        **Overview**

        Batch upload multiple files in a single request. Each file becomes a new record with automatic content indexing.
        Omit the `folderId` query parameter to upload to the KB root; include it to upload into that folder.

        **Upload Limits**

        - **Max files per request:** 1000
        - **Default max file size:** 30MB (configurable via platform settings)
        - Use `GET /knowledgeBase/limits` to check current limits

        **Supported File Types**

        Documents (PDF, DOCX, DOC, XLS, XLSX, PPT, PPTX, TXT, CSV, MD), Images (PNG, JPG, JPEG, SVG, WebP), Web (HTML, HTM), and Google Workspace formats.

        **File Metadata**

        Use `files_metadata` to provide additional info like file paths and last modified timestamps.

        **Versioning**

        Set `isVersioned: true` to enable version tracking for uploaded files.

        **Streaming response**

        This endpoint responds with `Content-Type: text/event-stream`.
        The upload and its per-file progress are a single request: the body streams
        a `file:succeeded` or `file:failed` event per file
        (including files rejected up front for size/type), followed by a final
        `done` summary, then closes. See the
        `UploadStreamSSEEvent` schema for the event/payload contract.


        :param kb_id: Knowledge base ID
        :param files: Files to upload (max 1000)
        :param folder_id: Target folder ID. Omit to upload to the KB root.
        :param files_metadata: JSON array with file_path and last_modified for each file
        :param is_versioned: Enable version tracking
        :param record_type: Type of records to create
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.UploadRecordsRequest(
            kb_id=kb_id,
            folder_id=folder_id,
            body=models.UploadRecordsRequestBody(
                files=utils.get_pydantic_model(files, List[models.UploadRecordsFile]),
                files_metadata=files_metadata,
                is_versioned=is_versioned,
                record_type=record_type,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/knowledgeBase/{kbId}/upload",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "multipart", models.UploadRecordsRequestBody
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadRecords",
                oauth2_scopes=["kb:upload"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "413",
                "429",
                "4XX",
                "500",
                "5XX",
            ],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStreamAsync(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models.UploadStreamSSEEvent),
                client_ref=self,
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "413", "429", "4XX"], "*"
        ):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = await utils.stream_to_text_async(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    def get_upload_limits(
        self,
        *,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UploadLimitsResponseSchema:
        r"""Get knowledge base upload limits

        Retrieve current upload constraints for the organization.

        **Use case:** Call this before uploads to validate file sizes on the client
        side and display appropriate limits to users.


        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)
        req = self._build_request(
            method="GET",
            path="/knowledgeBase/limits",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getUploadLimits",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UploadLimitsResponseSchema, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_upload_limits_async(
        self,
        *,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UploadLimitsResponseSchema:
        r"""Get knowledge base upload limits

        Retrieve current upload constraints for the organization.

        **Use case:** Call this before uploads to validate file sizes on the client
        side and display appropriate limits to users.


        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)
        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase/limits",
            base_url=base_url,
            url_variables=url_variables,
            request=None,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getUploadLimits",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UploadLimitsResponseSchema, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def reindex_record(
        self,
        *,
        record_id: str,
        depth: Optional[int] = 0,
        force: Optional[bool] = False,
        status_filters: Optional[List[models.IndexingStatusFilter]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ReIndexRecordResponseSchema:
        r"""Reindex single record

        Trigger reindexing for a specific record.

        **Overview:**

        Reprocesses the record's content to update search indexes and AI embeddings. Useful after content changes or to fix indexing failures.

        **Depth parameter:**

        Controls processing depth for complex documents (`-1` for full depth, `0`–`100` for limited).

        **Status filters:**

        Optional `statusFilters` array limits reindex to records in matching indexing states
        (e.g. `FAILED`, `AUTO_INDEX_OFF`).


        :param record_id:
        :param depth: Child traversal depth (`0` = record only; higher values include
            descendants; `100` is used by clients for folder-like reindex).

        :param force: Force reindex even when the connector considers the record unchanged.
        :param status_filters: When set, only records whose indexing status matches one of these
            values are reindexed (applies to the record and its descendants per `depth`).

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ReindexRecordRequest(
            record_id=record_id,
            body=models.ReindexRecordRequestBody(
                depth=depth,
                force=force,
                status_filters=status_filters,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/knowledgeBase/reindex/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "json",
                Optional[models.ReindexRecordRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="reindexRecord",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "409",
                "4XX",
                "500",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ReIndexRecordResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def reindex_record_async(
        self,
        *,
        record_id: str,
        depth: Optional[int] = 0,
        force: Optional[bool] = False,
        status_filters: Optional[List[models.IndexingStatusFilter]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ReIndexRecordResponseSchema:
        r"""Reindex single record

        Trigger reindexing for a specific record.

        **Overview:**

        Reprocesses the record's content to update search indexes and AI embeddings. Useful after content changes or to fix indexing failures.

        **Depth parameter:**

        Controls processing depth for complex documents (`-1` for full depth, `0`–`100` for limited).

        **Status filters:**

        Optional `statusFilters` array limits reindex to records in matching indexing states
        (e.g. `FAILED`, `AUTO_INDEX_OFF`).


        :param record_id:
        :param depth: Child traversal depth (`0` = record only; higher values include
            descendants; `100` is used by clients for folder-like reindex).

        :param force: Force reindex even when the connector considers the record unchanged.
        :param status_filters: When set, only records whose indexing status matches one of these
            values are reindexed (applies to the record and its descendants per `depth`).

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ReindexRecordRequest(
            record_id=record_id,
            body=models.ReindexRecordRequestBody(
                depth=depth,
                force=force,
                status_filters=status_filters,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/knowledgeBase/reindex/record/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "json",
                Optional[models.ReindexRecordRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="reindexRecord",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "409",
                "4XX",
                "500",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ReIndexRecordResponseSchema, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def reindex_record_group(
        self,
        *,
        record_group_id: str,
        depth: Optional[int] = 0,
        force: Optional[bool] = False,
        status_filters: Optional[List[models.IndexingStatusFilter]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ReIndexRecordGroupResponseSchema:
        r"""Reindex record group

        Trigger reindexing for all records in a folder or knowledge base.

        **Overview:**

        Batch reindex operation for entire containers. The `recordGroupId` can be a folder ID or KB ID.

        **Status filters:**

        Optional `statusFilters` limit which child records are queued (e.g. failed-only or manual-indexing).


        :param record_group_id: Folder ID or KB ID
        :param depth: Depth of records under the record group to include.
        :param force: Force reindex for all matched records in the group.
        :param status_filters: When set, only records matching these indexing statuses are reindexed.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ReindexRecordGroupRequest(
            record_group_id=record_group_id,
            body=models.ReindexRecordGroupRequestBody(
                depth=depth,
                force=force,
                status_filters=status_filters,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/knowledgeBase/reindex/record-group/{recordGroupId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "json",
                Optional[models.ReindexRecordGroupRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="reindexRecordGroup",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "409",
                "4XX",
                "500",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.ReIndexRecordGroupResponseSchema, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def reindex_record_group_async(
        self,
        *,
        record_group_id: str,
        depth: Optional[int] = 0,
        force: Optional[bool] = False,
        status_filters: Optional[List[models.IndexingStatusFilter]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ReIndexRecordGroupResponseSchema:
        r"""Reindex record group

        Trigger reindexing for all records in a folder or knowledge base.

        **Overview:**

        Batch reindex operation for entire containers. The `recordGroupId` can be a folder ID or KB ID.

        **Status filters:**

        Optional `statusFilters` limit which child records are queued (e.g. failed-only or manual-indexing).


        :param record_group_id: Folder ID or KB ID
        :param depth: Depth of records under the record group to include.
        :param force: Force reindex for all matched records in the group.
        :param status_filters: When set, only records matching these indexing statuses are reindexed.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.ReindexRecordGroupRequest(
            record_group_id=record_group_id,
            body=models.ReindexRecordGroupRequestBody(
                depth=depth,
                force=force,
                status_filters=status_filters,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/knowledgeBase/reindex/record-group/{recordGroupId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body if request is not None else None,
                False,
                True,
                "json",
                Optional[models.ReindexRecordGroupRequestBody],
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="reindexRecordGroup",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=[
                "400",
                "401",
                "403",
                "404",
                "409",
                "4XX",
                "500",
                "503",
                "5XX",
            ],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.ReIndexRecordGroupResponseSchema, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404", "409"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def move_record(
        self,
        *,
        kb_id: str,
        record_id: str,
        new_parent_id: Nullable[str],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeBaseMoveRecordResponse:
        r"""Move record to another location

        Move a file or folder record to a different location within the same knowledge base.

        Set `newParentId` to a folder ID to move the record into that folder, or `null` to move it to the knowledge base root.

        **Required Permission:** OWNER or WRITER


        :param kb_id: Knowledge base UUID
        :param record_id: Record identifier (file or folder)
        :param new_parent_id: Target folder ID, or null to move the record to the knowledge base root
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.MoveRecordRequest(
            kb_id=kb_id,
            record_id=record_id,
            body=models.KnowledgeBaseMoveRecordRequestBody(
                new_parent_id=new_parent_id,
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/knowledgeBase/{kbId}/record/{recordId}/move",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.KnowledgeBaseMoveRecordRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="moveRecord",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.KnowledgeBaseMoveRecordResponse, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def move_record_async(
        self,
        *,
        kb_id: str,
        record_id: str,
        new_parent_id: Nullable[str],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeBaseMoveRecordResponse:
        r"""Move record to another location

        Move a file or folder record to a different location within the same knowledge base.

        Set `newParentId` to a folder ID to move the record into that folder, or `null` to move it to the knowledge base root.

        **Required Permission:** OWNER or WRITER


        :param kb_id: Knowledge base UUID
        :param record_id: Record identifier (file or folder)
        :param new_parent_id: Target folder ID, or null to move the record to the knowledge base root
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.MoveRecordRequest(
            kb_id=kb_id,
            record_id=record_id,
            body=models.KnowledgeBaseMoveRecordRequestBody(
                new_parent_id=new_parent_id,
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/knowledgeBase/{kbId}/record/{recordId}/move",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models.KnowledgeBaseMoveRecordRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="moveRecord",
                oauth2_scopes=["kb:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.KnowledgeBaseMoveRecordResponse, http_res
            )
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_knowledge_hub_root_nodes(
        self,
        *,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[models.GetKnowledgeHubRootNodesSortBy] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubRootNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub root nodes

        Returns root-level nodes (connector apps and Collection apps) or, when
        filters or search are applied, a flat list of matching nodes across the
        entire knowledge hub tree.

        **Overview**

        The Knowledge Hub provides a unified view across all knowledge sources:
        - **Collection** — locally uploaded knowledge bases (`origin: COLLECTION`)
        - **Connector app** — external connector instances such as Google Drive,
        Slack, Confluence, Jira (`origin: CONNECTOR`)

        Use this endpoint to build file-browser UIs and sidebar navigation trees.

        **Browsing vs. searching**

        When no filters or search query are provided, only top-level app nodes
        are returned. Adding `nodeTypes`, `q`, or other filter params triggers a
        search across the full tree, returning matching nodes regardless of depth.

        For children of a specific node, use
        `GET /knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}`.

        **Pagination and sorting**

        Results are always paginated. Default sort is `updatedAt` descending.
        The `pagination` object in the response contains `hasNext` / `hasPrev`
        flags suitable for infinite-scroll or page-based navigation.

        **Expanding the response**

        Use the `include` parameter to request additional sections:
        - `availableFilters` — adds `filters.available` with all filter options
        - `counts` — adds a `counts` summary broken down by node type
        - `breadcrumbs` — adds the breadcrumb trail (empty at root level)
        - `permissions` — adds the caller's permission flags

        **Access control**

        Requires a valid bearer token. For OAuth tokens the `kb:read` scope
        must be present; regular JWT bearer tokens pass through without scope
        enforcement.


        :param only_containers: When `true`, only nodes that have children are returned (useful for
            building sidebar / tree navigation). Leaf nodes are excluded.

        :param page: Page number (1-indexed). Combined with `limit` to paginate results.

        :param limit: Maximum number of items to return per page.

        :param sort_by: Field to sort results by. Omitted → default `updatedAt`.
            Unknown value → silently falls back to `name`.

        :param sort_order: Sort direction. Omitted → default `desc`.
            Unknown value → silently falls back to `asc`.

        :param q: Full-text search query. Must be between 2 and 500 characters
            (inclusive). When provided, the endpoint searches across the entire
            node tree regardless of the current browse level.

        :param node_types: Comma-separated list of node types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `folder`, `app`, `recordGroup`, `record`

        :param record_types: Comma-separated list of record types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `FILE`, `DRIVE`, `WEBPAGE`, `DATABASE`, `DATASOURCE`,
            `MESSAGE`, `MAIL`, `GROUP_MAIL`, `TICKET`, `COMMENT`,
            `INLINE_COMMENT`, `CONFLUENCE_PAGE`, `CONFLUENCE_BLOGPOST`,
            `SHAREPOINT_PAGE`, `SHAREPOINT_LIST`, `SHAREPOINT_LIST_ITEM`,
            `SHAREPOINT_DOCUMENT_LIBRARY`, `LINK`, `PROJECT`, `PULL_REQUEST`,
            `MEETING`, `PRODUCT`, `DEAL`, `CASE`, `TASK`, `ARTIFACT`,
            `CODE_FILE`, `SQL_TABLE`, `SQL_VIEW`, `OTHERS`

        :param origins: Comma-separated list of origin types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `COLLECTION`, `CONNECTOR`

        :param connector_ids: Comma-separated list of connector instance IDs (UUIDs) to filter by.
            Maximum 100 items. No enum validation — any string is accepted, but
            non-existent IDs simply yield zero results.

        :param indexing_status: Comma-separated list of indexing statuses to include. Invalid values
            are silently ignored. Maximum 100 items.

            Valid values: `NOT_STARTED`, `PAUSED`, `IN_PROGRESS`, `COMPLETED`,
            `FAILED`, `FILE_TYPE_NOT_SUPPORTED`, `AUTO_INDEX_OFF`, `EMPTY`,
            `ENABLE_MULTIMODAL_MODELS`, `QUEUED`

        :param created_at: Created-date range filter. Format: `gte:<epochMs>,lte:<epochMs>`.
            Both bounds are optional (you may send just `gte:...` or just
            `lte:...`). Timestamps must be in the range 0 to 9999999999999 and
            `gte` must be less than or equal to `lte` when both are present.

        :param updated_at: Updated-date range filter. Same format and constraints as `createdAt`.

        :param size: File-size range filter in bytes. Format: `gte:<bytes>,lte:<bytes>`.
            Both bounds are optional. Values must be non-negative and at most
            1099511627776 (1 TB). `gte` must be less than or equal to `lte`
            when both are present.

        :param include: Comma-separated list of additional response sections to include.
            Invalid values are silently ignored. Maximum 100 items.

            Valid values: `breadcrumbs`, `counts`, `availableFilters`, `permissions`

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubRootNodesRequest(
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            include=include,
        )

        req = self._build_request(
            method="GET",
            path="/knowledgeBase/knowledge-hub/nodes",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubRootNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesBadRequestErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesUnauthorizedErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesUnauthorizedError(
                response_data, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesForbiddenErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesForbiddenError(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesInternalServerErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesInternalServerError(
                response_data, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_knowledge_hub_root_nodes_async(
        self,
        *,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[models.GetKnowledgeHubRootNodesSortBy] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubRootNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub root nodes

        Returns root-level nodes (connector apps and Collection apps) or, when
        filters or search are applied, a flat list of matching nodes across the
        entire knowledge hub tree.

        **Overview**

        The Knowledge Hub provides a unified view across all knowledge sources:
        - **Collection** — locally uploaded knowledge bases (`origin: COLLECTION`)
        - **Connector app** — external connector instances such as Google Drive,
        Slack, Confluence, Jira (`origin: CONNECTOR`)

        Use this endpoint to build file-browser UIs and sidebar navigation trees.

        **Browsing vs. searching**

        When no filters or search query are provided, only top-level app nodes
        are returned. Adding `nodeTypes`, `q`, or other filter params triggers a
        search across the full tree, returning matching nodes regardless of depth.

        For children of a specific node, use
        `GET /knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}`.

        **Pagination and sorting**

        Results are always paginated. Default sort is `updatedAt` descending.
        The `pagination` object in the response contains `hasNext` / `hasPrev`
        flags suitable for infinite-scroll or page-based navigation.

        **Expanding the response**

        Use the `include` parameter to request additional sections:
        - `availableFilters` — adds `filters.available` with all filter options
        - `counts` — adds a `counts` summary broken down by node type
        - `breadcrumbs` — adds the breadcrumb trail (empty at root level)
        - `permissions` — adds the caller's permission flags

        **Access control**

        Requires a valid bearer token. For OAuth tokens the `kb:read` scope
        must be present; regular JWT bearer tokens pass through without scope
        enforcement.


        :param only_containers: When `true`, only nodes that have children are returned (useful for
            building sidebar / tree navigation). Leaf nodes are excluded.

        :param page: Page number (1-indexed). Combined with `limit` to paginate results.

        :param limit: Maximum number of items to return per page.

        :param sort_by: Field to sort results by. Omitted → default `updatedAt`.
            Unknown value → silently falls back to `name`.

        :param sort_order: Sort direction. Omitted → default `desc`.
            Unknown value → silently falls back to `asc`.

        :param q: Full-text search query. Must be between 2 and 500 characters
            (inclusive). When provided, the endpoint searches across the entire
            node tree regardless of the current browse level.

        :param node_types: Comma-separated list of node types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `folder`, `app`, `recordGroup`, `record`

        :param record_types: Comma-separated list of record types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `FILE`, `DRIVE`, `WEBPAGE`, `DATABASE`, `DATASOURCE`,
            `MESSAGE`, `MAIL`, `GROUP_MAIL`, `TICKET`, `COMMENT`,
            `INLINE_COMMENT`, `CONFLUENCE_PAGE`, `CONFLUENCE_BLOGPOST`,
            `SHAREPOINT_PAGE`, `SHAREPOINT_LIST`, `SHAREPOINT_LIST_ITEM`,
            `SHAREPOINT_DOCUMENT_LIBRARY`, `LINK`, `PROJECT`, `PULL_REQUEST`,
            `MEETING`, `PRODUCT`, `DEAL`, `CASE`, `TASK`, `ARTIFACT`,
            `CODE_FILE`, `SQL_TABLE`, `SQL_VIEW`, `OTHERS`

        :param origins: Comma-separated list of origin types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `COLLECTION`, `CONNECTOR`

        :param connector_ids: Comma-separated list of connector instance IDs (UUIDs) to filter by.
            Maximum 100 items. No enum validation — any string is accepted, but
            non-existent IDs simply yield zero results.

        :param indexing_status: Comma-separated list of indexing statuses to include. Invalid values
            are silently ignored. Maximum 100 items.

            Valid values: `NOT_STARTED`, `PAUSED`, `IN_PROGRESS`, `COMPLETED`,
            `FAILED`, `FILE_TYPE_NOT_SUPPORTED`, `AUTO_INDEX_OFF`, `EMPTY`,
            `ENABLE_MULTIMODAL_MODELS`, `QUEUED`

        :param created_at: Created-date range filter. Format: `gte:<epochMs>,lte:<epochMs>`.
            Both bounds are optional (you may send just `gte:...` or just
            `lte:...`). Timestamps must be in the range 0 to 9999999999999 and
            `gte` must be less than or equal to `lte` when both are present.

        :param updated_at: Updated-date range filter. Same format and constraints as `createdAt`.

        :param size: File-size range filter in bytes. Format: `gte:<bytes>,lte:<bytes>`.
            Both bounds are optional. Values must be non-negative and at most
            1099511627776 (1 TB). `gte` must be less than or equal to `lte`
            when both are present.

        :param include: Comma-separated list of additional response sections to include.
            Invalid values are silently ignored. Maximum 100 items.

            Valid values: `breadcrumbs`, `counts`, `availableFilters`, `permissions`

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubRootNodesRequest(
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            include=include,
        )

        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase/knowledge-hub/nodes",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubRootNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesBadRequestErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesUnauthorizedErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesUnauthorizedError(
                response_data, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesForbiddenErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesForbiddenError(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubRootNodesInternalServerErrorData, http_res
            )
            raise errors.GetKnowledgeHubRootNodesInternalServerError(
                response_data, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_knowledge_hub_child_nodes(
        self,
        *,
        parent_type: models.ParentType,
        parent_id: str,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[models.GetKnowledgeHubChildNodesSortBy] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubChildNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub child nodes

        Returns the children of a specific node in the knowledge hub tree.
        Use this endpoint to drill down into Collections, connector app
        hierarchies, folders, and record groups.

        **Navigation hierarchy**

        The typical drill-down path is:
        1. Root apps (`GET /knowledgeBase/knowledge-hub/nodes`)
        2. Record groups / folders within an app (`parentType=app`)
        3. Records within a record group (`parentType=recordGroup`)
        4. Sub-records or attachments within a record (`parentType=record`)

        **Parent identification**

        - `parentType` must be one of: `app`, `recordGroup`, `folder`, `record`
        - `parentId` is either a standard UUID or the Collection app sentinel
        `knowledgeBase_<orgId>` (e.g. `knowledgeBase_org123`)

        **Filtering and searching**

        All query-param filters from the root endpoint are available here and
        operate within the scope of the parent node's subtree. When `q` is
        provided, the search spans all descendants of the parent node.

        **Response extras**

        When `include=breadcrumbs` is set, the response contains a
        `breadcrumbs` array tracing the path from the root to the current
        node. The `currentNode` and `parentNode` objects are always populated
        for non-root requests.

        **Access control**

        Requires a valid bearer token. For OAuth tokens the `kb:read` scope
        must be present; regular JWT bearer tokens pass through without scope
        enforcement.


        :param parent_type: Type of the parent node whose children to retrieve.

            Must be one of: `app`, `recordGroup`, `folder`, `record`.
            Any other value returns a 400 error.

        :param parent_id: Identifier of the parent node. Accepts two formats:
            - A standard UUID (e.g. `f3a4b5b6-5b6c-4e85-9097-3202cfe696fc`)
            - The Collection app sentinel `knowledgeBase_<orgId>`
            (e.g. `knowledgeBase_org123`)

            Any value that does not match either format returns a 400 error.

        :param only_containers: When `true`, only nodes that have children are returned (useful for
            building sidebar / tree navigation). Leaf nodes are excluded.

        :param page: Page number (1-indexed). Combined with `limit` to paginate results.

        :param limit: Maximum number of items to return per page.

        :param sort_by: Field to sort results by. Omitted → default `updatedAt`.
            Unknown value → silently falls back to `name`.

        :param sort_order: Sort direction. Omitted → default `desc`.
            Unknown value → silently falls back to `asc`.

        :param q: Full-text search query. Must be between 2 and 500 characters
            (inclusive). When provided, the endpoint searches across all
            descendants of the parent node.

        :param node_types: Comma-separated list of node types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `folder`, `app`, `recordGroup`, `record`

        :param record_types: Comma-separated list of record types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `FILE`, `DRIVE`, `WEBPAGE`, `DATABASE`, `DATASOURCE`,
            `MESSAGE`, `MAIL`, `GROUP_MAIL`, `TICKET`, `COMMENT`,
            `INLINE_COMMENT`, `CONFLUENCE_PAGE`, `CONFLUENCE_BLOGPOST`,
            `SHAREPOINT_PAGE`, `SHAREPOINT_LIST`, `SHAREPOINT_LIST_ITEM`,
            `SHAREPOINT_DOCUMENT_LIBRARY`, `LINK`, `PROJECT`, `PULL_REQUEST`,
            `MEETING`, `PRODUCT`, `DEAL`, `CASE`, `TASK`, `ARTIFACT`,
            `CODE_FILE`, `SQL_TABLE`, `SQL_VIEW`, `OTHERS`

        :param origins: Comma-separated list of origin types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `COLLECTION`, `CONNECTOR`

        :param connector_ids: Comma-separated list of connector instance IDs (UUIDs) to filter by.
            Maximum 100 items. No enum validation — any string is accepted, but
            non-existent IDs simply yield zero results.

        :param indexing_status: Comma-separated list of indexing statuses to include. Invalid values
            are silently ignored. Maximum 100 items.

            Valid values: `NOT_STARTED`, `PAUSED`, `IN_PROGRESS`, `COMPLETED`,
            `FAILED`, `FILE_TYPE_NOT_SUPPORTED`, `AUTO_INDEX_OFF`, `EMPTY`,
            `ENABLE_MULTIMODAL_MODELS`, `QUEUED`

        :param created_at: Created-date range filter. Format: `gte:<epochMs>,lte:<epochMs>`.
            Both bounds are optional (you may send just `gte:...` or just
            `lte:...`). Timestamps must be in the range 0 to 9999999999999 and
            `gte` must be less than or equal to `lte` when both are present.

        :param updated_at: Updated-date range filter. Same format and constraints as `createdAt`.

        :param size: File-size range filter in bytes. Format: `gte:<bytes>,lte:<bytes>`.
            Both bounds are optional. Values must be non-negative and at most
            1099511627776 (1 TB). `gte` must be less than or equal to `lte`
            when both are present.

        :param include: Comma-separated list of additional response sections to include.
            Invalid values are silently ignored. Maximum 100 items.

            Valid values: `breadcrumbs`, `counts`, `availableFilters`, `permissions`

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubChildNodesRequest(
            parent_type=parent_type,
            parent_id=parent_id,
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            include=include,
        )

        req = self._build_request(
            method="GET",
            path="/knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubChildNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesBadRequestErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesUnauthorizedErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesUnauthorizedError(
                response_data, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesForbiddenErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesForbiddenError(
                response_data, http_res
            )
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesNotFoundErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesNotFoundError(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesInternalServerErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesInternalServerError(
                response_data, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_knowledge_hub_child_nodes_async(
        self,
        *,
        parent_type: models.ParentType,
        parent_id: str,
        only_containers: Optional[bool] = False,
        page: Optional[int] = 1,
        limit: Optional[int] = 50,
        sort_by: Optional[models.GetKnowledgeHubChildNodesSortBy] = "updatedAt",
        sort_order: Optional[models.GetKnowledgeHubChildNodesSortOrder] = "desc",
        q: Optional[str] = None,
        node_types: Optional[str] = None,
        record_types: Optional[str] = None,
        origins: Optional[str] = None,
        connector_ids: Optional[str] = None,
        indexing_status: Optional[str] = None,
        created_at: Optional[str] = None,
        updated_at: Optional[str] = None,
        size: Optional[str] = None,
        include: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.KnowledgeHubNodesResponse:
        r"""Get knowledge hub child nodes

        Returns the children of a specific node in the knowledge hub tree.
        Use this endpoint to drill down into Collections, connector app
        hierarchies, folders, and record groups.

        **Navigation hierarchy**

        The typical drill-down path is:
        1. Root apps (`GET /knowledgeBase/knowledge-hub/nodes`)
        2. Record groups / folders within an app (`parentType=app`)
        3. Records within a record group (`parentType=recordGroup`)
        4. Sub-records or attachments within a record (`parentType=record`)

        **Parent identification**

        - `parentType` must be one of: `app`, `recordGroup`, `folder`, `record`
        - `parentId` is either a standard UUID or the Collection app sentinel
        `knowledgeBase_<orgId>` (e.g. `knowledgeBase_org123`)

        **Filtering and searching**

        All query-param filters from the root endpoint are available here and
        operate within the scope of the parent node's subtree. When `q` is
        provided, the search spans all descendants of the parent node.

        **Response extras**

        When `include=breadcrumbs` is set, the response contains a
        `breadcrumbs` array tracing the path from the root to the current
        node. The `currentNode` and `parentNode` objects are always populated
        for non-root requests.

        **Access control**

        Requires a valid bearer token. For OAuth tokens the `kb:read` scope
        must be present; regular JWT bearer tokens pass through without scope
        enforcement.


        :param parent_type: Type of the parent node whose children to retrieve.

            Must be one of: `app`, `recordGroup`, `folder`, `record`.
            Any other value returns a 400 error.

        :param parent_id: Identifier of the parent node. Accepts two formats:
            - A standard UUID (e.g. `f3a4b5b6-5b6c-4e85-9097-3202cfe696fc`)
            - The Collection app sentinel `knowledgeBase_<orgId>`
            (e.g. `knowledgeBase_org123`)

            Any value that does not match either format returns a 400 error.

        :param only_containers: When `true`, only nodes that have children are returned (useful for
            building sidebar / tree navigation). Leaf nodes are excluded.

        :param page: Page number (1-indexed). Combined with `limit` to paginate results.

        :param limit: Maximum number of items to return per page.

        :param sort_by: Field to sort results by. Omitted → default `updatedAt`.
            Unknown value → silently falls back to `name`.

        :param sort_order: Sort direction. Omitted → default `desc`.
            Unknown value → silently falls back to `asc`.

        :param q: Full-text search query. Must be between 2 and 500 characters
            (inclusive). When provided, the endpoint searches across all
            descendants of the parent node.

        :param node_types: Comma-separated list of node types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `folder`, `app`, `recordGroup`, `record`

        :param record_types: Comma-separated list of record types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `FILE`, `DRIVE`, `WEBPAGE`, `DATABASE`, `DATASOURCE`,
            `MESSAGE`, `MAIL`, `GROUP_MAIL`, `TICKET`, `COMMENT`,
            `INLINE_COMMENT`, `CONFLUENCE_PAGE`, `CONFLUENCE_BLOGPOST`,
            `SHAREPOINT_PAGE`, `SHAREPOINT_LIST`, `SHAREPOINT_LIST_ITEM`,
            `SHAREPOINT_DOCUMENT_LIBRARY`, `LINK`, `PROJECT`, `PULL_REQUEST`,
            `MEETING`, `PRODUCT`, `DEAL`, `CASE`, `TASK`, `ARTIFACT`,
            `CODE_FILE`, `SQL_TABLE`, `SQL_VIEW`, `OTHERS`

        :param origins: Comma-separated list of origin types to include. Invalid values are
            silently ignored. Maximum 100 items.

            Valid values: `COLLECTION`, `CONNECTOR`

        :param connector_ids: Comma-separated list of connector instance IDs (UUIDs) to filter by.
            Maximum 100 items. No enum validation — any string is accepted, but
            non-existent IDs simply yield zero results.

        :param indexing_status: Comma-separated list of indexing statuses to include. Invalid values
            are silently ignored. Maximum 100 items.

            Valid values: `NOT_STARTED`, `PAUSED`, `IN_PROGRESS`, `COMPLETED`,
            `FAILED`, `FILE_TYPE_NOT_SUPPORTED`, `AUTO_INDEX_OFF`, `EMPTY`,
            `ENABLE_MULTIMODAL_MODELS`, `QUEUED`

        :param created_at: Created-date range filter. Format: `gte:<epochMs>,lte:<epochMs>`.
            Both bounds are optional (you may send just `gte:...` or just
            `lte:...`). Timestamps must be in the range 0 to 9999999999999 and
            `gte` must be less than or equal to `lte` when both are present.

        :param updated_at: Updated-date range filter. Same format and constraints as `createdAt`.

        :param size: File-size range filter in bytes. Format: `gte:<bytes>,lte:<bytes>`.
            Both bounds are optional. Values must be non-negative and at most
            1099511627776 (1 TB). `gte` must be less than or equal to `lte`
            when both are present.

        :param include: Comma-separated list of additional response sections to include.
            Invalid values are silently ignored. Maximum 100 items.

            Valid values: `breadcrumbs`, `counts`, `availableFilters`, `permissions`

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetKnowledgeHubChildNodesRequest(
            parent_type=parent_type,
            parent_id=parent_id,
            only_containers=only_containers,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            q=q,
            node_types=node_types,
            record_types=record_types,
            origins=origins,
            connector_ids=connector_ids,
            indexing_status=indexing_status,
            created_at=created_at,
            updated_at=updated_at,
            size=size,
            include=include,
        )

        req = self._build_request_async(
            method="GET",
            path="/knowledgeBase/knowledge-hub/nodes/{parentType}/{parentId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getKnowledgeHubChildNodes",
                oauth2_scopes=["kb:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.KnowledgeHubNodesResponse, http_res)
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesBadRequestErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesUnauthorizedErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesUnauthorizedError(
                response_data, http_res
            )
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesForbiddenErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesForbiddenError(
                response_data, http_res
            )
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesNotFoundErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesNotFoundError(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.GetKnowledgeHubChildNodesInternalServerErrorData, http_res
            )
            raise errors.GetKnowledgeHubChildNodesInternalServerError(
                response_data, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
