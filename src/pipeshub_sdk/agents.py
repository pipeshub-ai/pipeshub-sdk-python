"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import datetime
from pipeshub_sdk import errors, models as models_, utils
from pipeshub_sdk._hooks import HookContext
from pipeshub_sdk.types import OptionalNullable, UNSET
from pipeshub_sdk.utils import eventstreaming, get_security_from_env
from pipeshub_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, List, Mapping, Optional, Union


class Agents(BaseSDK):
    r"""Custom AI agents with specialized capabilities and tool integrations"""

    def list_agents(
        self,
        *,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        search: Optional[str] = None,
        sort_by: Optional[str] = "updatedAtTimestamp",
        sort_order: Optional[models_.ListAgentsSortOrder] = "desc",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentListResponse:
        r"""List agents

        Retrieve a paginated list of agents available to the authenticated user.

        **Overview**

        Returns agents accessible through direct, team, or org-level permissions.
        Search is performed across agent name, description, and tags. Sorting and
        pagination are applied by the AI backend and the resulting envelope is
        forwarded unchanged by the Node gateway.

        **Gateway contract**

        The Node route supports only these query params: `page`, `limit`, `search`,
        `sort_by`, and `sort_order`.

        The Python backend also understands `isDeleted`, but this gateway route
        does not forward it, so it is not part of the public API contract here.


        :param page: 1-based page number.
        :param limit: Maximum number of agents to return in the current page.
        :param search: Case-insensitive search across agent name, description, and tags. Leading/trailing whitespace is trimmed; blank-after-trim values are rejected.
        :param sort_by: Backend sort field. Leading/trailing whitespace is trimmed. Common value is `updatedAtTimestamp`.
        :param sort_order: Sort direction.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentsRequest(
            page=page,
            limit=limit,
            search=search,
            sort_by=sort_by,
            sort_order=sort_order,
        )

        req = self._build_request(
            method="GET",
            path="/agents",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgents",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentListResponse, http_res)
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def list_agents_async(
        self,
        *,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        search: Optional[str] = None,
        sort_by: Optional[str] = "updatedAtTimestamp",
        sort_order: Optional[models_.ListAgentsSortOrder] = "desc",
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentListResponse:
        r"""List agents

        Retrieve a paginated list of agents available to the authenticated user.

        **Overview**

        Returns agents accessible through direct, team, or org-level permissions.
        Search is performed across agent name, description, and tags. Sorting and
        pagination are applied by the AI backend and the resulting envelope is
        forwarded unchanged by the Node gateway.

        **Gateway contract**

        The Node route supports only these query params: `page`, `limit`, `search`,
        `sort_by`, and `sort_order`.

        The Python backend also understands `isDeleted`, but this gateway route
        does not forward it, so it is not part of the public API contract here.


        :param page: 1-based page number.
        :param limit: Maximum number of agents to return in the current page.
        :param search: Case-insensitive search across agent name, description, and tags. Leading/trailing whitespace is trimmed; blank-after-trim values are rejected.
        :param sort_by: Backend sort field. Leading/trailing whitespace is trimmed. Common value is `updatedAtTimestamp`.
        :param sort_order: Sort direction.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentsRequest(
            page=page,
            limit=limit,
            search=search,
            sort_by=sort_by,
            sort_order=sort_order,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgents",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentListResponse, http_res)
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def create_agent(
        self,
        *,
        name: str,
        description: Optional[str] = None,
        start_message: Optional[str] = None,
        system_prompt: Optional[str] = None,
        instructions: Optional[str] = None,
        models: Optional[
            Union[
                List[models_.AgentCreateModelEntryUnion],
                List[models_.AgentCreateModelEntryUnionTypedDict],
            ]
        ] = None,
        tags: Optional[List[str]] = None,
        share_with_org: Optional[bool] = False,
        is_service_account: Optional[bool] = False,
        toolsets: Optional[
            Union[
                List[models_.AgentCreateToolset],
                List[models_.AgentCreateToolsetTypedDict],
            ]
        ] = None,
        knowledge: Optional[
            Union[
                List[models_.AgentCreateKnowledge],
                List[models_.AgentCreateKnowledgeTypedDict],
            ]
        ] = None,
        skills: Optional[
            Union[
                List[models_.AgentSkillAssignment],
                List[models_.AgentSkillAssignmentTypedDict],
            ]
        ] = None,
        web_search: OptionalNullable[
            Union[
                models_.AgentCreateWebSearchUnion,
                models_.AgentCreateWebSearchUnionTypedDict,
            ]
        ] = UNSET,
        default_reasoning_effort: OptionalNullable[
            models_.AgentCreateRequestDefaultReasoningEffort
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentCreateResponse:
        r"""Create agent

        Create a new custom AI agent.

        **Overview:**
        Agents are specialized AI assistants configured for specific tasks.
        They can have custom system prompts, access to specific tools, and
        be limited to certain knowledge bases.

        **Agent Configuration:**
        - **System prompt:** Instructions that define agent behavior
        - **Tools:** Capabilities like web search, code execution, etc.
        - **Knowledge bases:** Data sources the agent can access
        - **Model config:** AI model settings (temperature, max tokens)

        **Use Cases:**
        - Customer support bot with product knowledge
        - Code review assistant with repository access
        - HR assistant with policy documents


        :param name: Agent display name
        :param description: What the agent does
        :param start_message: Initial greeting shown when conversation starts
        :param system_prompt: System instructions for the agent
        :param instructions: Additional agent execution instructions
        :param models: Agent model configuration entries. Optional — an agent created without
            any models (an empty array or an omitted field) uses the organization's
            default LLM at chat time. When at least one model entry IS provided, the
            gateway requires at least one object entry with `isReasoning: true`.
            String-only arrays are schema-valid but rejected at runtime with HTTP 400
            unless the array is empty.

        :param tags:
        :param share_with_org: Share agent with the organization
        :param is_service_account: Create the agent as a service-account agent
        :param toolsets: Toolsets attached to the agent (instance-aware)
        :param knowledge: Knowledge sources connected to the agent
        :param skills: Existing skills to assign to the agent
        :param web_search: Web-search attachment for an agent. Accepts either a provider string
            or an object with at least a `provider` field.

        :param default_reasoning_effort: Agent-level reasoning effort used when a chat request omits its own.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.AgentCreateRequest(
            name=name,
            description=description,
            start_message=start_message,
            system_prompt=system_prompt,
            instructions=instructions,
            models=utils.get_pydantic_model(
                models, Optional[List[models_.AgentCreateModelEntryUnion]]
            ),
            tags=tags,
            share_with_org=share_with_org,
            is_service_account=is_service_account,
            toolsets=utils.get_pydantic_model(
                toolsets, Optional[List[models_.AgentCreateToolset]]
            ),
            knowledge=utils.get_pydantic_model(
                knowledge, Optional[List[models_.AgentCreateKnowledge]]
            ),
            skills=utils.get_pydantic_model(
                skills, Optional[List[models_.AgentSkillAssignment]]
            ),
            web_search=utils.get_pydantic_model(
                web_search, OptionalNullable[models_.AgentCreateWebSearchUnion]
            ),
            default_reasoning_effort=default_reasoning_effort,
        )

        req = self._build_request(
            method="POST",
            path="/agents/create",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models_.AgentCreateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models_.AgentCreateResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def create_agent_async(
        self,
        *,
        name: str,
        description: Optional[str] = None,
        start_message: Optional[str] = None,
        system_prompt: Optional[str] = None,
        instructions: Optional[str] = None,
        models: Optional[
            Union[
                List[models_.AgentCreateModelEntryUnion],
                List[models_.AgentCreateModelEntryUnionTypedDict],
            ]
        ] = None,
        tags: Optional[List[str]] = None,
        share_with_org: Optional[bool] = False,
        is_service_account: Optional[bool] = False,
        toolsets: Optional[
            Union[
                List[models_.AgentCreateToolset],
                List[models_.AgentCreateToolsetTypedDict],
            ]
        ] = None,
        knowledge: Optional[
            Union[
                List[models_.AgentCreateKnowledge],
                List[models_.AgentCreateKnowledgeTypedDict],
            ]
        ] = None,
        skills: Optional[
            Union[
                List[models_.AgentSkillAssignment],
                List[models_.AgentSkillAssignmentTypedDict],
            ]
        ] = None,
        web_search: OptionalNullable[
            Union[
                models_.AgentCreateWebSearchUnion,
                models_.AgentCreateWebSearchUnionTypedDict,
            ]
        ] = UNSET,
        default_reasoning_effort: OptionalNullable[
            models_.AgentCreateRequestDefaultReasoningEffort
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentCreateResponse:
        r"""Create agent

        Create a new custom AI agent.

        **Overview:**
        Agents are specialized AI assistants configured for specific tasks.
        They can have custom system prompts, access to specific tools, and
        be limited to certain knowledge bases.

        **Agent Configuration:**
        - **System prompt:** Instructions that define agent behavior
        - **Tools:** Capabilities like web search, code execution, etc.
        - **Knowledge bases:** Data sources the agent can access
        - **Model config:** AI model settings (temperature, max tokens)

        **Use Cases:**
        - Customer support bot with product knowledge
        - Code review assistant with repository access
        - HR assistant with policy documents


        :param name: Agent display name
        :param description: What the agent does
        :param start_message: Initial greeting shown when conversation starts
        :param system_prompt: System instructions for the agent
        :param instructions: Additional agent execution instructions
        :param models: Agent model configuration entries. Optional — an agent created without
            any models (an empty array or an omitted field) uses the organization's
            default LLM at chat time. When at least one model entry IS provided, the
            gateway requires at least one object entry with `isReasoning: true`.
            String-only arrays are schema-valid but rejected at runtime with HTTP 400
            unless the array is empty.

        :param tags:
        :param share_with_org: Share agent with the organization
        :param is_service_account: Create the agent as a service-account agent
        :param toolsets: Toolsets attached to the agent (instance-aware)
        :param knowledge: Knowledge sources connected to the agent
        :param skills: Existing skills to assign to the agent
        :param web_search: Web-search attachment for an agent. Accepts either a provider string
            or an object with at least a `provider` field.

        :param default_reasoning_effort: Agent-level reasoning effort used when a chat request omits its own.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.AgentCreateRequest(
            name=name,
            description=description,
            start_message=start_message,
            system_prompt=system_prompt,
            instructions=instructions,
            models=utils.get_pydantic_model(
                models, Optional[List[models_.AgentCreateModelEntryUnion]]
            ),
            tags=tags,
            share_with_org=share_with_org,
            is_service_account=is_service_account,
            toolsets=utils.get_pydantic_model(
                toolsets, Optional[List[models_.AgentCreateToolset]]
            ),
            knowledge=utils.get_pydantic_model(
                knowledge, Optional[List[models_.AgentCreateKnowledge]]
            ),
            skills=utils.get_pydantic_model(
                skills, Optional[List[models_.AgentSkillAssignment]]
            ),
            web_search=utils.get_pydantic_model(
                web_search, OptionalNullable[models_.AgentCreateWebSearchUnion]
            ),
            default_reasoning_effort=default_reasoning_effort,
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/create",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models_.AgentCreateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="createAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models_.AgentCreateResponse, http_res)
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_agent(
        self,
        *,
        agent_key: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.GetAgentResponse:
        r"""Get agent

        Retrieve agent details by its unique key.

        **Gateway not-found behavior:**
        Unknown `agentKey`, lookup after soft-delete, and other AI-backend failures
        that return 404 from the Python query service are surfaced by the Node
        gateway as **HTTP 404** with an `ErrorResponse` body.


        :param agent_key: Unique agent identifier
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.GetAgentRequest(
            agent_key=agent_key,
        )

        req = self._build_request(
            method="GET",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getAgent",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.GetAgentResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_agent_async(
        self,
        *,
        agent_key: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.GetAgentResponse:
        r"""Get agent

        Retrieve agent details by its unique key.

        **Gateway not-found behavior:**
        Unknown `agentKey`, lookup after soft-delete, and other AI-backend failures
        that return 404 from the Python query service are surfaced by the Node
        gateway as **HTTP 404** with an `ErrorResponse` body.


        :param agent_key: Unique agent identifier
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.GetAgentRequest(
            agent_key=agent_key,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getAgent",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "503", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.GetAgentResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def update_agent(
        self,
        *,
        agent_key: str,
        name: Optional[str] = None,
        description: Optional[str] = None,
        start_message: Optional[str] = None,
        system_prompt: Optional[str] = None,
        instructions: Optional[str] = None,
        models: Optional[
            Union[
                List[models_.AgentCreateModelEntryUnion],
                List[models_.AgentCreateModelEntryUnionTypedDict],
            ]
        ] = None,
        tags: Optional[List[str]] = None,
        share_with_org: Optional[bool] = False,
        is_service_account: Optional[bool] = False,
        toolsets: Optional[
            Union[
                List[models_.AgentCreateToolset],
                List[models_.AgentCreateToolsetTypedDict],
            ]
        ] = None,
        knowledge: Optional[
            Union[
                List[models_.AgentCreateKnowledge],
                List[models_.AgentCreateKnowledgeTypedDict],
            ]
        ] = None,
        skills: Optional[
            Union[
                List[models_.AgentSkillAssignment],
                List[models_.AgentSkillAssignmentTypedDict],
            ]
        ] = None,
        web_search: OptionalNullable[
            Union[
                models_.AgentCreateWebSearchUnion,
                models_.AgentCreateWebSearchUnionTypedDict,
            ]
        ] = UNSET,
        default_reasoning_effort: OptionalNullable[
            models_.AgentUpdateRequestDefaultReasoningEffort
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentUpdateResponse:
        r"""Update agent

        Apply a partial update to an existing agent configuration.

        **Gateway contract**

        The Node gateway validates the request body via Zod middleware before
        forwarding to the Python agent service. The `agentKey` path param and
        the request body are both validated. Query parameters are ignored by
        the controller.

        **Update semantics**

        Only fields present in the request body are updated. `models` may be
        omitted (the agent's existing models are kept), set to an empty array
        (clears the agent's models so it falls back to the organization's
        default LLM at chat time), or set to a non-empty array. When a
        non-empty array is provided, the gateway Zod middleware requires at
        least one object entry with `isReasoning: true`.

        **Permissions**

        The authenticated user must have `can_edit` on the agent (typically the
        owner). Service-account and `shareWithOrg` transitions follow additional
        Python business rules.

        **Success response**

        Returns a lightweight success envelope only. Use
        `GET /agents/{agentKey}` to read the persisted agent after an update.


        :param agent_key: Unique agent identifier
        :param name: Agent display name
        :param description: What the agent does
        :param start_message: Initial greeting shown when conversation starts
        :param system_prompt: System instructions for the agent
        :param instructions: Additional agent execution instructions
        :param models: Agent model configuration entries. Optional. An empty array clears the
            agent's models so it falls back to the organization's default LLM.
            When a non-empty array is present, the Zod middleware requires at
            least one object entry with `isReasoning: true`. String-only arrays
            are schema-valid but rejected at runtime with HTTP 400 unless empty.

        :param tags:
        :param share_with_org: Share agent with the organization
        :param is_service_account: Mark agent as a service account
        :param toolsets: Toolsets attached to the agent (instance-aware)
        :param knowledge: Knowledge sources connected to the agent
        :param skills: Complete replacement set of skills assigned to the agent. Send
            an empty array to clear all skill assignments.

        :param web_search: Web-search attachment for an agent. Accepts either a provider string
            or an object with at least a `provider` field.

        :param default_reasoning_effort: Agent-level reasoning effort used when a chat request omits its own.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentRequest(
            agent_key=agent_key,
            body=models_.AgentUpdateRequest(
                name=name,
                description=description,
                start_message=start_message,
                system_prompt=system_prompt,
                instructions=instructions,
                models=utils.get_pydantic_model(
                    models, Optional[List[models_.AgentCreateModelEntryUnion]]
                ),
                tags=tags,
                share_with_org=share_with_org,
                is_service_account=is_service_account,
                toolsets=utils.get_pydantic_model(
                    toolsets, Optional[List[models_.AgentCreateToolset]]
                ),
                knowledge=utils.get_pydantic_model(
                    knowledge, Optional[List[models_.AgentCreateKnowledge]]
                ),
                skills=utils.get_pydantic_model(
                    skills, Optional[List[models_.AgentSkillAssignment]]
                ),
                web_search=utils.get_pydantic_model(
                    web_search, OptionalNullable[models_.AgentCreateWebSearchUnion]
                ),
                default_reasoning_effort=default_reasoning_effort,
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentUpdateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentUpdateResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_agent_async(
        self,
        *,
        agent_key: str,
        name: Optional[str] = None,
        description: Optional[str] = None,
        start_message: Optional[str] = None,
        system_prompt: Optional[str] = None,
        instructions: Optional[str] = None,
        models: Optional[
            Union[
                List[models_.AgentCreateModelEntryUnion],
                List[models_.AgentCreateModelEntryUnionTypedDict],
            ]
        ] = None,
        tags: Optional[List[str]] = None,
        share_with_org: Optional[bool] = False,
        is_service_account: Optional[bool] = False,
        toolsets: Optional[
            Union[
                List[models_.AgentCreateToolset],
                List[models_.AgentCreateToolsetTypedDict],
            ]
        ] = None,
        knowledge: Optional[
            Union[
                List[models_.AgentCreateKnowledge],
                List[models_.AgentCreateKnowledgeTypedDict],
            ]
        ] = None,
        skills: Optional[
            Union[
                List[models_.AgentSkillAssignment],
                List[models_.AgentSkillAssignmentTypedDict],
            ]
        ] = None,
        web_search: OptionalNullable[
            Union[
                models_.AgentCreateWebSearchUnion,
                models_.AgentCreateWebSearchUnionTypedDict,
            ]
        ] = UNSET,
        default_reasoning_effort: OptionalNullable[
            models_.AgentUpdateRequestDefaultReasoningEffort
        ] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentUpdateResponse:
        r"""Update agent

        Apply a partial update to an existing agent configuration.

        **Gateway contract**

        The Node gateway validates the request body via Zod middleware before
        forwarding to the Python agent service. The `agentKey` path param and
        the request body are both validated. Query parameters are ignored by
        the controller.

        **Update semantics**

        Only fields present in the request body are updated. `models` may be
        omitted (the agent's existing models are kept), set to an empty array
        (clears the agent's models so it falls back to the organization's
        default LLM at chat time), or set to a non-empty array. When a
        non-empty array is provided, the gateway Zod middleware requires at
        least one object entry with `isReasoning: true`.

        **Permissions**

        The authenticated user must have `can_edit` on the agent (typically the
        owner). Service-account and `shareWithOrg` transitions follow additional
        Python business rules.

        **Success response**

        Returns a lightweight success envelope only. Use
        `GET /agents/{agentKey}` to read the persisted agent after an update.


        :param agent_key: Unique agent identifier
        :param name: Agent display name
        :param description: What the agent does
        :param start_message: Initial greeting shown when conversation starts
        :param system_prompt: System instructions for the agent
        :param instructions: Additional agent execution instructions
        :param models: Agent model configuration entries. Optional. An empty array clears the
            agent's models so it falls back to the organization's default LLM.
            When a non-empty array is present, the Zod middleware requires at
            least one object entry with `isReasoning: true`. String-only arrays
            are schema-valid but rejected at runtime with HTTP 400 unless empty.

        :param tags:
        :param share_with_org: Share agent with the organization
        :param is_service_account: Mark agent as a service account
        :param toolsets: Toolsets attached to the agent (instance-aware)
        :param knowledge: Knowledge sources connected to the agent
        :param skills: Complete replacement set of skills assigned to the agent. Send
            an empty array to clear all skill assignments.

        :param web_search: Web-search attachment for an agent. Accepts either a provider string
            or an object with at least a `provider` field.

        :param default_reasoning_effort: Agent-level reasoning effort used when a chat request omits its own.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentRequest(
            agent_key=agent_key,
            body=models_.AgentUpdateRequest(
                name=name,
                description=description,
                start_message=start_message,
                system_prompt=system_prompt,
                instructions=instructions,
                models=utils.get_pydantic_model(
                    models, Optional[List[models_.AgentCreateModelEntryUnion]]
                ),
                tags=tags,
                share_with_org=share_with_org,
                is_service_account=is_service_account,
                toolsets=utils.get_pydantic_model(
                    toolsets, Optional[List[models_.AgentCreateToolset]]
                ),
                knowledge=utils.get_pydantic_model(
                    knowledge, Optional[List[models_.AgentCreateKnowledge]]
                ),
                skills=utils.get_pydantic_model(
                    skills, Optional[List[models_.AgentSkillAssignment]]
                ),
                web_search=utils.get_pydantic_model(
                    web_search, OptionalNullable[models_.AgentCreateWebSearchUnion]
                ),
                default_reasoning_effort=default_reasoning_effort,
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentUpdateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentUpdateResponse, http_res)
        if utils.match_response(
            http_res, ["400", "401", "403", "404"], "application/json"
        ):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_agent(
        self,
        *,
        agent_key: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentDeleteResponse:
        r"""Delete agent

        Soft-delete an agent (tombstone) in the graph database.

        **Overview:**
        The Python query service marks the agent instance deleted inside a transaction.
        List and search endpoints exclude tombstoned agents. Toolsets, tools, and
        knowledge linked to the agent are not removed by this call.

        **Permissions:**
        Only the agent owner may delete (`can_delete` on the permission check).

        **Warning:**
        All conversations with this agent will become inaccessible.

        **Gateway not-found behavior:**
        Unknown `agentKey`, deleting an already-deleted agent, and `GET /agents/{agentKey}`
        after delete return **HTTP 404** with an `ErrorResponse` body.


        :param agent_key: Unique agent identifier (gateway Zod requires non-empty string).
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentRequest(
            agent_key=agent_key,
        )

        req = self._build_request(
            method="DELETE",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentDeleteResponse, http_res)
        if utils.match_response(http_res, ["401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_agent_async(
        self,
        *,
        agent_key: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentDeleteResponse:
        r"""Delete agent

        Soft-delete an agent (tombstone) in the graph database.

        **Overview:**
        The Python query service marks the agent instance deleted inside a transaction.
        List and search endpoints exclude tombstoned agents. Toolsets, tools, and
        knowledge linked to the agent are not removed by this call.

        **Permissions:**
        Only the agent owner may delete (`can_delete` on the permission check).

        **Warning:**
        All conversations with this agent will become inaccessible.

        **Gateway not-found behavior:**
        Unknown `agentKey`, deleting an already-deleted agent, and `GET /agents/{agentKey}`
        after delete return **HTTP 404** with an `ErrorResponse` body.


        :param agent_key: Unique agent identifier (gateway Zod requires non-empty string).
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentRequest(
            agent_key=agent_key,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/agents/{agentKey}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgent",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models_.AgentDeleteResponse, http_res)
        if utils.match_response(http_res, ["401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def list_agent_archived_conversations_grouped(
        self,
        *,
        agent_page: Optional[int] = 1,
        agent_limit: Optional[int] = 5,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentArchivedGroupsResponse:
        r"""List archived agent conversations grouped by agent

        Returns archived agent conversations for the current user, grouped by
        `agentKey`, with pagination over agent groups. Excludes conversations
        whose agent was soft-deleted upstream.


        :param agent_page:
        :param agent_limit:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentArchivedConversationsGroupedRequest(
            agent_page=agent_page,
            agent_limit=agent_limit,
        )

        req = self._build_request(
            method="GET",
            path="/agents/conversations/show/archives",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentArchivedConversationsGrouped",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentArchivedGroupsResponse, http_res
            )
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def list_agent_archived_conversations_grouped_async(
        self,
        *,
        agent_page: Optional[int] = 1,
        agent_limit: Optional[int] = 5,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentArchivedGroupsResponse:
        r"""List archived agent conversations grouped by agent

        Returns archived agent conversations for the current user, grouped by
        `agentKey`, with pagination over agent groups. Excludes conversations
        whose agent was soft-deleted upstream.


        :param agent_page:
        :param agent_limit:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentArchivedConversationsGroupedRequest(
            agent_page=agent_page,
            agent_limit=agent_limit,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents/conversations/show/archives",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentArchivedConversationsGrouped",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentArchivedGroupsResponse, http_res
            )
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def list_agent_conversation_archives(
        self,
        *,
        agent_key: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[models_.ListAgentConversationArchivesSortBy] = None,
        sort_order: Optional[models_.ListAgentConversationArchivesSortOrder] = None,
        search: Optional[str] = None,
        start_date: Optional[datetime] = None,
        end_date: Optional[datetime] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentArchivedConversationListResponse:
        r"""List archived conversations for an agent

        Paginated list of archived conversations for the given agent key.

        :param agent_key:
        :param page:
        :param limit:
        :param sort_by:
        :param sort_order:
        :param search:
        :param start_date:
        :param end_date:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentConversationArchivesRequest(
            agent_key=agent_key,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            search=search,
            start_date=start_date,
            end_date=end_date,
        )

        req = self._build_request(
            method="GET",
            path="/agents/{agentKey}/conversations/show/archives",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentConversationArchives",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentArchivedConversationListResponse, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def list_agent_conversation_archives_async(
        self,
        *,
        agent_key: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[models_.ListAgentConversationArchivesSortBy] = None,
        sort_order: Optional[models_.ListAgentConversationArchivesSortOrder] = None,
        search: Optional[str] = None,
        start_date: Optional[datetime] = None,
        end_date: Optional[datetime] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentArchivedConversationListResponse:
        r"""List archived conversations for an agent

        Paginated list of archived conversations for the given agent key.

        :param agent_key:
        :param page:
        :param limit:
        :param sort_by:
        :param sort_order:
        :param search:
        :param start_date:
        :param end_date:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentConversationArchivesRequest(
            agent_key=agent_key,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            search=search,
            start_date=start_date,
            end_date=end_date,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents/{agentKey}/conversations/show/archives",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentConversationArchives",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentArchivedConversationListResponse, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, ["401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def upload_agent_conversation_chat_attachments(
        self,
        *,
        agent_key: str,
        files: Union[
            List[models_.UploadAgentConversationChatAttachmentsFile],
            List[models_.UploadAgentConversationChatAttachmentsFileTypedDict],
        ],
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.ChatAttachmentUploadResponse:
        r"""Upload agent chat attachments

        Multipart upload of PDF, JPEG, or PNG files for agent chat. Same limits as assistant
        chat (`POST /conversations/attachments/upload`): up to 10 files, 5 MiB each. Proxies to
        the AI backend. Optional `conversationId` associates uploads with an existing agent thread.


        :param agent_key:
        :param files: One or more files; field name must be `files`. Accepted MIME types: `application/pdf`, `image/jpeg`, `image/jpg`, `image/png`, `text/plain`, `text/markdown`, `text/mdx`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, `text/csv`, `text/tab-separated-values`. Max 5 MiB each.

        :param conversation_id: Optional existing agent conversation id. Empty string is treated as unset; any non-empty value must be a 24-character ObjectId.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UploadAgentConversationChatAttachmentsRequest(
            agent_key=agent_key,
            body=models_.UploadAgentConversationChatAttachmentsRequestBody(
                conversation_id=conversation_id,
                files=utils.get_pydantic_model(
                    files, List[models_.UploadAgentConversationChatAttachmentsFile]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/attachments/upload",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "multipart",
                models_.UploadAgentConversationChatAttachmentsRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadAgentConversationChatAttachments",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.ChatAttachmentUploadResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "403", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def upload_agent_conversation_chat_attachments_async(
        self,
        *,
        agent_key: str,
        files: Union[
            List[models_.UploadAgentConversationChatAttachmentsFile],
            List[models_.UploadAgentConversationChatAttachmentsFileTypedDict],
        ],
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.ChatAttachmentUploadResponse:
        r"""Upload agent chat attachments

        Multipart upload of PDF, JPEG, or PNG files for agent chat. Same limits as assistant
        chat (`POST /conversations/attachments/upload`): up to 10 files, 5 MiB each. Proxies to
        the AI backend. Optional `conversationId` associates uploads with an existing agent thread.


        :param agent_key:
        :param files: One or more files; field name must be `files`. Accepted MIME types: `application/pdf`, `image/jpeg`, `image/jpg`, `image/png`, `text/plain`, `text/markdown`, `text/mdx`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, `text/csv`, `text/tab-separated-values`. Max 5 MiB each.

        :param conversation_id: Optional existing agent conversation id. Empty string is treated as unset; any non-empty value must be a 24-character ObjectId.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UploadAgentConversationChatAttachmentsRequest(
            agent_key=agent_key,
            body=models_.UploadAgentConversationChatAttachmentsRequestBody(
                conversation_id=conversation_id,
                files=utils.get_pydantic_model(
                    files, List[models_.UploadAgentConversationChatAttachmentsFile]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/attachments/upload",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "multipart",
                models_.UploadAgentConversationChatAttachmentsRequestBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="uploadAgentConversationChatAttachments",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.ChatAttachmentUploadResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "403", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_agent_conversation_chat_attachment(
        self,
        *,
        agent_key: str,
        record_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Delete an agent chat attachment

        Deletes a previously uploaded attachment by proxying `DELETE` to the query service
        (`/api/v1/chat/attachments/{recordId}`). The Node handler always ends the response **without
        a JSON body** on success (empty body); the **status code** is the upstream status, or **204**
        if none is returned.

        On validation failure in the gateway (invalid / blank path params), the response is **400**
        with a small JSON error object. Same fire-and-forget semantics as
        `DELETE /conversations/attachments/{recordId}` on the client.


        :param agent_key: Agent key path parameter. Must be non-empty.
        :param record_id: Attachment record id (from the upload response). Must be non-blank after trim.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentConversationChatAttachmentRequest(
            agent_key=agent_key,
            record_id=record_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/agents/{agentKey}/conversations/attachments/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgentConversationChatAttachment",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "204", "*"):
            return
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.DeleteAgentConversationChatAttachmentBadRequestErrorData,
                http_res,
            )
            raise errors.DeleteAgentConversationChatAttachmentBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, ["401", "403", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "default", "*"):
            return

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_agent_conversation_chat_attachment_async(
        self,
        *,
        agent_key: str,
        record_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Delete an agent chat attachment

        Deletes a previously uploaded attachment by proxying `DELETE` to the query service
        (`/api/v1/chat/attachments/{recordId}`). The Node handler always ends the response **without
        a JSON body** on success (empty body); the **status code** is the upstream status, or **204**
        if none is returned.

        On validation failure in the gateway (invalid / blank path params), the response is **400**
        with a small JSON error object. Same fire-and-forget semantics as
        `DELETE /conversations/attachments/{recordId}` on the client.


        :param agent_key: Agent key path parameter. Must be non-empty.
        :param record_id: Attachment record id (from the upload response). Must be non-blank after trim.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentConversationChatAttachmentRequest(
            agent_key=agent_key,
            record_id=record_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/agents/{agentKey}/conversations/attachments/{recordId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgentConversationChatAttachment",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "403", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "204", "*"):
            return
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.DeleteAgentConversationChatAttachmentBadRequestErrorData,
                http_res,
            )
            raise errors.DeleteAgentConversationChatAttachmentBadRequestError(
                response_data, http_res
            )
        if utils.match_response(http_res, ["401", "403", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "default", "*"):
            return

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def stream_agent_conversation(
        self,
        *,
        agent_key: str,
        query: str,
        chat_mode: models_.AgentStreamCreateConversationRequestChatMode,
        record_ids: Optional[List[str]] = None,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        applied_filters: Optional[
            Union[models_.AppliedFilters, models_.AppliedFiltersTypedDict]
        ] = None,
        attachments: Optional[
            Union[
                List[models_.ChatAttachmentRef],
                List[models_.ChatAttachmentRefTypedDict],
            ]
        ] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentStreamCreateConversationRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStream[models_.AgentStreamSSEEvent]:
        r"""Create agent conversation with streaming response

        Start a new conversation with the specified agent and stream the AI
        response as Server-Sent Events (SSE). The first user message is saved
        and forwarded to the upstream agent backend; subsequent tokens, tool
        calls, and lifecycle events are emitted on the open SSE connection.

        AG-UI is the sole wire protocol. The request must include
        `chatMode: quick`; see `AgentStreamSSEEvent` for the event vocabulary.


        :param agent_key: Stable key identifying the agent that owns this conversation.
        :param query: User prompt for the first turn. Saved as the initial `user_query`
            message and sent to the agent backend.

        :param chat_mode: Required execution mode. Scoped agent conversations currently
            support only `quick`.

        :param record_ids: Optional record ids to include as context for this turn. Each id
            must be a 24-character MongoDB ObjectId.

        :param filters: Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
            be a valid UUID. Omit for agent defaults; send `{ \"apps\": [], \"kb\": [] }`
            to force no knowledge sources for this turn.

        :param applied_filters: UI filter state persisted on the saved user message. Not used for
            retrieval and not forwarded to the upstream agent backend.

        :param attachments: Uploaded attachments to ground this turn. Each entry references a
            record id returned from the agent attachment upload endpoint.

        :param model_key: AI model configuration id for this turn. Omit to use the agent's
            default model.

        :param model_name: Provider model name (the underlying LLM identifier).
        :param model_friendly_name: Friendly UI label for the selected model.
        :param timezone: Client IANA timezone, such as `America/New_York`. Helps the agent
            resolve relative date references in the prompt.

        :param current_time: Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
            offset). Sent alongside `timezone` for time-aware answers.

        :param tools: Allowed tool ids for this turn, such as `jira.create_issue`. Omit
            to let the agent use its default toolset; send `[]` to disable
            tools for this turn.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary (see `AgentStreamSSEEvent`). Kept in
            the schema for backward compatibility with callers that already
            send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.StreamAgentConversationRequest(
            agent_key=agent_key,
            body=models_.AgentStreamCreateConversationRequest(
                query=query,
                record_ids=record_ids,
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                applied_filters=utils.get_pydantic_model(
                    applied_filters, Optional[models_.AppliedFilters]
                ),
                attachments=utils.get_pydantic_model(
                    attachments, Optional[List[models_.ChatAttachmentRef]]
                ),
                chat_mode=chat_mode,
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models_.AgentStreamCreateConversationRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="streamAgentConversation",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStream(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models_.AgentStreamSSEEvent),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = utils.stream_to_text(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    async def stream_agent_conversation_async(
        self,
        *,
        agent_key: str,
        query: str,
        chat_mode: models_.AgentStreamCreateConversationRequestChatMode,
        record_ids: Optional[List[str]] = None,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        applied_filters: Optional[
            Union[models_.AppliedFilters, models_.AppliedFiltersTypedDict]
        ] = None,
        attachments: Optional[
            Union[
                List[models_.ChatAttachmentRef],
                List[models_.ChatAttachmentRefTypedDict],
            ]
        ] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentStreamCreateConversationRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStreamAsync[models_.AgentStreamSSEEvent]:
        r"""Create agent conversation with streaming response

        Start a new conversation with the specified agent and stream the AI
        response as Server-Sent Events (SSE). The first user message is saved
        and forwarded to the upstream agent backend; subsequent tokens, tool
        calls, and lifecycle events are emitted on the open SSE connection.

        AG-UI is the sole wire protocol. The request must include
        `chatMode: quick`; see `AgentStreamSSEEvent` for the event vocabulary.


        :param agent_key: Stable key identifying the agent that owns this conversation.
        :param query: User prompt for the first turn. Saved as the initial `user_query`
            message and sent to the agent backend.

        :param chat_mode: Required execution mode. Scoped agent conversations currently
            support only `quick`.

        :param record_ids: Optional record ids to include as context for this turn. Each id
            must be a 24-character MongoDB ObjectId.

        :param filters: Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
            be a valid UUID. Omit for agent defaults; send `{ \"apps\": [], \"kb\": [] }`
            to force no knowledge sources for this turn.

        :param applied_filters: UI filter state persisted on the saved user message. Not used for
            retrieval and not forwarded to the upstream agent backend.

        :param attachments: Uploaded attachments to ground this turn. Each entry references a
            record id returned from the agent attachment upload endpoint.

        :param model_key: AI model configuration id for this turn. Omit to use the agent's
            default model.

        :param model_name: Provider model name (the underlying LLM identifier).
        :param model_friendly_name: Friendly UI label for the selected model.
        :param timezone: Client IANA timezone, such as `America/New_York`. Helps the agent
            resolve relative date references in the prompt.

        :param current_time: Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
            offset). Sent alongside `timezone` for time-aware answers.

        :param tools: Allowed tool ids for this turn, such as `jira.create_issue`. Omit
            to let the agent use its default toolset; send `[]` to disable
            tools for this turn.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary (see `AgentStreamSSEEvent`). Kept in
            the schema for backward compatibility with callers that already
            send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.StreamAgentConversationRequest(
            agent_key=agent_key,
            body=models_.AgentStreamCreateConversationRequest(
                query=query,
                record_ids=record_ids,
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                applied_filters=utils.get_pydantic_model(
                    applied_filters, Optional[models_.AppliedFilters]
                ),
                attachments=utils.get_pydantic_model(
                    attachments, Optional[List[models_.ChatAttachmentRef]]
                ),
                chat_mode=chat_mode,
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models_.AgentStreamCreateConversationRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="streamAgentConversation",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStreamAsync(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models_.AgentStreamSSEEvent),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = await utils.stream_to_text_async(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    def stream_agent_conversation_message(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        query: str,
        chat_mode: models_.AgentAddMessageStreamRequestChatMode,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        applied_filters: Optional[
            Union[models_.AppliedFilters, models_.AppliedFiltersTypedDict]
        ] = None,
        attachments: Optional[
            Union[
                List[models_.ChatAttachmentRef],
                List[models_.ChatAttachmentRefTypedDict],
            ]
        ] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentAddMessageStreamRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStream[models_.AgentMessageStreamSSEEvent]:
        r"""Add message to agent conversation with streaming response

        Append a user message to an existing agent conversation and stream the
        assistant reply over SSE.

        AG-UI is the sole wire protocol. The request must include
        `chatMode: quick`; see `AgentMessageStreamSSEEvent` for the event
        vocabulary.


        :param agent_key:
        :param conversation_id:
        :param query: User follow-up prompt to append to the existing agent conversation.
            Saved as a new `user_query` message before the upstream AI stream
            starts.

        :param chat_mode: Required execution mode. Scoped agent conversations currently
            support only `quick`.

        :param filters: Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
            be a valid UUID. Omit to let the agent use its stored defaults;
            send `{ \"apps\": [], \"kb\": [] }` to force no knowledge sources for this turn.

        :param applied_filters: UI filter state persisted on the saved user message. Not used for
            retrieval and not forwarded to the upstream agent backend.

        :param attachments: Uploaded attachments to ground this turn. Each entry references a
            record id returned from the agent attachment upload endpoint.

        :param model_key: AI model configuration id override for this turn. Omit to use the
            agent's default model.

        :param model_name: Provider model name (the underlying LLM identifier).
        :param model_friendly_name: Friendly UI label for the selected model.
        :param timezone: Client IANA timezone, such as `America/New_York`. Helps the agent
            resolve relative date references in the prompt.

        :param current_time: Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
            offset). Sent alongside `timezone` for time-aware answers.

        :param tools: Allowed tool ids for this turn, such as `jira.create_issue`. Omit
            to let the agent use its default toolset; send `[]` to disable
            tools for this turn.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary (see `AgentMessageStreamSSEEvent`).
            Kept in the schema for backward compatibility with callers that
            already send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.StreamAgentConversationMessageRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            body=models_.AgentAddMessageStreamRequest(
                query=query,
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                applied_filters=utils.get_pydantic_model(
                    applied_filters, Optional[models_.AppliedFilters]
                ),
                attachments=utils.get_pydantic_model(
                    attachments, Optional[List[models_.ChatAttachmentRef]]
                ),
                chat_mode=chat_mode,
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/messages/stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentAddMessageStreamRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="streamAgentConversationMessage",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStream(
                http_res,
                lambda raw: utils.unmarshal_json(
                    raw, models_.AgentMessageStreamSSEEvent
                ),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = utils.stream_to_text(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    async def stream_agent_conversation_message_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        query: str,
        chat_mode: models_.AgentAddMessageStreamRequestChatMode,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        applied_filters: Optional[
            Union[models_.AppliedFilters, models_.AppliedFiltersTypedDict]
        ] = None,
        attachments: Optional[
            Union[
                List[models_.ChatAttachmentRef],
                List[models_.ChatAttachmentRefTypedDict],
            ]
        ] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentAddMessageStreamRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStreamAsync[models_.AgentMessageStreamSSEEvent]:
        r"""Add message to agent conversation with streaming response

        Append a user message to an existing agent conversation and stream the
        assistant reply over SSE.

        AG-UI is the sole wire protocol. The request must include
        `chatMode: quick`; see `AgentMessageStreamSSEEvent` for the event
        vocabulary.


        :param agent_key:
        :param conversation_id:
        :param query: User follow-up prompt to append to the existing agent conversation.
            Saved as a new `user_query` message before the upstream AI stream
            starts.

        :param chat_mode: Required execution mode. Scoped agent conversations currently
            support only `quick`.

        :param filters: Optional retrieval scope (`apps` / `kb`) for this turn. Each id must
            be a valid UUID. Omit to let the agent use its stored defaults;
            send `{ \"apps\": [], \"kb\": [] }` to force no knowledge sources for this turn.

        :param applied_filters: UI filter state persisted on the saved user message. Not used for
            retrieval and not forwarded to the upstream agent backend.

        :param attachments: Uploaded attachments to ground this turn. Each entry references a
            record id returned from the agent attachment upload endpoint.

        :param model_key: AI model configuration id override for this turn. Omit to use the
            agent's default model.

        :param model_name: Provider model name (the underlying LLM identifier).
        :param model_friendly_name: Friendly UI label for the selected model.
        :param timezone: Client IANA timezone, such as `America/New_York`. Helps the agent
            resolve relative date references in the prompt.

        :param current_time: Client time in ISO 8601 / RFC 3339 format (UTC `Z` or numeric
            offset). Sent alongside `timezone` for time-aware answers.

        :param tools: Allowed tool ids for this turn, such as `jira.create_issue`. Omit
            to let the agent use its default toolset; send `[]` to disable
            tools for this turn.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary (see `AgentMessageStreamSSEEvent`).
            Kept in the schema for backward compatibility with callers that
            already send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.StreamAgentConversationMessageRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            body=models_.AgentAddMessageStreamRequest(
                query=query,
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                applied_filters=utils.get_pydantic_model(
                    applied_filters, Optional[models_.AppliedFilters]
                ),
                attachments=utils.get_pydantic_model(
                    attachments, Optional[List[models_.ChatAttachmentRef]]
                ),
                chat_mode=chat_mode,
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/messages/stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentAddMessageStreamRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="streamAgentConversationMessage",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStreamAsync(
                http_res,
                lambda raw: utils.unmarshal_json(
                    raw, models_.AgentMessageStreamSSEEvent
                ),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = await utils.stream_to_text_async(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    def regenerate_agent_conversation_message(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        message_id: str,
        chat_mode: models_.AgentRegenerateRequestChatMode,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentRegenerateRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStream[models_.AgentRegenerateSSEEvent]:
        r"""Regenerate agent conversation message

        Regenerate the AI response for a specific message in an agent
        conversation and stream the new answer over Server-Sent Events.

        **Constraints:**

        - Only the last message in the conversation can be regenerated.
        - The target message must be of type `bot_response`.

        **Request body:**

        `chatMode: quick` is required. Other fields are optional and reuse
        the original model/context when omitted. The body supports:
        - `filters`
        - `chatMode`
        - `modelKey`
        - `modelName`
        - `modelFriendlyName`
        - `timezone`
        - `currentTime`
        - `tools`
        - `protocol`
        - `agentCapabilities`

        **Streaming behavior:**

        The response is delivered as an AG-UI `text/event-stream`. Stable
        outcomes are `RUN_FINISHED` and `RUN_ERROR`; see
        `AgentRegenerateSSEEvent`.

        Additional agent/tool lifecycle events may be forwarded by the
        backend and should be treated as informational updates.

        Validation failures on params/body are returned as normal HTTP `400`
        responses before the stream starts. Valid-shape requests that fail
        conversation lookup or regenerate rules are reported as
        `RUN_ERROR` events after stream initialization.


        :param agent_key: Stable key identifying the agent that owns this conversation.
        :param conversation_id: ID of the agent conversation containing the target message.
        :param message_id: ID of the bot-response message to regenerate.
        :param chat_mode:
        :param filters: App connector instance ids and knowledge-base / record-group ids that narrow retrieval
            for a turn. For **org assistant** chat streams, send explicit `apps` / `kb` lists.
            For **agent** chat streams, send explicit id lists, or **omit** `filters` (and `tools`)
            to let the service use the agent’s stored knowledge and tool configuration. Sending
            `{ \"apps\": [], \"kb\": [] }` on an agent stream means **no** knowledge sources for that
            turn (it is not “full org default”).

        :param model_key: Identifier of the AI model configuration to use for regeneration.
            Typically a UUID returned by the model-management endpoints. When
            omitted, the model used for the original message is reused.

        :param model_name: Provider model name (e.g. the underlying LLM identifier).
        :param model_friendly_name: Friendly display name of the selected model.
        :param timezone: IANA timezone identifier from the client. Used to provide
            time-aware context to the AI during regeneration.

        :param current_time: ISO 8601 / RFC 3339 datetime from the client (UTC `Z` or numeric
            offset). Used to anchor any relative time references in the query.

        :param tools: Optional list of tool identifiers (fully-qualified action names
            such as `jira.create_issue`) the agent may invoke when
            regenerating. Applicable only in agent chat modes.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary. Kept in the schema for backward
            compatibility with callers that already send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.RegenerateAgentConversationMessageRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            message_id=message_id,
            body=models_.AgentRegenerateRequest(
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                chat_mode=chat_mode,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/message/{messageId}/regenerate",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentRegenerateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="regenerateAgentConversationMessage",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStream(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models_.AgentRegenerateSSEEvent),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = utils.stream_to_text(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    async def regenerate_agent_conversation_message_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        message_id: str,
        chat_mode: models_.AgentRegenerateRequestChatMode,
        filters: Optional[Union[models_.Filters, models_.FiltersTypedDict]] = None,
        model_key: Optional[str] = None,
        model_name: Optional[str] = None,
        model_friendly_name: Optional[str] = None,
        timezone: Optional[str] = None,
        current_time: Optional[datetime] = None,
        tools: Optional[List[str]] = None,
        protocol: Optional[models_.AgentRegenerateRequestProtocol] = None,
        agent_capabilities: Optional[
            Union[models_.AgentCapabilities, models_.AgentCapabilitiesTypedDict]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStreamAsync[models_.AgentRegenerateSSEEvent]:
        r"""Regenerate agent conversation message

        Regenerate the AI response for a specific message in an agent
        conversation and stream the new answer over Server-Sent Events.

        **Constraints:**

        - Only the last message in the conversation can be regenerated.
        - The target message must be of type `bot_response`.

        **Request body:**

        `chatMode: quick` is required. Other fields are optional and reuse
        the original model/context when omitted. The body supports:
        - `filters`
        - `chatMode`
        - `modelKey`
        - `modelName`
        - `modelFriendlyName`
        - `timezone`
        - `currentTime`
        - `tools`
        - `protocol`
        - `agentCapabilities`

        **Streaming behavior:**

        The response is delivered as an AG-UI `text/event-stream`. Stable
        outcomes are `RUN_FINISHED` and `RUN_ERROR`; see
        `AgentRegenerateSSEEvent`.

        Additional agent/tool lifecycle events may be forwarded by the
        backend and should be treated as informational updates.

        Validation failures on params/body are returned as normal HTTP `400`
        responses before the stream starts. Valid-shape requests that fail
        conversation lookup or regenerate rules are reported as
        `RUN_ERROR` events after stream initialization.


        :param agent_key: Stable key identifying the agent that owns this conversation.
        :param conversation_id: ID of the agent conversation containing the target message.
        :param message_id: ID of the bot-response message to regenerate.
        :param chat_mode:
        :param filters: App connector instance ids and knowledge-base / record-group ids that narrow retrieval
            for a turn. For **org assistant** chat streams, send explicit `apps` / `kb` lists.
            For **agent** chat streams, send explicit id lists, or **omit** `filters` (and `tools`)
            to let the service use the agent’s stored knowledge and tool configuration. Sending
            `{ \"apps\": [], \"kb\": [] }` on an agent stream means **no** knowledge sources for that
            turn (it is not “full org default”).

        :param model_key: Identifier of the AI model configuration to use for regeneration.
            Typically a UUID returned by the model-management endpoints. When
            omitted, the model used for the original message is reused.

        :param model_name: Provider model name (e.g. the underlying LLM identifier).
        :param model_friendly_name: Friendly display name of the selected model.
        :param timezone: IANA timezone identifier from the client. Used to provide
            time-aware context to the AI during regeneration.

        :param current_time: ISO 8601 / RFC 3339 datetime from the client (UTC `Z` or numeric
            offset). Used to anchor any relative time references in the query.

        :param tools: Optional list of tool identifiers (fully-qualified action names
            such as `jira.create_issue`) the agent may invoke when
            regenerating. Applicable only in agent chat modes.

        :param protocol: AG-UI is the only supported wire protocol. When present must be
            `\"agui\"`. Omitting the field is equivalent — the server always
            uses the AG-UI vocabulary. Kept in the schema for backward
            compatibility with callers that already send it.

        :param agent_capabilities: Per-request agent capability toggles. Only meaningful when `chatMode`
            selects an agent mode; ignored otherwise. Each field falls back to its
            own `default` below when omitted — a missing flag is not uniformly
            `true`. Omitting the whole object applies every default.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.RegenerateAgentConversationMessageRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            message_id=message_id,
            body=models_.AgentRegenerateRequest(
                filters=utils.get_pydantic_model(filters, Optional[models_.Filters]),
                model_key=model_key,
                model_name=model_name,
                model_friendly_name=model_friendly_name,
                chat_mode=chat_mode,
                timezone=timezone,
                current_time=current_time,
                tools=tools,
                protocol=protocol,
                agent_capabilities=utils.get_pydantic_model(
                    agent_capabilities, Optional[models_.AgentCapabilities]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/message/{messageId}/regenerate",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.AgentRegenerateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="regenerateAgentConversationMessage",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            stream=True,
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStreamAsync(
                http_res,
                lambda raw: utils.unmarshal_json(raw, models_.AgentRegenerateSSEEvent),
                client_ref=self,
            )
        if utils.match_response(http_res, ["400", "401", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        http_res_text = await utils.stream_to_text_async(http_res)
        raise errors.PipeshubDefaultError(
            "Unexpected response received", http_res, http_res_text
        )

    def update_agent_conversation_message_feedback(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        message_id: str,
        is_helpful: Optional[bool] = None,
        categories: Optional[List[models_.MessageFeedbackSubmitRequestCategory]] = None,
        comments: Optional[
            Union[
                models_.MessageFeedbackSubmitRequestComments,
                models_.MessageFeedbackSubmitRequestCommentsTypedDict,
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.MessageFeedbackUpdateResponse:
        r"""Submit feedback for an agent message

        Append structured feedback to a bot-response message in an agent
        conversation. Uses the same request body shape as
        `updateMessageFeedback` (helpfulness, categories, comments). Feedback
        can only be submitted on `bot_response` messages.


        :param agent_key: Unique agent identifier (gateway Zod requires non-empty string).
        :param conversation_id: Unique conversation identifier.
        :param message_id: Identifier of the bot-response message being rated.
        :param is_helpful: Overall helpfulness signal (thumbs up/down).
        :param categories: Issue or positive categories that apply to the response.
        :param comments: Free-text comments grouped by sentiment.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentConversationMessageFeedbackRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            message_id=message_id,
            body=models_.MessageFeedbackSubmitRequest(
                is_helpful=is_helpful,
                categories=categories,
                comments=utils.get_pydantic_model(
                    comments, Optional[models_.MessageFeedbackSubmitRequestComments]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/message/{messageId}/feedback",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.MessageFeedbackSubmitRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgentConversationMessageFeedback",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.MessageFeedbackUpdateResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_agent_conversation_message_feedback_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        message_id: str,
        is_helpful: Optional[bool] = None,
        categories: Optional[List[models_.MessageFeedbackSubmitRequestCategory]] = None,
        comments: Optional[
            Union[
                models_.MessageFeedbackSubmitRequestComments,
                models_.MessageFeedbackSubmitRequestCommentsTypedDict,
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.MessageFeedbackUpdateResponse:
        r"""Submit feedback for an agent message

        Append structured feedback to a bot-response message in an agent
        conversation. Uses the same request body shape as
        `updateMessageFeedback` (helpfulness, categories, comments). Feedback
        can only be submitted on `bot_response` messages.


        :param agent_key: Unique agent identifier (gateway Zod requires non-empty string).
        :param conversation_id: Unique conversation identifier.
        :param message_id: Identifier of the bot-response message being rated.
        :param is_helpful: Overall helpfulness signal (thumbs up/down).
        :param categories: Issue or positive categories that apply to the response.
        :param comments: Free-text comments grouped by sentiment.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentConversationMessageFeedbackRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            message_id=message_id,
            body=models_.MessageFeedbackSubmitRequest(
                is_helpful=is_helpful,
                categories=categories,
                comments=utils.get_pydantic_model(
                    comments, Optional[models_.MessageFeedbackSubmitRequestComments]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/message/{messageId}/feedback",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body, False, False, "json", models_.MessageFeedbackSubmitRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgentConversationMessageFeedback",
                oauth2_scopes=["agent:execute"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.MessageFeedbackUpdateResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def archive_agent_conversation(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationArchiveResponse:
        r"""Archive an agent conversation

        Marks the conversation as archived for the authenticated owner.

        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ArchiveAgentConversationRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/archive",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="archiveAgentConversation",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationArchiveResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def archive_agent_conversation_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationArchiveResponse:
        r"""Archive an agent conversation

        Marks the conversation as archived for the authenticated owner.

        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ArchiveAgentConversationRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/archive",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="archiveAgentConversation",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationArchiveResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def unarchive_agent_conversation(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationUnarchiveResponse:
        r"""Unarchive an agent conversation

        Restores an archived agent conversation to the active list.

        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UnarchiveAgentConversationRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/unarchive",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="unarchiveAgentConversation",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationUnarchiveResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def unarchive_agent_conversation_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationUnarchiveResponse:
        r"""Unarchive an agent conversation

        Restores an archived agent conversation to the active list.

        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UnarchiveAgentConversationRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request_async(
            method="POST",
            path="/agents/{agentKey}/conversations/{conversationId}/unarchive",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="unarchiveAgentConversation",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationUnarchiveResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def update_agent_conversation_title(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        title: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationTitleUpdateResponse:
        r"""Update agent conversation title

        Updates the display title for an agent conversation owned by the caller.

        The controller looks up the conversation by `_id`, `orgId`, `userId`,
        `agentKey`, and `isDeleted: false`.

        The request body uses the shared title validator (`1..200` chars), and
        the controller trims the incoming title before saving it. A whitespace-only
        title can therefore still return HTTP 400 even if the raw string is
        non-empty.


        :param agent_key:
        :param conversation_id:
        :param title: New title for the conversation
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentConversationTitleRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            body=models_.ConversationTitleUpdateRequest(
                title=title,
            ),
        )

        req = self._build_request(
            method="PATCH",
            path="/agents/{agentKey}/conversations/{conversationId}/title",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models_.ConversationTitleUpdateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgentConversationTitle",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationTitleUpdateResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def update_agent_conversation_title_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        title: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationTitleUpdateResponse:
        r"""Update agent conversation title

        Updates the display title for an agent conversation owned by the caller.

        The controller looks up the conversation by `_id`, `orgId`, `userId`,
        `agentKey`, and `isDeleted: false`.

        The request body uses the shared title validator (`1..200` chars), and
        the controller trims the incoming title before saving it. A whitespace-only
        title can therefore still return HTTP 400 even if the raw string is
        non-empty.


        :param agent_key:
        :param conversation_id:
        :param title: New title for the conversation
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.UpdateAgentConversationTitleRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            body=models_.ConversationTitleUpdateRequest(
                title=title,
            ),
        )

        req = self._build_request_async(
            method="PATCH",
            path="/agents/{agentKey}/conversations/{conversationId}/title",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.body,
                False,
                False,
                "json",
                models_.ConversationTitleUpdateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="updateAgentConversationTitle",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationTitleUpdateResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401", "404"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def delete_agent_conversation_by_id(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationDeleteResponse:
        r"""Delete an agent conversation

        Soft-deletes an agent conversation owned by the authenticated user.

        The controller scopes the lookup by `_id`, `orgId`, `userId`, and
        `agentKey`. If no matching writable conversation is found, the route is
        intentionally a no-op and still returns HTTP 200 with `conversation: null`.

        This makes the operation idempotent:

        - deleting a nonexistent conversation returns success with `null`
        - deleting through a different `agentKey` returns success with `null`
        - deleting an already deleted conversation returns success with `null`


        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentConversationByIDRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/agents/{agentKey}/conversations/{conversationId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgentConversationById",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationDeleteResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def delete_agent_conversation_by_id_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationDeleteResponse:
        r"""Delete an agent conversation

        Soft-deletes an agent conversation owned by the authenticated user.

        The controller scopes the lookup by `_id`, `orgId`, `userId`, and
        `agentKey`. If no matching writable conversation is found, the route is
        intentionally a no-op and still returns HTTP 200 with `conversation: null`.

        This makes the operation idempotent:

        - deleting a nonexistent conversation returns success with `null`
        - deleting through a different `agentKey` returns success with `null`
        - deleting an already deleted conversation returns success with `null`


        :param agent_key:
        :param conversation_id:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.DeleteAgentConversationByIDRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/agents/{agentKey}/conversations/{conversationId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="deleteAgentConversationById",
                oauth2_scopes=["agent:write"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "500", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationDeleteResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def get_agent_conversation_by_id(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[models_.GetAgentConversationByIDSortBy] = "createdAt",
        sort_order: Optional[models_.GetAgentConversationByIDSortOrder] = "desc",
        start_date: Optional[datetime] = None,
        end_date: Optional[datetime] = None,
        message_type: Optional[models_.GetAgentConversationByIDMessageType] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationDetailResponse:
        r"""Get agent conversation by ID

        Returns the conversation with paginated/sorted messages and filter metadata.

        **Message Pagination:**

        Messages are paginated newest-first: `page=1` returns the most recent
        batch. Increment `page` to load older batches (used by the infinite-scroll
        \"load older messages\" feature).

        - `page`: Page number (default: 1)
        - `limit`: Messages per page (default: 20, max: 100)


        :param agent_key:
        :param conversation_id:
        :param page: Page number for message pagination (1 = most recent batch)
        :param limit: Number of messages per page
        :param sort_by: Field to sort messages by
        :param sort_order: Sort direction
        :param start_date: Filter messages created on or after this date
        :param end_date: Filter messages created on or before this date
        :param message_type: Filter messages by type
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.GetAgentConversationByIDRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            start_date=start_date,
            end_date=end_date,
            message_type=message_type,
        )

        req = self._build_request(
            method="GET",
            path="/agents/{agentKey}/conversations/{conversationId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getAgentConversationById",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationDetailResponse, http_res
            )
        if utils.match_response(http_res, ["401", "404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def get_agent_conversation_by_id_async(
        self,
        *,
        agent_key: str,
        conversation_id: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[models_.GetAgentConversationByIDSortBy] = "createdAt",
        sort_order: Optional[models_.GetAgentConversationByIDSortOrder] = "desc",
        start_date: Optional[datetime] = None,
        end_date: Optional[datetime] = None,
        message_type: Optional[models_.GetAgentConversationByIDMessageType] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationDetailResponse:
        r"""Get agent conversation by ID

        Returns the conversation with paginated/sorted messages and filter metadata.

        **Message Pagination:**

        Messages are paginated newest-first: `page=1` returns the most recent
        batch. Increment `page` to load older batches (used by the infinite-scroll
        \"load older messages\" feature).

        - `page`: Page number (default: 1)
        - `limit`: Messages per page (default: 20, max: 100)


        :param agent_key:
        :param conversation_id:
        :param page: Page number for message pagination (1 = most recent batch)
        :param limit: Number of messages per page
        :param sort_by: Field to sort messages by
        :param sort_order: Sort direction
        :param start_date: Filter messages created on or after this date
        :param end_date: Filter messages created on or before this date
        :param message_type: Filter messages by type
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.GetAgentConversationByIDRequest(
            agent_key=agent_key,
            conversation_id=conversation_id,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            start_date=start_date,
            end_date=end_date,
            message_type=message_type,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents/{agentKey}/conversations/{conversationId}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="getAgentConversationById",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["401", "404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationDetailResponse, http_res
            )
        if utils.match_response(http_res, ["401", "404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    def list_agent_conversations(
        self,
        *,
        agent_key: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[str] = None,
        sort_order: Optional[str] = None,
        search: Optional[str] = None,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None,
        status: Optional[str] = None,
        is_archived: Optional[models_.IsArchived] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationListResponse:
        r"""List agent conversations

        Paginated list of conversations for the agent (owned and shared-with-me),
        excluding archived threads.


        :param agent_key: Agent identifier used to scope the conversation list.
        :param page: 1-based page number. Defaults to `1`.
        :param limit: Page size. Defaults to `20`; maximum `100`.
        :param sort_by: Preferred sort field. Supported values are `createdAt`,
            `lastActivityAt`, and `title`.

            The current gateway validator preserves legacy behavior: unsupported
            values are accepted but ignored, and the handler falls back to
            `lastActivityAt`.

        :param sort_order: Preferred sort direction. Supported values are `asc` and `desc`.

            The current gateway validator preserves legacy behavior: unsupported
            values are accepted but ignored, and the handler falls back to
            descending order.

        :param search: Case-insensitive search term applied to conversation `title` and
            `messages.content`. Maximum length is 1000 characters. HTML/XSS
            payloads and format specifiers are rejected.

        :param start_date: Inclusive lower bound on `createdAt`. The handler accepts any
            JavaScript-parseable date string; invalid values return HTTP 400.

        :param end_date: Inclusive upper bound on `createdAt`. The handler accepts any
            JavaScript-parseable date string; invalid values return HTTP 400.

        :param status: Optional status filter applied to the `sharedWithMeConversations`
            branch of the response. The main `conversations` list ignores this
            parameter.

        :param is_archived: Optional archived flag applied to the `sharedWithMeConversations`
            branch before the route-level non-archived guard is enforced.
            Accepted values are `true` and `false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentConversationsRequest(
            agent_key=agent_key,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            search=search,
            start_date=start_date,
            end_date=end_date,
            status=status,
            is_archived=is_archived,
        )

        req = self._build_request(
            method="GET",
            path="/agents/{agentKey}/conversations",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentConversations",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationListResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)

    async def list_agent_conversations_async(
        self,
        *,
        agent_key: str,
        page: Optional[int] = 1,
        limit: Optional[int] = 20,
        sort_by: Optional[str] = None,
        sort_order: Optional[str] = None,
        search: Optional[str] = None,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None,
        status: Optional[str] = None,
        is_archived: Optional[models_.IsArchived] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models_.AgentConversationListResponse:
        r"""List agent conversations

        Paginated list of conversations for the agent (owned and shared-with-me),
        excluding archived threads.


        :param agent_key: Agent identifier used to scope the conversation list.
        :param page: 1-based page number. Defaults to `1`.
        :param limit: Page size. Defaults to `20`; maximum `100`.
        :param sort_by: Preferred sort field. Supported values are `createdAt`,
            `lastActivityAt`, and `title`.

            The current gateway validator preserves legacy behavior: unsupported
            values are accepted but ignored, and the handler falls back to
            `lastActivityAt`.

        :param sort_order: Preferred sort direction. Supported values are `asc` and `desc`.

            The current gateway validator preserves legacy behavior: unsupported
            values are accepted but ignored, and the handler falls back to
            descending order.

        :param search: Case-insensitive search term applied to conversation `title` and
            `messages.content`. Maximum length is 1000 characters. HTML/XSS
            payloads and format specifiers are rejected.

        :param start_date: Inclusive lower bound on `createdAt`. The handler accepts any
            JavaScript-parseable date string; invalid values return HTTP 400.

        :param end_date: Inclusive upper bound on `createdAt`. The handler accepts any
            JavaScript-parseable date string; invalid values return HTTP 400.

        :param status: Optional status filter applied to the `sharedWithMeConversations`
            branch of the response. The main `conversations` list ignores this
            parameter.

        :param is_archived: Optional archived flag applied to the `sharedWithMeConversations`
            branch before the route-level non-archived guard is enforced.
            Accepted values are `true` and `false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models_.ListAgentConversationsRequest(
            agent_key=agent_key,
            page=page,
            limit=limit,
            sort_by=sort_by,
            sort_order=sort_order,
            search=search,
            start_date=start_date,
            end_date=end_date,
            status=status,
            is_archived=is_archived,
        )

        req = self._build_request_async(
            method="GET",
            path="/agents/{agentKey}/conversations",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="listAgentConversations",
                oauth2_scopes=["agent:read"],
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models_.Security
                ),
            ),
            request=req,
            error_status_codes=["400", "401", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models_.AgentConversationListResponse, http_res
            )
        if utils.match_response(http_res, ["400", "401"], "application/json"):
            response_data = unmarshal_json_response(errors.ErrorResponseData, http_res)
            raise errors.ErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.PipeshubDefaultError(
                "API error occurred", http_res, http_res_text
            )

        raise errors.PipeshubDefaultError("Unexpected response received", http_res)
